# dosbox-automation: MCP capability plan

Three things make the current surface weak for an agent, and almost everything else is downstream of them:

1. **No way to wait.** Every "is it done yet" is a poll loop. Each iteration costs a turn, a round trip, and a full result block in the transcript.
2. **No way to tell why something failed.** Bridge timeouts, bad parameters and genuine crashes all arrive as `500 Internal server error`, and the bridge reports refusals as successes while reporting 4xx as errors.
3. **Several advertised capabilities do not work at all.** `script_run` raises AttributeError on every call. `bridge_start` fails on macOS. `type: "memory"` breakpoints return 200 and never fire. Absolute mouse coordinates are validated, recorded, and then discarded.

Fix those before adding surface. Ordering below is impact per effort within each tier.

Effort scale: S = under a day, M = a few days, L = a week or more, XL = multi-week.

**Progress: 1.1-1.8, 2.1-2.13, 2.15-2.17 done. Rest of tiers 2-4 outstanding.**

---

## Tier 1: foundations

Nothing above this tier is worth building until these land. Items 1.1 through 1.4 are defect repair; 1.5 through 1.8 are the substrate every later tool inherits.

### 1.1 Fix script_run
**Bridge. Effort: S. Status: done.**

`tools/script.py:54` calls `client.post_text(...)`. In production `client` is the `Connection` object (`server.py:87-89`), which defines only get/post/put/delete (`connection.py:178-188`). `post_text` exists only on `DosboxClient`. Every `script_run` call raises AttributeError. The unit test passes a hand-rolled fake that has the method, so it ships green.

Add the delegator, with the signature the verifier corrected (`Connection.call` is `call(self, method, path, **kwargs)`, so a third positional raises TypeError):

```python
def post_text(self, path, text, **kw):
    return self.call("post_text", path, text=text, **kw)
```

Add `params` passthrough. Write the regression test through a real `Connection` wired to `httpx.MockTransport` (the pattern exists in `tests/test_connection.py:25,37-51`), never another fake, because a fake is what hid the bug.

**Why the agent is better off:** this one line restores every blocking primitive the REST surface lacks. `dosbox.wait_for_text`, buffer-gated `dosbox.type`, `screen_match`, `wait_frames` all become reachable immediately, as a stopgap until 1.8 lands.

**Risk:** none technical. But note that fixing it opens the capability-mode hole described in 3.1: `script` is in `_INTERACT_GROUPS` while Lua exposes `mem_write`, so interact mode will start granting the memory writes it claims to withhold.

**Shipped:** exactly as proposed. `Connection.post_text()` delegator added; regression test goes through a real `Connection` wired to `httpx.MockTransport` in `tests/test_server.py`, confirmed it reproduces the original `AttributeError` on the pre-fix code.

### 1.2 Fix bridge_start on macOS
**Bridge. Effort: S. Status: done, two deviations from the proposal below, both verified against the real macOS build.**

`lifecycle.py:121-131` writes a conf file and points the child at it with `XDG_CONFIG_HOME` only. `src/misc/cross.cpp:54-64` shows macOS resolves the config dir through `HOME` and never reads `XDG_CONFIG_HOME`. So on Darwin the conf is ignored, `webserver_enabled` stays false, no token file appears, and `_wait_for_token` stalls 30s then kills the child with a message blaming the engine. This is the documented way an agent obtains a machine.

Stop depending on config discovery. `build_argv()` gains `--noprimaryconf --nolocalconf --set webserver_enabled=true --set webserver_port=N --set webserver_bind_address=127.0.0.1 --set webserver_token_file=true`. All three flags exist (`config.cpp:571,572,608`). Set `HOME` as well as `XDG_CONFIG_HOME`. Compute `token_path` with the same per-platform rule `default_token_path()` implements, parameterized on the overridden home rather than `os.environ`. Keep writing the conf file for `mount_allowed_bases` / `mount_allowed_image_roots`, which are read only from the primary config by design.

**Verify before shipping:** `lifecycle.py:129` sets `SDL_VIDEODRIVER=dummy`; the engine's own harness (`conftest.py:69`) uses `offscreen`. If the dummy driver never reaches `GFX_EndUpdate`, then under `bridge_start` freezes stop being re-asserted, frame-numbered sequences never dispatch, and every Lua script hangs at its first wait, all with 200 OK. Match the harness or prove `dummy` presents. Add an assertion that `GFX_GetRenderedFrameCount()` advances.

**Test fix required:** `tests/test_lifecycle.py`'s fake engine implements the Windows/Linux token path on every platform, which is why CI cannot see this. Make it mirror the real per-platform rule.

**Shipped, deviating on two points found while implementing:**
1. Dropped the token-file approach entirely in favor of `DOSBOX_API_TOKEN` (webserver.cpp's "channel A": a launcher-supplied token via env var, already supported precisely so a launcher never has to read a file or scrape logs). `InstanceManager` now generates the token itself and retries the authenticated attach until the child is reachable, instead of polling for a token file. This sidesteps the `default_token_path()` platform-parity work entirely, not just on macOS.
2. Tested both `dummy` and `offscreen` against the real macOS binary: `offscreen` (the plan's suggestion, matching the engine's Linux e2e harness) **aborts** on this SDL3 build (`SDL: Could not initialize SDL render backend`). Kept `dummy`, which starts cleanly and serves the webserver correctly — the plan had the risk assessment backwards for this platform.

`build_argv()` now matches the engine's own working e2e harness pattern: `--noprimaryconf --nolocalconf --set webserver_enabled=true --set webserver_port=N`, with both `HOME` and `XDG_CONFIG_HOME` redirected. Verified end-to-end: spawned the real binary, authenticated attach, feature negotiation, and a real `/api/v1/status` call, all through the production `Connection`/`_make_attach` path.

### 1.3 video/frame: validate before acquiring, RAII the frame
**Engine. Effort: S. Status: done.**

`VideoHandlers::GetFrame` (`video.cpp:211-270`) deep-copies the framebuffer at line 231 or 240 and frees it with a single unguarded statement at 269. `num_param<int>(req, Source::Param, "quality", 1, 100)` at line 263 throws on `quality=0`, `quality=101` or `quality=abc`, unwinding past the free. Up to 1.2 MB leaked per malformed request, loopable from a shell. `screen_capture` is the most-called tool in an agent loop and therefore the most likely to carry a typo'd parameter.

Parse mode, format and quality first, then acquire, then encode. Wrap the `RenderedImage` in a scope guard so both the shared-frame and rendered-tap paths free on every exit. While in there, override libjpeg's `error_exit` (`video.cpp:96` leaves the default, which calls `exit()`).

**Why the agent is better off:** a long session stops slowly leaking the emulator to death from its own typos, and a bad parameter fails in microseconds instead of after the 2000 ms rendered-frame wait.

**Prerequisite for:** 3.3 (screen_capture parameters). Do not add more parameters to that handler before this restructure.

**Shipped**, plus a second bug found while implementing the libjpeg `error_exit` fix: `buf`/`buf_size` (from `jpeg_mem_dest`) have to be declared *before* the `setjmp` call, not after, or the longjmp error path can't reach them to free a partially-encoded buffer. `FrameGuard` (a small local RAII wrapper) applied to both `GetFrame` and `GetFrameInfo`.

### 1.4 Trust-boundary validation sweep
**Engine. Effort: M to L. Status: done.**

Four concrete defects on the boundary the project's own rules call the trust boundary, plus the structural reason they were not caught.

- **Remote heap corruption.** `FreeMemoryCommand::Post` (`dos.cpp:273-284`) reads `addr` with no range check. Above `XMS_START*MEM_PAGE_SIZE` it calls `MEM_ReleasePages(addr / MEM_PAGE_SIZE)`, whose loop (`memory.cpp:346-352`) indexes `memory.mhandles` (4096 entries at 16 MB) with no bounds check. `addr=0xFFFFFFFF` writes megabytes past the vector, then follows the garbage it read.
- **Silent truncation.** `AllocMemoryCommand`'s ctor takes `uint16_t bytes` (`private/dos.h:55`) while `Post` reads a `uint32_t size` (`dos.cpp:191`) and passes it at `:241`. A request for 65536 allocates 0 and returns a valid-looking address.
- **Freeze range check wraps.** `freeze.cpp:103` is `address + width > mem_total` in 32-bit arithmetic. `address=0xFFFFFFFE, width=4` passes, and `ApplyFreezes` then writes past the end every rendered frame. `memory.cpp:87` does the same check correctly with a 64-bit cast.
- **Replay timing is unbounded doubles.** `input.cpp:418-438` checks only `< 0`; `cps` (`input.cpp:983`) only `<= 0`. `{"t": 1e308}` reaches `PIC_AddEvent` and `PIC_MakeCycles`, which asserts in debug (one request kills the process) and is UB in release. `frame` (`input.cpp:443`) is an unbounded `uint64_t` and a negative JSON integer becomes `0xFFFF...`.

Fix each as a **pure validator** in the module's `private/*.h`, not inline in the handler, which is why `tests/webserver_freeze_tests.cpp` cannot reach the freeze bug today. Add `ValidateMemoryRange(addr, len, mem_total)` with 64-bit math and use it from read, write, search and freeze. Gate `memory/free` behind an allocation registry (mutex-guarded, capped, modelled on `FreezeRegistry`) so `MEM_ReleasePages` is only ever reached with a handle the engine minted, and add a bounds guard inside `MEM_ReleasePages` itself. Replace `io_port.cpp:45-46`'s `std::stoul/stoi` with `num_param`. Add `#include "util/checks.h"` and `CHECK_NARROWING()` to every `.cpp` in `src/webserver/`, file by file; that is what would have caught the alloc truncation.

**Deliverable is the test suite:** `tests/integration/test_malformed_input.py`, a fixed hostile corpus (negative, 2^31, 2^32-1, 1e308, empty body, wrong type, unknown field, 12 MiB body, wrong Content-Type, deep nesting) applied to every registered route, asserting the response is JSON, the status is in `{400,401,403,404,409,413,415,501}`, never 500, and `/api/v1/status` still answers afterwards. Wire it into `tests/run-e2e.py`, which currently reaches only 5 of 13 suites.

**Risk:** `CHECK_NARROWING` surfaces existing implicit conversions that each need a decision, not a blanket cast. Freeing an address the API did not allocate now fails instead of silently mangling state.

**Shipped, one deliberate scope cut:** did not add `CHECK_NARROWING()` project-wide, or even to the touched files. The macro's own header comment defines it as declaring a file free of narrowing issues; `dos.cpp` still has unrelated pre-existing narrowing conversions (e.g. `DosInternalsCommand::Execute`'s `PhysPt`->`uint16_t` assignments) that are out of scope for this fix, so adding the macro would have misrepresented the file's state. All four bugs fixed with explicit runtime validation instead (`AllocationRegistry` gating `FreeMemoryCommand`/`AllocMemoryCommand`, `ValidateFreezeRange`, bounded `t`/`delay_ms`/`frame`/`cps` in `input.cpp`, `num_param`-based parsing in `io_port.cpp`). Also found and fixed a bug the plan missed: `cps` near zero in `InputTypeCommand` derives the same unbounded-timeline crash via `step_ms = 1000/cps`, not just direct `t`/`delay_ms` injection.

20 new unit tests added (`webserver_alloc_tests.cpp`, `webserver_input_validation_tests.cpp`, extended `webserver_freeze_tests.cpp`), extracting pure validators for testability rather than testing the HTTP handlers directly, matching the codebase's existing convention (`ValidatePortRequest`, `ScanBufferForValue`). Verified end-to-end against the real binary: every fixed input (oversized alloc, bogus free address, the freeze wraparound address, malformed port query, zero-quality frame, `1e308`/near-zero `cps`) now returns 400 instead of corrupting state, leaking, or crashing.

### 1.5 Typed error envelope and correct isError polarity
**Engine + bridge + protocol. Effort: M. Status: done.**

`error_handler` (`webserver.cpp:62-90`) discards `e.what()` in every branch. The precise message `num_param` builds never reaches the caller, and `Bridge::ExecuteCommand`'s timeout (`bridge.cpp:41`) is indistinguishable from a crash. That timeout is the *routine* failure: neither SDL pause loop pumps `ProcessRequests`, so a minimized or unfocused window fails every bridge-backed route with an opaque 500. On the bridge, `client._handle` flattens everything to `RuntimeError(f"{status}: {msg}")` that no handler catches; `httpx.ReadTimeout` is not caught at all (`connection.py:167` catches only `ConnectError`/`RemoteProtocolError`), producing the literal two-word result `timed out` and leaving a dead client attached. And nothing in `dosbox_mcp/` ever sets `isError`, so guard refusals and lifecycle failures return as successes while genuine 4xx return as errors. The signal is inverted for the common cases.

Engine: keep throwing `std::invalid_argument` / `std::out_of_range` (do not plumb a new exception through `num_param`, which is instantiated in every handler). Change `error_handler` to emit `{"error": {"code", "message": e.what(), "retryable"}}` with the code derived from the caught type, keeping a top-level `error` string alias for one release. Add one dedicated `Webserver::BridgeTimeout` thrown from `ExecuteCommand`, mapped to **503 + Retry-After + retryable:true**, not 500. Unify the two pre-routing text/plain rejections (`Forbidden` at `:367`, `Unauthorized` at `:383`) onto the same JSON shape. **Keep the terminal `catch (const std::exception&)` generic**: invalid_argument/out_of_range messages are parameter names and address ranges and are safe to echo; an arbitrary exception from deeper in the emulator may carry paths.

Bridge: a `DosboxError` hierarchy carrying `.status` and `.code`; replace the `"401" in str(e)` substring match (`connection.py:130,171`) with a status check on the typed exception; catch `httpx.TimeoutException` and `TransportError` in both `call` and `_try_connect`, detaching on timeout; add a per-call timeout override (required by 1.8); wrap handlers so failures return `CallToolResult(isError=True)` with tool, route, code, message and hint; flip guard and NotConnected to isError.

**Why the agent is better off:** recovery hinges on "is this worth retrying, is it my fault, or is the machine wrong". A stable code plus `retryable` answers that in one field. `retryable:true` with "the emulator may be paused or minimized" turns today's mystery 500 into an instruction.

**Blocks:** 1.8 (client timeout handling), 3.1 (`outputSchema` cannot land while error paths return bare content lists).

**Shipped**, close to the proposal, plus a few corrections found while implementing:

- **Kept `error` as a plain string, added `error_code`/`retryable` as sibling fields**, not `{"error": {code, message, retryable}}`. Nesting would have been an actual breaking type change to an existing field (string -> object), not additive - PROTOCOL.md's own versioning rule treats that as a MAJOR break. `{error, error_code, retryable}` is what shipped, and matches "keeping a top-level error string alias" literally rather than contradicting it.
- Extracted the whole classification (exception type -> status/code/retryable) into `Webserver::ClassifyException(std::exception_ptr) -> ErrorInfo`, exposed via `webserver.h`, so it is unit-testable without a live server (`webserver_error_tests.cpp`, 7 tests) - `error_handler` itself is a thin wrapper now.
- **`DosboxError` is one class, not a hierarchy**: `.status`/`.code`/`.message`/`.retryable`/`.route`, all set from the engine's response. A single typed exception with structured fields covers every dispatch need (`e.status == 401`, `e.code == "bridge_timeout"`) without the added indirection of subclasses that behave identically.
- `httpx.TimeoutException` is already a subclass of `httpx.TransportError` (verified against the installed httpx, not assumed) - one `except httpx.TransportError` in both `call` and `_try_connect` covers connect errors, timeouts, and protocol errors uniformly; no separate catch needed.
- **"flip guard and NotConnected to isError" turned out to mean every ad-hoc `except NotConnected/LifecycleError/ToolProtectedKey/ValueError: return _text(str(e))` site**, not just the one inside `guard()`. `bridge.py`'s six unguarded handlers (`_connect`, `_start`, `_stop`, `_logs`, `_setup`, `_swagger`) had the identical bug for their own exception types; fixed all of them with the same `to_error_result()` helper for consistency, since leaving them half-fixed would have kept the exact same "signal inverted" bug for `bridge_start`/`bridge_stop`/`bridge_logs`/`bridge_setup` failures.
- Added `tool` and `route` to the error text (`"[port_write PUT /api/v1/io/port] port must be 0x0000..0xFFFF"`) and a short `hint` for cases where the message alone doesn't make the remedy obvious (retryable -> "may be transient"; `unauthorized` -> "bridge_connect will re-attach"; `not_connected` -> "call bridge_status or bridge_start"). `CallToolResult(isError=True)` is the *only* way to get isError set at all - the installed mcp SDK's `call_tool` wrapper hardcodes `isError=False` for any handler return that isn't already a full `CallToolResult` (verified against the installed SDK source, `mcp.server.lowlevel.server.Server.call_tool`).
- Per-call `timeout=` override added to every `DosboxClient` method and threaded through `Connection`'s existing `**kwargs` passthrough for free. Verified against real httpx internals (`request.extensions["timeout"]`) that omitting it still uses the 30s client default rather than accidentally passing `timeout=None` (which httpx treats as no timeout at all).

Verified end-to-end through the **real MCP SDK dispatch path** (`server.request_handlers[types.CallToolRequest]`, not a mock) against the real engine binary: a `port_write` with an out-of-range port returns `isError: True` with text `"[port_write PUT /api/v1/io/port] port must be 0x0000..0xFFFF"`; a valid `mem_read` returns `isError: False` with the normal JSON payload. 46 new tests total (7 engine-side `ClassifyExceptionTest`, the rest bridge-side across `test_client.py`/`test_connection.py`/`test_bridge_tools.py`).

### 1.6 Bridge hardening: never wedge, pump while paused, report liveness
**Engine. Effort: M. Status: done.**

Three gaps in the trust boundary itself.

- `ProcessRequests` (`bridge.cpp:45-59`) runs `cmd->Execute()` with no try/catch. A `std::bad_alloc` from `SearchMemoryCommand::Execute`'s 16 MiB resize or `LuaStatusCommand`'s unbounded serialization leaves `done` false for that command and every later one in the batch, never clears the queue, never notifies, and escapes into `normal_loop`.
- The queue is unbounded and there is no liveness signal.
- `ProcessRequests` is pumped only from `dosbox.cpp:124` and `debugger.cpp:2956`. Both SDL pause loops (`sdl_gui.cpp:350`, `:2229`) block on `SDL_WaitEvent` and never pump.

Wrap `Execute` in try/catch, recording the message into the command and setting `done` in all cases. Add `std::atomic<uint64_t> last_pump_ms` and a `PumpAgeMs()` accessor; refuse new commands with a distinct `not_pumping` code when the pump is stale and `queue_full` at a depth cap. Call `ProcessRequests()` and `LuaReapStalledWaits()` from both SDL pause loops (two lines each, already on the emulation thread). Extend `GET /api/v1/status` with `{emulation: running|paused|stalled, last_tick_ms_ago, frame}`; make `rendered_frame_count` (`sdl_gui.cpp:1164-1169`) a `std::atomic<uint64_t>` first, because `/status` does not cross the Bridge and reading it today would be an unsynchronized cross-thread read.

**Separate, land first:** `RecordingHandlers::PostPause/PostStop/GetStatus` (`input.cpp:751-818`) call `InputRecording::Stop` directly on the httplib worker, and `Stop` calls `OsdManager::SetIcon`. `OsdManager` has no mutex and no atomics, and its icon array is read by `Render` on the emulation thread. Route Pause and Stop through Commands like `StartRecordingCommand` already does. This is a correctness bug, not a feature; it should be its own commit.

**Why the agent is better off:** every failure mode currently arrives as the same 500. Distinguishing "paused, tell the human to focus the window" from "queue saturated, back off" from "the command failed" is the difference between recovering and looping. Pumping while paused is also what makes background automation reliable at all.

**Risk:** API-driven state changes now happen while the user believes the machine is paused. That is the correct trade for automation, but document it and keep the OSD indicators showing activity. Add `tests/webserver_bridge_tests.cpp` (the first ever): throwing Execute still drains the batch, a timed-out command is erased and never executed afterwards, queue-full is refused without dropping.

**Depends on:** 1.5 for the error codes it returns.

**Shipped, with one deviation from the literal "two lines each" for the SDL pause loops:**

- `ProcessRequests()` now try/catches each command's `Execute()` individually, records `e.what()` into `cmd->error`, and always sets `done = true` - so a throw mid-batch (bad_alloc, whatever) can no longer strand every command queued after it. Also dropped the `queue.empty()` early return so `last_pump_ms` refreshes on every pump, even an empty one - that refresh is the whole basis for `PumpAgeMs()`/`not_pumping`/`/status`, and normal_loop pumps constantly whether or not anything is queued.
- `Bridge::ExecuteCommand` checks `PumpAgeMs() > StalePumpThresholdMs` (1000ms) *before* acquiring the lock or enqueueing, throwing the new `BridgeNotPumping` - a stalled emulator is now diagnosed in microseconds instead of after the command's own timeout (up to 15000ms once 1.8 lands). A `MaxQueueDepth` (64) cap throws `BridgeQueueFull` on overflow; both new exception types get their own `ClassifyException` branches (`not_pumping`/503, `queue_full`/429, both retryable).
- **Deviation:** the plan's "two lines each" for the SDL pause loops (`sdl_gui.cpp`) undersold what "never wedge" requires. Both loops previously called plain `SDL_WaitEvent`, which blocks *indefinitely* with no OS event - inserting a pump call next to it would only pump once per actual event, and a backgrounded/minimized window can go arbitrarily long with none. Switched both to `SDL_WaitEventTimeout(..., PausePumpIntervalMs)` (50ms) so the loop pumps on a real cadence regardless of event traffic, well under the 1000ms staleness threshold so a paused window never reads as `stalled` to the API.
- `GET /api/v1/status` gained `emulation` (`running`/`paused`/`stalled`), `last_tick_ms_ago` (`Bridge::PumpAgeMs()`), and `frame` (`GFX_GetRenderedFrameCount()`). This handler deliberately does not cross the Bridge - a Command here would block or time out in exactly the `stalled` case it exists to report. `rendered_frame_count` (`sdl_gui.cpp`) became `std::atomic<uint64_t>` first, since this is now a genuine unsynchronized cross-thread read otherwise.
- **Landed as its own commit, ahead of the rest:** `RecordingHandlers::PostPause`/`PostStop` called `InputRecording::Pause`/`Stop` directly on the httplib worker thread; `Stop` touches `OsdManager::SetIcon`, which has no mutex and is read by `Render` on the emulation thread - a real data race, not a hypothetical one. Added `PauseRecordingCommand`/`StopRecordingCommand` alongside the existing `StartRecordingCommand`, both routed through the Bridge. `PauseRecordingCommand::Execute()` re-checks `IsRecording()` on the emulation thread rather than trusting the httplib thread's own pre-check, closing a TOCTOU the original code had (recording could stop between a worker-thread check and the actual pause).
- `tests/webserver_bridge_tests.cpp` (new): throwing `Execute()` still drains the batch, a timed-out command is erased and never executed by a later pump, `queue_full` is refused without dropping the commands already queued, and a genuinely stale pump refuses fast (sub-second) rather than waiting out the command's own timeout. `webserver_error_tests.cpp` gained `ClassifyException` coverage for both new exception types.

Verified end-to-end against the real binary: `GET /api/v1/status` reports `emulation: "running"`, a live `frame` count, and `last_tick_ms_ago` near zero; the full record start/pause/resume/stop/stop-again(409)/pause-again(409) lifecycle works correctly through the new Command-routed handlers; normal Bridge-backed routes (`cpu/state`) stay unaffected. Manufacturing a genuine `stalled`/`not_pumping` state against the live binary isn't practical to script safely (it requires the emulation thread to actually stop ticking), so that mechanism is covered by the deterministic unit tests instead; likewise the two SDL pause loops themselves need a real window/OS event source neither the `offscreen` nor `dummy` headless SDL video drivers provide, so their bodies are verified by code review plus the shared pump logic's unit tests rather than a live pause.

### 1.7 Change detection for screen text and frames
**Engine + bridge. Effort: M. Status: done.**

`video/text` returns the full padded grid and nothing else. An 80x25 DOS prompt arrives as one ~2050-character line of escaped `\n` that is 98% trailing spaces, re-paid on every poll. `video/frame` always returns a full image. There is no ETag anywhere in `src/webserver/` (the only conditional-request support is `If-Match` on memory PUT). There is also no cursor position, though `ScreenTextCommand::Execute` already reads the adjacent BIOS fields.

- **Text:** add `cursor_row`/`cursor_col` via `CURSOR_POS_ROW/COL` (`int10.h:310-320`, already included) using the page it already reads. Compute a 64-bit FNV-1a **on the web thread** in `Get`, over `cmd.text_dos` (raw CP437, before UTF-8 conversion). Accept `if_none_match`, guarded with `req.has_param` because `num_param` throws on an absent value; on a match return `{is_text_mode, text_hash, cursor_row, cursor_col, unchanged:true}` and omit `text`. Always return the cursor even in the unchanged response: it moves independently of the character grid and is exactly the "is it at a prompt yet" signal.
- **Frames:** do **not** hash in the handler. `RENDER_UpdateSharedFrame` (`render.cpp:19-25,47-53`) already holds the mutex and already deep-copies the whole framebuffer on every `EndUpdate`; compute the hash there and expose `RENDER_GetSharedFrameHash()` so the web thread reads a `uint64` with no copy. A frame counter alone is not a valid ETag, because the copy happens unconditionally on identical frames. Honour `If-None-Match` on `video/frame`, `video/frame/info` and `video/text` with a 304 and empty body. Make `frame/info` respect `mode=` (it ignores it today, so the geometry of a rendered capture is unknowable).
- **Bridge:** render the text grid as real text, not escaped JSON. rstrip rows, drop trailing blank rows, prefix row numbers, one header line: `80x25 mode 0x03 cursor 12,40 hash 0x9a3f (7 blank rows omitted)`.

**Why the agent is better off:** an unchanged screen poll drops from ~2050 characters to about 30, and the hash gives the agent a one-token handle for "this screen" so it can say "wait until it differs from 0x9a3f" instead of diffing grids in context.

**Be honest:** the Bridge round trip still happens. The saving is payload, not latency. On `mode=rendered` the 304 saves the encode and transfer, not the 2000 ms forced-present wait.

**Feeds:** 1.8's `screen_change` condition.

**Shipped, with real HTTP semantics resolved in favor of the more clearly-stated instruction where the two Text/Frames bullets disagreed:**

- The plan's Text bullet wanted a JSON-bearing "unchanged" response (`{is_text_mode, text_hash, cursor_row, cursor_col, unchanged:true}`) via a query-param `if_none_match`; the Frames bullet wanted a real `304` with an *empty* body via the real `If-None-Match` header, for all three routes. A `304` with a body is a spec violation (RFC 7232) that both httplib (server) and httpx (the bridge's own client) can silently mishandle - shipped as a true, empty-body `304` with the real header, uniformly across `video/frame`, `video/frame/info` and `video/text`. `text_hash`/`frame_hash` still ride in the normal `200` JSON body (`video/text`, `video/frame/info`) so the bridge can read them without needing header access it doesn't have today - `video/frame`'s binary response can only carry it via the `ETag` header.
- **Frames get an extra optimization the plan didn't fully spell out**: the shared-frame hash is checked *before* calling `RENDER_GetSharedFrame()`, so a match skips its mutex-locked deep copy entirely instead of paying for it and discarding the result. `mode=rendered` has no equivalent precomputed hash - it still pays the up-to-2s forced-present wait, matching the plan's own "be honest" note.
- `RENDER_UpdateSharedFrame`'s hash covers `height * abs(pitch)` bytes, matching `encode_raw`'s existing size computation for the same buffer (a flipped image is addressed via a negative pitch from its last row).
- New `src/utils/fnv_hash.h` (FNV-1a 64-bit, verified against the official reference test vectors, not just self-consistency) shared by both the frame and text hashing paths.
- `EtagMatches`/`FormatEtag` extracted as pure, header-exposed functions rather than kept as file-local `httplib::Request`-taking helpers - a first draft of the `If-None-Match` parsing hit a real dangling-`string_view` bug (caught by `-Wdangling-gsl`, not by review) from binding a view directly to a temporary; the pure-function form is both safer and directly unit-testable.
- Bridge: found and fixed a discrepancy in the same pass - `dos_to_utf8`'s row-separating newlines include one after the *last* row, so splitting `text` on `\n` yields one more element than the engine's own `rows` count, which was double-counted as a phantom extra blank row. Bounded to `rows` before counting.

Verified end-to-end against the real binary and through the real MCP tool dispatch path: `video/text` first request returns cursor position and hash with a full grid; a repeat with matching `If-None-Match` returns a true `304`/`Content-Length: 0`; `video/frame` and `video/frame/info` do the same and share the identical hash (same underlying frame); `video/frame/info?mode=rendered` now reports the actual rendered geometry (758x569) distinctly from the raw framebuffer's (720x400), where before it silently reported the wrong one regardless of `mode=`. The bridge's `screen_text` tool renders a real numbered grid instead of an escaped JSON blob, with the blank-row count now exactly matching the visible screen.

### 1.8 Server-side wait
**Engine + bridge. Effort: L. Status: done.**

The single largest token and turn saving available. There is no wait-for-condition anywhere in the API: no long-poll, no SSE, no streaming (verified by grep for `set_chunked_content_provider`). Every "has it finished" is a poll, and a 60-second wait at 1 Hz is 60 model turns. The engine's own `dosbox.wait_for_text` (`lua_api.cpp:513-585`) exists only inside Lua, behind a script upload and a 2-second load rate limiter.

`POST /api/v1/wait` with `{for, timeout_ms, ...}`, long-polled on the **web thread**, never inside a Bridge Command (a 30 s wait inside `Execute` would freeze the emulation thread and every other queued request). Implement a `WaitRegistry` holding validated predicates behind a mutex and condvar; the precedent is `RenderedFrameTap` (`frame_tap.cpp:56-104`), which is already a web-thread condvar wait fed from the render path.

Evaluate from **two** places: the frame hook where `ReplayDispatchFrame`, `ApplyFreezes` and `LuaDispatchFrame` already run in sequence (`sdl_gui.cpp:1233-1235`), and **from `DEBUG_Loop`** (`debugger.cpp:2951-2957`). Without the second, a `stopped` condition can never fire, because no frames are presented while stopped in the debugger. Conversely, `text` and `frames` waits are unsatisfiable while stopped: return a distinguishable `emulator_stopped` reason rather than a bare timeout. Same for the SDL pause loops after 1.6.

Conditions: `text` (via the already-exported `Lua::MatchSubstring` over `Lua::ReadScreenText`), `screen_change` (the hashes from 1.7), `frames`, `replay_done` (the `pending_dispatched`/`pending_total` counters at `input.cpp:176-186`, which exist and have no reader), `memory` (value compare, width 1/2/4), `stopped`, `script_done`, `program`.

Hard requirements:
- **Evaluate `ReadScreenText` once per frame and share the string.** Per-waiter evaluation is ~2000 `mem_readb` calls per waiter per frame at 70 fps; the waiter cap alone does not bound that.
- Validate on the web thread before registering: `for` a strict enum, `timeout_ms` clamped 1..15000 (not 30000, see below), pattern <= 256 bytes, memory range checked with 64-bit math.
- Cap concurrent waiters at 4 and return 429. httplib's pool is `max(8, hw_concurrency-1)`; unbounded waiters starve the server of the threads needed to recover.
- Drain and notify the registry in `WEBSERVER_Destroy`.
- Response carries the observation that satisfied it (the matched screen, the stop context), so no follow-up call is needed. That pattern should govern the whole surface.

Bridge: one `wait_for` tool, and a per-call timeout override, because `DosboxClient` pins `timeout=30.0` for everything (`client.py:20`). Clamping the server side to 15 s keeps a margin under the client timeout.

**Why the agent is better off:** "wait until the installer finishes" becomes one call and roughly 40 tokens instead of 60 polls at 700+ tokens each, and it removes the class of racy sleeps where the agent guesses a delay, screenshots, and guesses again.

**Depends on:** 1.5 (timeout exceptions are unhandled today and would surface as `timed out` with a dead client attached), 1.7 (hashes for `screen_change`), 1.6 (the paused case must return a clean timeout, not a 500).

**Optional generalization:** a `PolledCommand` subclass on the Bridge (`Poll()` evaluated outside the mutex from both existing pump sites) would make deferred execution reusable beyond waits. Not required for 1.8; consider it only if a second deferred consumer appears - skipped, as proposed, since nothing else needs it yet.

**Shipped, as proposed, with a few discoveries and one deliberate extension:**

- New `src/webserver/wait.h`/`wait.cpp`: a `WaitRegistry` singleton mirroring `RenderedFrameTap`'s mutex+condvar shape (not a Bridge Command - the wait itself blocks the web thread on a condvar, never the emulation thread). `WaitFor()` registers a waiter and blocks up to `timeout_ms`; `Tick(frames_flowing)` runs on the emulation thread and is the only thing that ever reads emulator-core state for this feature.
- All 8 conditions implemented: `text`, `screen_change`, `frames`, `replay_done`, `memory`, `stopped`, `script_done`, `program`. `screen_change` takes an explicit `baseline_hash` (reusing the `text_hash`/`frame_hash` a caller already has from 1.7, per `source: "text"|"frame"`) rather than capturing a baseline at registration time, so a caller never needs a throwaway read first. `program` supports both a `pattern` substring match and, when `pattern` is omitted, an edge-triggered "wait until the program name changes" form.
- `Tick()` is called from **three** places, not two: the frame hook (`GFX_EndUpdate`, `frames_flowing=true`), `DEBUG_Loop` (`frames_flowing=false`), and - per the plan's own "same for the SDL pause loops after 1.6" - both SDL pause loops added in 1.6 (`frames_flowing=false`). `ReadScreenText()` is memoized once per `Tick()` call via a lazy local, shared by both `text` and `screen_change(source=text)` regardless of how many waiters need it.
- **Extended** the plan's "text and frames waits are unsatisfiable while stopped" rule to `screen_change` and `replay_done` too - both are exactly as frame-hook-dependent as the two the plan named (replay dispatch only advances from the frame hook; a text/frame hash cannot change if no frame renders). All four resolve as `emulator_stopped` immediately on the first `frames_flowing=false` tick rather than waiting out the timeout; `memory`, `stopped`, `script_done` and `program` still progress normally while stopped or paused, since none of them depend on a frame being presented.
- **Discovery that changed scope:** the plan assumed `DosboxClient` needed a per-call timeout override added (`client.py:20` pins `timeout=30.0`). It's already there - every verb method already accepts an optional `timeout=` kwarg and forwards it, just unused by any existing tool. `wait_for` is the first caller; no bridge client change was needed, only using what was already plumbed through.
- **Addition beyond the plan's text:** `stopped` returns a `501` ("debugger capability not built in this binary") immediately when `C_DEBUGGER` is off, matching the existing convention in `debug.cpp`'s other debugger-gated routes, rather than silently timing out on every call for a condition that can never be satisfied in that build.
- Concurrent waiters capped at `MaxWaiters = 4`, refused with a new `TooManyWaiters` (429, `too_many_waiters`) distinct from `BridgeQueueFull` - this registry never touches the Bridge queue at all. `WaitRegistry::DrainAll()` is called first thing in `WEBSERVER_Destroy()`, ahead of `OSDPORT_Destroy()`/`server.stop()`, so an in-flight wait doesn't block shutdown from joining.
- A real validation bug caught during testing, not review: checking `is_number_unsigned()` on `addr`/`value` rejected values built from a plain C++ integer literal (nlohmann only tags the unsigned representation when JSON *text* had no leading `-`; a literal like `0x1000` is always the signed representation regardless of value). Fixed by validating via the inclusive `is_number_integer()` plus an explicit non-negative check, which is also more robust for real requests than depending on that representation detail.
- Bridge: new `dosbox_mcp/tools/wait.py`, one `wait_for` tool taking the same flat `{for, timeout_ms, ...}` shape as the engine route (extra fields the chosen condition ignores are harmless), registered read-only (so it needs no mode gating) in `server.py`'s tool-registration loop. Passes `args` straight through as the JSON body and sets the httpx per-call timeout to `timeout_ms/1000 + 5.0` so the engine's own deadline always fires first.

40 new engine-side tests (parsing/validation plus `WaitRegistry` mechanics - deliberately scoped to conditions that don't need a booted DOS session, since the unit-test binary never runs one; documented in the test file) and 4 bridge-side tests, all passing. Verified end-to-end against the real binary and through the real MCP tool dispatch path: `text`/`frames`/`replay_done` resolve correctly (including a real off-by-one caught live - `Z:\>` not `C:\>`, since the default drive is `Z:`, not a code bug); `screen_change`/`program` correctly time out when nothing changes; `memory` distinguishes a real timeout (address in range, value never matches) from an out-of-range address; the 5th concurrent waiter gets a real `429` while the other 4 resolve normally; a wait in flight during `/api/v1/control/shutdown` resolves with `reason: "shutting_down"` almost immediately instead of blocking; and the bridge's `wait_for` tool correctly reports `isError: false` for a legitimate timeout but `isError: true` for a bad condition (caught by the tool schema's own enum before the handler even runs) and for the `stopped`-without-debugger case.

---

## Tier 2: high-value additions

### 2A. Capability and debugger

#### 2.1 Capability descriptor with state and limits
**Engine + bridge + protocol. Effort: L. Status: done.**

Feature flags are the wrong granularity in three ways. They are fiction (`webserver.cpp:441-446` hardcodes memory, input, cpu_registers, cpu_control, port_io and freeze to literal `true`; only `debugger` is real), so `guard(feature=...)` can block exactly one group on exactly one build. They cannot say "present but degraded". And every operational limit is invisible: the 10 MiB body cap, the 250 ms Bridge timeout, 32000 events, 4096 chars, 256 KB scripts, the 16 MiB search span, the 128 MiB read cap.

The degraded case is the worst silent failure in the system. `DEBUG_Breakpoint`/`DEBUG_IntBreakpoint` are called only from `core_normal/prefix_none.h:739,752` and `core_full/op.h:347,349`. The dynrec and dyn_x86 cores call only `DEBUG_HeavyIsBreakpoint`, and every one of those is inside `#if C_HEAVY_DEBUGGER`. The existing `build-debugger/` tree has `OPT_DEBUGGER=ON, OPT_HEAVY_DEBUGGER=OFF`, so under the default `core=auto` an execute breakpoint has **no check site at all**: 200 OK from add, 200 OK from continue, and a program that never stops.

Add `capabilities[group] = {state: on|off|degraded, reason, limits:{}}` to `dosbox/info` as a 1.2 addition, keeping `features` as a boolean projection so 1.0/1.1 peers are untouched. Cover every served group, not just the optional ones. Report all three inputs to the breakpoint predicate (built, heavy, effective core) so the reason can be phrased accurately. Populate limits from the same named `constexpr` values the validators use so they cannot drift.

**Thread safety, mandatory:** do not add a plain `CPU_IsDynamicCoreActive()` over `is_dynamic_core_active()` (`cpu.cpp:160-169`). It reads the global `cpudecoder` pointer, which the emulation thread reassigns at runtime, and `dosbox/info` is a plain lambda that does not cross the Bridge. Routing info through the Bridge is worse: it is the call the bridge makes at attach, and a 250 ms timeout there means "cannot connect". Keep a `std::atomic<CoreKind>` written by `ConfigureCpuCore` and the auto-switch path, and read the atomic. The `core` setting is `WhenIdle`, so reading the Property object from the web thread is also a race.

**Scope:** land the descriptor and the limit constants first. The bridge-side "rewrite `inputSchema` maximums at attach and send `listChanged`" half is mechanically sound (mcp 1.29 does re-run the list handler and refill its cache) but needs `ToolsCapability(listChanged=True)` and a session, and the first `list_tools` may already have been served. The descriptor alone lets tool *descriptions* carry real numbers, which is most of the win.

Delete `dosbox_mcp/capabilities.py` (dead: imported only by its own test) or make it the single group-to-capability map `server.py:88-99` consumes.

**Why the agent is better off:** it stops discovering limits by failing, and it stops setting breakpoints that structurally cannot fire.

**Shipped**, as scoped (descriptor and limit constants; the bridge-side `inputSchema`/`listChanged` rewrite explicitly deferred), with one correction to the plan's own claim about dynamic-core breakpoints:

- New `src/webserver/capabilities.{h,cpp}`. `BuildCapabilitiesBlock()` returns `capabilities[group] = {state: on|off|degraded, reason, limits}` for 11 groups: the original 7 (`memory`, `input`, `cpu_registers`, `cpu_control`, `port_io`, `freeze`, `debugger`) plus `script`, `drive`, `capture`, `wait` (the last four were never in `features` before at all - new groups the old hardcoded block simply didn't cover). `FeaturesProjection()` collapses that back to the old boolean shape (`state != off`), so a 1.0/1.1 peer sees exactly what it always saw - `dosbox/info` still returns `features`, unchanged in shape, alongside the new `capabilities` and a sibling `limits` object for the two server-wide caps (`max_request_body_bytes`, `bridge_default_timeout_ms`) that don't belong to any one group.
- **CPU core tracking, new:** `enum class CoreKind { Normal, Simple, Full, Dynamic }` in `cpu.h`, plus `std::atomic<CoreKind> active_core_kind` in `cpu.cpp`, mirroring the `rendered_frame_count` pattern from 1.6 (`cpudecoder` itself is a plain, non-atomic function pointer written only on the emulation thread - unsafe to read from `dosbox/info`'s plain lambda, which must answer without crossing the Bridge). Written at `ConfigureCpuCore` and the runtime auto-switch in `CPU_SET_CRX`, per the plan; **also** written at the reverse switch in `CPU_RestoreRealModeCyclesConfig` (leaving `auto_core` protected mode and reverting to Normal), which the plan's text didn't call out explicitly but is required for the atomic not to go permanently stale after the first `auto`-mode excursion into protected mode. The `386_prefetch`/`486_prefetch` cputype override (which can force `CPU_Core_Prefetch_Run` independent of the `core` setting) is deliberately not tracked separately - Prefetch shares the identical breakpoint check site as Normal, so folding it into `CoreKind::Normal` doesn't change any capability verdict.
- **Correction, found while mapping the actual check sites (not assumed from the plan text):** the plan's motivating paragraph claims "under `core=auto` an execute breakpoint has no check site at all" for the dynamic/JIT cores. That's not quite right. Neither `core_dyn_x86.cpp` nor `core_dynrec.cpp` has a native call to the light `DEBUG_Breakpoint()` check, but they also don't decode `0xCC` (INT3) themselves - an untranslated `0xCC` (which is exactly what an execute breakpoint patches into guest memory) falls through to `BR_Opcode`, which runs that one instruction through the interpreter (`CPU_Core_Normal_Run`), and *that* does hit the check. So execute/interrupt breakpoints still fire on the dynamic core, just indirectly, via a per-instruction fallback rather than a call site native to the JIT. `ComputeDebuggerCapability()` reports this accurately: `degraded` (not `off`) with a reason naming the interpreter-fallback mechanism specifically for `CoreKind::Dynamic`, and naming the concrete core (`normal`/`simple`/`full`) otherwise. Memory (`type="memory"`) breakpoints are a separate, unconditional `off` component of the same `degraded` state whenever `C_HEAVY_DEBUGGER` is 0, regardless of core - that check site is fully compiled out project-wide, confirmed by grep, not just absent from the JIT cores.
- `ComputeDebuggerCapability(built, heavy, core)` is a pure function (built/heavy/core all parameters, not read from macros/atomics internally), so every combination is unit-testable regardless of what this binary actually has compiled in - 6 dedicated tests cover off/on/degraded across all four `CoreKind` values.
- **Limit constants hoisted out of function-local/magic-number scope so the descriptor can cite them without duplicating (and risking drift from) the enforced value:** `MaxRequestBodyBytes` (`webserver.h`, was a bare `10 * 1024 * 1024` literal at the `set_payload_max_length` call site), `MaxInputEvents`/`MaxTypedTextChars` (`input.h`, were function-local `constexpr` in `input.cpp`), `MaxMemoryTransferBytes`/`MaxSearchSpanBytes` (`private/memory.h` - the same `128 * 1024 * 1024` literal was duplicated at both the read-length `num_param` bound and the write-size check; now one named constant backs both), `DefaultBridgeTimeoutMs` (`bridge.h`, was a bare `250` default-argument literal). `MaxEventTimeMs`/`MaxEventFrame`/`MinTypingCps`/`MaxTypingCps` (already namespace-scope `constexpr` in `input.cpp`, but non-`inline` and therefore internal-linkage - moved into `input.h` as `inline constexpr` so `capabilities.cpp` can reference them too). Freeze (`FreezeRegistry::MaxEntries`), DOS allocations (`AllocationRegistry::MaxEntries`), Lua script (`Lua::ScriptValidator::MaxBodySize`, `Lua::LuaEngine::Default{MemoryCapBytes,InstructionLimit,WallClockLimitMs}`) and wait (`wait.h`'s `Min/Max/DefaultWaitTimeoutMs`, `MaxPatternLen`, `MaxWaiters`) constants were already class/namespace-scope and header-exposed - included directly, no changes needed.
- **Found and fixed in passing, unrelated to 2.1's own diff:** `tests/webserver_bridge_tests.cpp`'s `StalePumpIsRefusedFastWithoutQueuing` (from 1.6) relies on `Bridge::Instance()` already existing with an old `last_pump_ms` by the time its sleep completes; every other test in that file calls a `RefreshPump()` helper first to establish that baseline, but this one didn't. Under `ctest`'s default one-process-per-test isolation (`--gtest_filter`), nothing had touched the singleton yet, so it was lazily constructed - with `last_pump_ms` set to "now" - only *after* the sleep, making the test's own timeout window measure staleness against nothing. Reproduced deterministically (100% of runs, both isolated and as part of the full suite) on a clean pre-2.1 checkout, confirming it predates this work; fixed by adding the same `RefreshPump()` call every other test in the file already makes.
- Bridge: `Connection` gains a `capabilities` property, mirroring `features` - populated in `_try_connect` from `info.get("capabilities", {})`, cleared in `detach()`, included in `status()`. `guard()` now checks `connection.capabilities.get(feature)` first when present: `state == "off"` refuses with the engine's own `reason` folded into the message; `degraded` still passes the call through (matching what the boolean `features` projection already implies); an engine that predates 1.2 and sends no `capabilities` block at all falls back to the exact old boolean check, byte-for-byte unchanged. Deleted `dosbox_mcp/capabilities.py` and its test - confirmed dead (imported only by itself) before removing, per the research step, not assumed from the plan's own note.

18 new engine-side tests (`webserver_capabilities_tests.cpp`: `CapabilityStateNameTest`, `ComputeDebuggerCapabilityTest` x6, `BuildCapabilitiesBlockTest` x3, `FeaturesProjectionTest` x2, `BuildServerLimitsTest`) plus the `RefreshPump()` fix, and 9 new bridge-side tests (`test_connection.py`: 4 new `guard()` cases, 2 new `TestStatus` cases, extended `test_detach_clears_state`). Verified end-to-end against the real `build-debugger` binary (`C_DEBUGGER=1`, `C_HEAVY_DEBUGGER=0`): `GET /api/v1/dosbox/info` reports `debugger: {state: "degraded", reason: "...normal core", limits: {built: true, heavy_debugger_built: false, effective_core: "normal"}}`, `features.debugger: true` (backward-compatible - `degraded` still projects `true`), every other group `on` with limits matching the enforced constants exactly, and the new top-level `limits` block. Full engine test suite: 1101 tests, same 20 pre-existing `MountPolicyTest` failures (environment-specific, unrelated), zero new failures after the `RefreshPump()` fix. Full bridge suite: 172 passed, 2 skipped (pre-existing), zero failures.

#### 2.2 Debugger stop record and debug/wait
**Engine + bridge. Effort: M. Status: done.**

`debug/status` returns `{"debugging": bool}` and nothing else; continue and step return `{status, debugging}`. `CBreakpoint::CheckBreakpoint` has the matching breakpoint in hand at its `return true` (`debugger.cpp:644-673`) and `DEBUG_Breakpoint` discards it. So "run to breakpoint and tell me where I am" is four-plus tool calls and still cannot distinguish a breakpoint hit from a manual pause.

Set a file-static `DebugStopInfo` at the match sites just before `return true` (do **not** change `CheckBreakpoint`'s return type: three call sites, including `debugger.cpp:3981`, for the same information). Populate it also in `DEBUG_Enable`, the pause command, and `DEBUG_SingleStep`. There is exactly one place where `debugging = true` is assigned (`debugger.cpp:3026`), which is the choke point. Contents: `stop_id` (monotonic), reason, cs/eip/linear, register snapshot (reuse `Webserver::Registers::load`), CPU mode and the live core, breakpoint id and descriptor, and 16 raw bytes at CS:EIP.

Add `GET /api/v1/debug/wait?since_stop_id=&timeout_ms=` on the web thread against a condvar in a `Webserver::DebugEvents` singleton, never a Bridge Command. Clamp `timeout_ms` to 1..15000 and cap concurrent waiters explicitly.

**Correction that changes the shape:** `debug/continue` cannot return the resulting stop record. `DEBUG_Resume` is `DEBUG_Run(1, false)`: it executes one instruction, arms breakpoints, restores the normal loop and returns; the next stop happens arbitrarily later. Continue returns the **current** `stop_id` it is resuming from, and the client calls `debug/wait?since_stop_id=<that>`. Step is different: `DEBUG_Run(1, true)` really has executed by the time the response is written, so step can return the new record directly.

Publish side must be allocation-free: copy a POD under a short mutex and notify; build the JSON on the web thread.

**Why the agent is better off:** "continue, then tell me what happened" becomes two calls with no polling, and the answer already contains registers, the breakpoint that fired, and the code at the stop point.

**Note found while verifying:** `cpu.cpp:891` handles a hit INT3 breakpoint by setting `CPU_Cycles=0` and returning without routing to `debugCallback`, so that path never enters the debugger. The stop record will expose the inconsistency.

**Depends on:** 1.5 (client timeout handling for the long poll).

**Shipped, with two corrected citations and one real thread-safety bug found while implementing:**

- **The "three call sites, including `debugger.cpp:3981`" citation was wrong on inspection.** `CheckBreakpoint` has exactly three `return true;` sites, but they're internal to the function's body (674, 721, 730 in the current file) - `debugger.cpp:3981` is a *caller* of `CheckBreakpoint`, inside the unrelated `DEBUG_HeavyIsBreakpoint()`. Populated a file-static `pending_breakpoint_hit` at all three internal sites (plus the equivalent single site inside the separate `CBreakpoint::CheckIntBreakpoint`, which the plan's text didn't call out on its own but needs identical treatment for interrupt breakpoints to be reported at all). Every snapshot happens **before** any of the sites' own once-only deletion logic can free the matched `CBreakpoint*` - the physical-breakpoint site in particular deletes `bp` (or reassigns it to a different once-only breakpoint entirely) before its own `return true`, so reading fields after that point would have been a use-after-free.
- **`CBreakpoint` itself has no id, descriptor, condition, hit-count or ignore-count fields at all** (confirmed against the class definition, not assumed) - "breakpoint id and descriptor" is built from the same `DebugBreakpointInfo` DTO `DEBUG_ListBreakpoints()` already produces, including its own pre-existing, honest caveat that `index` is list position, not a stable identifier (2.4's job). Extracted the per-breakpoint `CBreakpoint*` -> `DebugBreakpointInfo` mapping into a shared `ToDebugBreakpointInfo()` so `DEBUG_ListBreakpoints()` and the new match-site capture can't drift apart.
- **`DEBUG_Enable` is confirmed as the literal single choke point**, not two separate things: `DebugPauseCommand::Execute()` (the webserver's pause command) just calls `DEBUG_Enable(true)` directly, and a breakpoint hit reaches the same function via `debugCallback -> DEBUG_EnableDebugger() -> DEBUG_Enable(true)`. `pending_breakpoint_hit`, set by the match sites moments earlier on the same thread, is what lets `DEBUG_Enable` tell a breakpoint-triggered stop from a manual pause (hotkey or API); it's consumed and cleared there so a later manual pause never reports a stale breakpoint.
- **Real bug found while wiring up `GET /api/v1/debug/wait`:** the file-static `debugging` flag (`debugger.cpp`) was a plain `bool`, safe only because every existing reader ran on the emulation thread. `debug/wait`'s handler needs `DEBUG_IsDebugging()` from the web thread - an unsynchronized cross-thread read, the same class of bug `rendered_frame_count` (1.6) and `active_core_kind` (2.1) already had to be fixed for. Changed it to `std::atomic<bool>`; every existing read/write site (11 of them) is source-compatible with no further changes.
- New `src/webserver/debug_events.{h,cpp}`: `Webserver::DebugEvents` mirrors `WaitRegistry`'s shape (mutex + condvar, `WaitFor`/`Publish`/`Current`/`DrainAll`), reusing `wait.h`'s existing `TooManyWaiters` and `MaxWaiters` for its own concurrent-waiter cap rather than minting a parallel type for a second, unrelated 4-waiter resource - `ClassifyException` already has the right branch wired up. `PublishDebugStop(reason, breakpoint)` builds the full record (register snapshot via `Webserver::Registers::load()`, `cpu.pmode`, `CPU_GetActiveCoreKind()` from 2.1, 16 bytes at CS:EIP via `mem_readb<MemOpMode::SkipBreakpoints>`) and publishes it - called from `DEBUG_Enable` and `DEBUG_SingleStep`, both already on the emulation thread. JSON-building stays entirely on the web thread (`debug.cpp`'s `DebugStopToJson`), matching "publish side must be allocation-free."
- Response shapes: `debug/status`, `debug/pause` and `debug/step` now carry a full `stop` object alongside their existing fields. `debug/continue` follows the plan's own correction exactly - `resumed_from_stop_id`, not a new record, since `DEBUG_Resume` can't know what happens next. **Fixed a real, separate bug found while touching `debug/step`:** its `debugging` field was hardcoded `true` in the response regardless of whether `DEBUG_SingleStep()` actually succeeded; now reads real state (`DEBUG_IsDebugging()`) like `pause` already did.
- `GET /api/v1/debug/wait` added unconditionally in `webserver.cpp` (matching every other debug route); the real long-poll lives in `debug.cpp`'s `#if C_DEBUGGER` block, a `501` `NotBuilt` stub in its `#else`, same convention as the other debugger-gated routes.
- `Webserver::DebugEvents::Instance().DrainAll()` added to `WEBSERVER_Destroy()` alongside `WaitRegistry`'s, so an in-flight `debug/wait` doesn't block shutdown.

Bridge: `debug_status`/`debug_pause`/`debug_continue`/`debug_step` already relay the engine's JSON straight through, so they picked up the new `stop`/`resumed_from_stop_id` fields for free. Added a new `debug_wait` tool (`dosbox_mcp/tools/debug.py`), same shape as `wait_for` from 1.8: query params passed straight through, per-call httplib timeout set to `timeout_ms/1000 + 5.0` so the engine's own deadline always fires first.

**Four real bugs found and fixed by an adversarial review pass (four independent reviewers, each cross-verified by a second), none of them use-after-free/memory-unsafe but all genuine data-correctness defects:**

- **`pending_breakpoint_hit` could leak stale data into an unrelated later stop.** The safety comment on `DEBUG_Enable`'s consumption of it claimed "a match is always followed by DEBUG_Enable... before any other breakpoint can fire" - false. `DEBUG_Resume`/`DEBUG_SingleStep` force exactly one instruction through `DEBUG_Run`, whose return value (how a match would normally reach `debugCallback` -> `DEBUG_Enable`) is discarded by both callers, exactly as the normal opcode-dispatch loop would consume it - but neither `DEBUG_Resume` nor `DEBUG_SingleStep` themselves route it anywhere. If that forced instruction matched an already-armed breakpoint (reachable in ordinary use: arm a breakpoint via `continue`, pause manually before it's hit - `DebugPauseCommand`/the Pause hotkey both call `DEBUG_Enable` directly and never deactivate breakpoints - then step through it), `pending_breakpoint_hit` was set but never cleared, and would surface as a false `reason: "breakpoint"` with stale data on the *next*, completely unrelated manual pause. Fixed by clearing `pending_breakpoint_hit` at the end of both `DEBUG_Resume` and `DEBUG_SingleStep`, right after their own `DEBUG_Run` call - a match from either path is deliberately discarded, not attributed to that call, since 2.2's scope is reporting *the normal-dispatch stop path* honestly, not redesigning how a mid-step breakpoint hit is handled. Also closes a narrower sibling case in pre-existing, undiffed `cpu.cpp` code (`CPU_Interrupt`'s `case 0x03`, a second independent `DEBUG_Breakpoint()` call site reachable via a specific ROM-byte coincidence) via the same fix, since it sets the same file-static.
- **`debug/continue`'s `debugging` field was `!cmd.resumed`, wrong when nothing was ever paused.** `DEBUG_Resume()` returns `false` both when it successfully can't resume *and* when the emulator simply wasn't paused to begin with - either way `debugging` stays `false`, but negating `resumed` reported `true`. Gave `DebugContinueCommand` a real `debugging` member read back via `DEBUG_IsDebugging()` (mirroring the identical fix already applied to `debug/step` earlier in this same item), instead of inferring it.
- **`debug/wait`'s `reason` could leak an undocumented empty string.** `DebugStopInfo`'s default-constructed `reason` was `""`, reachable any time nothing has been published yet (a freshly started server whose debugger has never paused). `debug_wait`'s own docstring promised only `'paused'`/`'breakpoint'`/`'step'`. Gave `DebugStopInfo::reason` an explicit `"never_stopped"` default and documented it as a fourth value.
- **`debug/wait`'s `debugging` field could disagree with the `stop` record it's paired with.** It was read via a separate, unsynchronized `DEBUG_IsDebugging()` call *after* `WaitFor()` already returned its snapshot - a concurrent `debug/continue` (or a fresh breakpoint hit) landing in that window could make the response report e.g. `reason: "breakpoint"` alongside `debugging: false`. `debug_status`/`pause`/`step` don't have this gap because their `Execute()` reads both fields back-to-back inside one Bridge command, uninterruptible by anything else. `debug/wait` has no such boundary (deliberately - it's not a Command, see below). Fixed by giving `DebugStopInfo` its own `debugging` field, captured under `DebugEvents`' mutex at publish time (via a new `bool debugging` parameter on `PublishDebugStop`, supplied by the caller rather than assumed, since `debug_events.cpp` has no access to the `debugging` atomic itself); `debug/wait` now reports `result.info.debugging` on a genuine wakeup (coherent with the rest of that snapshot) and falls back to a live `DEBUG_IsDebugging()` read only on a real timeout, where there's no specific stop to be coherent with.

**One further finding, documented rather than fixed - deliberately out of scope:** `DEBUG_Loop`'s pre-existing IRQ-while-paused handling plants its own throwaway one-shot breakpoint purely as internal bookkeeping (`CBreakpoint::AddBreakpoint(oldCS, oldEIP, true)`). When that breakpoint later fires through the normal dispatch chain, it now genuinely reports `reason: "breakpoint"` with a `DebugStopBreakpoint` indistinguishable in shape from one a caller created - internal debugger machinery leaking through the API surface as if it were a user breakpoint. Not data corruption (the reason is technically accurate) and not reachable in the common case; distinguishing "internal" from "caller" breakpoints would need real provenance tracking, which belongs with 2.4's stable-id work, not this item.

**Verification:** 8 new engine-side tests (`webserver_debug_events_tests.cpp`) cover `DebugEvents`' own mechanics (monotonic stop ids, immediate-vs-blocking `WaitFor`, the waiter cap, `DrainAll`, the `never_stopped`/`debugging` defaults) without depending on live CPU/memory state, which this unit-test binary never initializes - matching `webserver_wait_tests.cpp`'s own documented scoping. `PublishDebugStop` itself (which reads real registers/memory) is exercised only live. Full engine suite: 1105 tests (excluding disabled), same 20 pre-existing `MountPolicyTest` failures, zero new failures. 4 new bridge-side tests (`test_debug_tools.py`, the first test coverage `debug.py` has had at all); full bridge suite 176 passed, 2 skipped (pre-existing), zero failures. `debug_wait` also verified through the real MCP dispatch path (`server.request_handlers[types.CallToolRequest]`) against the live binary, `isError: false` throughout. The four fixes above were re-verified live after applying them: `debug/wait` before any pause correctly returns `reason: "never_stopped"`; `debug/continue` while not paused correctly returns `{"status": "not_paused", "debugging": false}` instead of the previous `debugging: true`; pause/continue sequences report coherent `debugging` values throughout.

Live-verified end-to-end against the real `build-debugger` binary: pause/status/continue/step all report correct `stop` records; 30 consecutive single-steps tracked real EIP/register changes exactly; a background `debug/wait` call correctly blocked and then resolved the instant a concurrent request published a new stop, proving the push path (not just polling) works. One live surprise, run down before concluding anything: an execute breakpoint planted on the address the emulator was paused at (freshly-booted BIOS POST code, segment `F000`) never fired even across multiple retries. Traced to the root cause rather than assumed: `CBreakpoint::Activate()` plants a breakpoint by writing `0xCC` directly into guest memory, and a direct `PUT /api/v1/memory` write to that same BIOS ROM address silently no-ops (read-back still shows the original byte) - the ROM segment is write-protected in this emulator's memory model, exactly like real BIOS ROM. Confirmed this is pre-existing and unrelated to this change by reverting `debugger.cpp` alone (`git stash push` on that one file) and reproducing the identical non-firing behavior against the original, unmodified code. Real guest DOS programs load into ordinary conventional RAM, not ROM, so this doesn't affect the feature's actual use case; a true positive-path confirmation (breakpoint fired, `stop.reason == "breakpoint"`, correct `stop.breakpoint` payload) needs a booted DOS environment with a mounted image and was out of scope for this pass.

#### 2.3 Watchpoint honesty
**Engine. Effort: S, plus M if watchpoints are actually wanted. Status: done, scoped to the S half exactly as instructed.**

`POST /debug/breakpoints` with `type: "memory"` returns 200 with the engine's own read-back, and never fires: every memory branch of `CheckBreakpoint` is inside `#if C_HEAVY_DEBUGGER` (`debugger.cpp:674-733`), which the shipped debugger build has off. The engine's own BPM/BPMR/BPPM/BPLM commands are hidden on that build (`debugger.cpp:1998-2044`), so REST exposes a facility the UI does not. `tools/debug.py:59-61` asserts the opposite of the truth.

**Ship first, alone:** a `watchpoints` boolean in the features/capabilities block sourced from `C_HEAVY_DEBUGGER`, a 501 in the existing NotBuilt shape when `type=="memory"` and it is false, and a rewritten tool description. A refusal an agent can read beats a breakpoint that never fires.

**Two real bugs to fix if you extend the type set:**
1. `AddMemBreakpoint` never seeds the current byte (ctor sets `ahValue(0)`), so a watchpoint on any address whose byte is non-zero fires immediately. `BPMR` does call `FlagMemoryAsUnread`; the write variant does not.
2. The memory branch uses `return false` where it means `continue` (`debugger.cpp:684,690,693,707`), so one failed read aborts the scan of the entire breakpoint list and silently skips later breakpoints.

**Be honest about the cost:** `C_HEAVY_DEBUGGER` puts `DEBUG_HeavyIsBreakpoint` on the per-instruction path in every core. Shipping a heavy build is a performance decision. If the answer is no heavy build, do only the flag and the 501 and skip the rest.

**Shipped, exactly the S-scoped half, with one deliberate deviation from the plan's own text:**

- **No separate `watchpoints` boolean added.** The plan's text predates item 2.1, shipped earlier this session: `BuildCapabilitiesBlock()`'s `capabilities.debugger` already reports `state: "degraded"` with a reason string naming memory breakpoints unavailable specifically, whenever `!C_HEAVY_DEBUGGER` (`capabilities.cpp:90-104`, `ComputeDebuggerCapability`). A second, standalone `watchpoints` flag would either duplicate that disclosure or risk drifting from it as a separate source of truth - the existing capability is more precise (it also names which core execute/interrupt breakpoints fall back to) and already does everything the plan's `watchpoints` boolean was for. Verified live: `GET /api/v1/dosbox/info`'s `capabilities.debugger` still reads exactly as it did after 2.1, unchanged by this item.
- **The 501 refusal is real and new:** `DebugAddBreakpointCommand::Post`'s `type == "memory"` branch (`debug.cpp`) now checks `#if !C_HEAVY_DEBUGGER` and returns `501` with a clear error naming the reason and pointing at `capabilities.debugger`, before ever constructing a `Command` or crossing the Bridge - matching the plan's "refusal an agent can read beats a breakpoint that never fires." `execute`/`interrupt` breakpoints are unaffected; confirmed live.
- **Found and fixed in passing, not originally in scope:** the bridge's own `debug_breakpoint_add` tool description for `type: "memory"` was not just silent about non-support, it was **actively wrong about the semantics** - it read "stop on execution reaching segment:offset ... same effect as execute breakpoints for stepping purposes," describing an execute breakpoint, not a watchpoint. The engine's actual mechanism (`CheckBreakpoint`'s `BKPNT_MEMORY` branch, `debugger.cpp:818-928`) is a watched-byte-value-changed check, unrelated to execution reaching the address at all. This predates this item and wasn't touched by 2.4/2.6's own edits to the same tool description (those extended the schema with unrelated fields and never audited this bullet). Rewritten to describe the real semantics, the heavy-debugger requirement, and the 501.
- **The "two real bugs" and "be honest about the cost" sections are deliberately untouched**, per the plan's own explicit instruction: this build ships without `C_HEAVY_DEBUGGER`, so "do only the flag and the 501 and skip the rest" applies exactly.

**Verification:** engine build clean, zero new warnings. Full suite unaffected (same 20 pre-existing `MountPolicyTest` failures). Live-verified against the real `build-debugger` binary: `POST /api/v1/debug/breakpoints` with `type: "memory"` now returns `501` with the documented error; `type: "execute"` at the same address still returns `200` and works normally; `capabilities.debugger` cross-checked as unchanged and already consistent with the new refusal's own reasoning. Bridge: full suite 185 passed, 2 skipped, zero failures; ruff clean (no new findings beyond the pre-existing, unrelated `PLR0402` pattern already present throughout this file).

#### 2.4 Execution control and stable breakpoint identity
**Engine. Effort: M. Status: done.**

`DEBUG_SingleStep` is exactly one instruction, so stepping over a DOS call is hundreds of requests. `StepOver()` already exists and works (`debugger.cpp:929-946`) but is static and absent from the header, and the resume half lives in the F10 key handler, so exporting it means exporting both. `AddBreakpoint` takes a `once` flag that `DEBUG_AddExecuteBreakpoint` hardcodes to false. Breakpoint identity is a list position, and all three add functions `push_front`, so every add renumbers everything.

Export `DEBUG_StepOver()` (doing both halves, returning false when the instruction is not call/int/loop/rep so the caller falls back to a plain step), `DEBUG_RunToAddress(seg, off)` as one-shot breakpoint plus resume, and a `once` field on the breakpoint add body. Give `CBreakpoint` a monotonic `uint32 id` assigned at construction, report it alongside `index`, and accept either on DELETE with an unambiguous body shape (`{"id":...}` vs `{"index":...}`).

**Step-N is a footgun as usually specified.** `DEBUG_Run(n, quickexit=true)` never calls `ActivateBreakpoints`, so a multi-instruction step runs with every breakpoint disarmed. Either drive the decoder one instruction at a time checking `CheckBreakpoint` between iterations, or cap at 64 and state plainly in the description that breakpoints do not fire during a multi-step. Also note that N separate HTTP steps each get a `PIC_runIRQs` between them (`DEBUG_Loop`) while a loop inside one `Execute` does not, so a trace advances the CPU without advancing the interrupt timeline. The whole run holds the Bridge mutex, so size the cap against an explicit raised `WaitForCompletion`.

**Depends on:** 2.2 for the stop reason returned after each step.

**Shipped**, with one corrected citation and one significant bug found while implementing that also reaches back into already-shipped 2.2 code:

- **The `debugger.cpp:929-946` citation for `StepOver()` was off by current line numbers** (source has moved since the plan was written) - it's `debugger.cpp:1070-1088` now, same shape as described: disassembles the current instruction, and for call/int/loop/rep plants a one-shot breakpoint just past it (`reg_eip + size`, deduplicated against an existing one at that address) and sets `debugging = false`, returning `true`; otherwise returns `false` unchanged. The F10 key handler this mirrors is `debugger.cpp:3043-3053` (`if (StepOver()) { ret = DEBUG_Run(1, false); break; }`, falling through to F11's plain `DEBUG_Run(1, true)` otherwise) - exported as `DEBUG_StepOver()` doing exactly that, and `DEBUG_RunToAddress(seg, off)` doing the equivalent with an explicit target instead of disassembly-based detection (plant a one-shot breakpoint at the given address if none exists there yet, `debugging = false`, resume).
- `CBreakpoint` gained a `uint64_t id`, assigned from a file-static monotonic counter in the constructor's init list (`id(++next_breakpoint_id)`, ids start at 1, never reused or renumbered even as `index` shifts under adds/removes elsewhere in the list). No mutex: every `CBreakpoint` construction and deletion already happens on the emulation thread, either via a Bridge `Command::Execute()` or the interactive debugger's own key-handler chain - confirmed by grep that `CBreakpoint` has zero references outside `debugger.cpp` and zero Lua-scripting access. `DEBUG_ListBreakpoints()` and the new match-site capture (2.2) share the one `ToDebugBreakpointInfo()` mapping, so `id` reaches both without a second place to keep in sync. `DELETE /api/v1/debug/breakpoints` now requires exactly one of `{"id":...}`/`{"index":...}` in the body (or an empty body to clear all), rejecting both-present or neither-present with a 400 rather than silently picking one.
- `once` threaded through `DEBUG_AddExecuteBreakpoint`/`DEBUG_AddIntBreakpoint`/`DEBUG_AddMemBreakpoint` (previously hardcoded `false` in all three) and the `POST /api/v1/debug/breakpoints` body (`{"once": true}`, defaulting `false`).
- Step-N shipped as the plan's second option: `DEBUG_SingleStep(count = 1)`, capped at `DEBUG_MaxStepCount = 64`, documented plainly (in the header doc comment and the tool description) that only breakpoints already active from a prior continue/run are honoured mid-burst, that memory breakpoints need `C_HEAVY_DEBUGGER` regardless, and that IRQs are only serviced between HTTP calls, not mid-burst - a trace advances the CPU without advancing the interrupt timeline, exactly as flagged. `POST /api/v1/debug/step`'s `WaitForCompletion` is raised to 2000 ms only when `count > 1`, matching `SearchMemoryCommand`'s own precedent for a bounded worst case under the held Bridge mutex; a plain single step keeps the Bridge's normal default.
- **Real bug found while implementing, confirmed by two independent adversarial reviewers with a deterministic repro, that also affects already-shipped 2.2 code:** `DEBUG_Resume`, `DEBUG_StepOver` and `DEBUG_RunToAddress` all force exactly one instruction via `DEBUG_Run(1, false)` and discarded its return value. That return value, when non-zero, is a callback id - it's how a breakpoint match reaches `debugCallback -> DEBUG_EnableDebugger() -> DEBUG_Enable(true)`, and both the normal opcode-dispatch loop (`dosbox.cpp`) and the interactive debugger's own F5/F10/F11 handler (`debugger.cpp:3125-3139`, immediately following the switch that calls `DEBUG_Run`) route it through `Callback_Handlers[ret]` before doing anything else. Discarding it meant a match on a *different*, already-armed breakpoint hit during that one forced instruction was silently dropped - not misattributed, never reported at all - leaving the emulator to resume at full speed past a stop it should have made, while the caller was told the call succeeded. Reachable in ordinary use, not theoretical: `CBreakpoint::Activate()`'s `0xCC` patch is written into guest memory only when a Command's own `DEBUG_Run(..., false)` call reaches its `CBreakpoint::ActivateBreakpoints()` step (`debugger.cpp:2693-2710`) - which happens *after* that call's own forced instruction executes - so the very next call whose forced instruction happens to land on an address patched by a *prior* call's activation will decode that patch instead of the real opcode. A manual pause (`DebugPauseCommand`'s `DEBUG_Enable`) never deactivates breakpoints the way a genuine hit does, so a breakpoint armed by an earlier continue stays armed and patched across it. Fixed by a shared `DispatchDebugRunCallback(ret)` helper (`debugger.cpp`, next to `DEBUG_Resume`) replicating the exact `Callback_Handlers`/`CB_MAX` dispatch `DEBUG_CheckKeys` already does, applied to all three functions - including `DEBUG_Resume`, which predates this item and shipped as part of 2.2.
- **Second bug found by the same review:** `DebugStepOverCommand::Post` unconditionally serialized `cmd.stop` even on the plant-and-resume path, where - absent the fix above - it was guaranteed to be a stale, unrelated prior stop record, contradicting the class's own documented contract that the immediate response only says whether the resume was armed. Fixed to include `stop` only when `cmd.stop.stop_id != cmd.resumed_from_stop_id`, i.e. exactly when something was genuinely published during this call (the plain-step fallback always publishes one; the plant-and-resume path now also can, via the fix above, when the forced instruction itself hits a different already-armed breakpoint synchronously).
- **Third bug found by the same review, a JSON-validation gap:** `count` (step) and `offset` (run-to, breakpoint add) were parsed with a bare `.get<int64_t>()`/`.get<uint32_t>()`, unlike `RequireU64` written in the same diff specifically to close this class of gap - `nlohmann::json::get<T>()` on an integral `T` silently truncates a non-integral JSON number (`{"count": 2.9}` -> `2`) rather than throwing, and an extreme out-of-range float literal reaching it is UB per `[conv.fpint]`. Consolidated all integer parsing in `debug.cpp` through one `RequireInt` (`is_number_integer()` guard, matching `wait.cpp`'s own `RequireNonNegative` reasoning: nlohmann only tags a parsed number as its unsigned representation when the source text had no leading `-`, so validate the value, not which representation happens to be present) with `RequireU16`/`RequireU32`/`RequireU64` layered on top for their respective ranges; `ByteOrWildcard` and the interrupt-breakpoint `int` field picked up the same guard in passing, since they had the identical bare-`.get<int>()` gap.

Engine: build clean, zero new warnings. Full suite 1109 tests, same 20 pre-existing `MountPolicyTest` failures, zero new failures. Live-verified against the real `build-debugger` binary: stable `id` survives other breakpoints being added/removed while `index` shifts underneath it; delete-by-id removes the correct entry; both-`id`-and-`index` and neither rejected with 400; `count=5` advanced EIP by exactly 5 one-byte NOPs, `count=0`/`count=100` rejected; `step_over` correctly fell back to a plain step off a non-call/int/loop/rep instruction; malformed `count`/`offset`/`segment`/`int` (float, extreme float) now rejected with 400 instead of silently truncating. The `DispatchDebugRunCallback` fix itself could not be triggered through its full end-to-end path live: the only reachable test address range at this point in boot is BIOS ROM (segment `F000`), where `CBreakpoint::Activate()`'s `0xCC` write silently no-ops (same ROM write-protection behaviour documented in 2.2, reconfirmed here by reading the target address back over `GET /api/v1/memory` after arming - `90 90`, not `CC`), and the automation API exposes no way to relocate CS:IP into RAM to route around it. Verified instead by tracing the full call chain by hand against the live source: `DEBUG_Run(1, false)` calls `(*cpudecoder)()` synchronously inside `Execute()` before returning (`debugger.cpp:2693-2710` - the "one forced instruction" is not deferred to the main loop at all), and `Callback_Handlers[debugCallback]` is registered as `DEBUG_EnableDebugger` (`debugger.cpp:3740`), which calls `DEBUG_Enable(true)` directly (`debugger.cpp:3715-3721`) - the exact function 2.2 established as the one place a stop gets published. Combined with the two independent adversarial reviewers who confirmed the same defect by reading the same call chain, this is treated as sufficient - the same "code-reasoning plus a targeted live check, not a full positive-path trigger" standard 2.2 used for its own BIOS-ROM-blocked scenario.

Bridge: `debug_step` gained an optional `count` (1-64), `debug_breakpoint_add` gained `once`, `debug_breakpoint_delete` now takes `id` or `index` (passing through whichever the caller sent, rather than resolving ambiguity bridge-side, so the engine's own mutual-exclusivity check is what actually fires on a confused caller). New `debug_step_over` and `debug_run_to` tools, both documented with the plant-and-resume caveat and pointed at `debug_wait`. 8 new bridge-side tests; full suite 184 passed, 2 skipped, zero failures.

#### 2.5 Disassembly route
**Engine + bridge. Effort: M. Status: done.**

`DasmI386` (`debugger_disasm.cpp:1084`) is declared only in the private `debugger_inc.h` and reaches no route. An agent paused at a breakpoint reads base64 and decodes x86 in its head. Worse, on a non-heavy build active execute breakpoints are `0xCC` bytes patched into guest memory, so bytes read through `/api/v1/memory` while running contain traps rather than the program.

The file is genuinely self-contained: it includes only `dosbox.h`, `hardware/memory.h` and four cstd headers, and its sole engine dependency is `mem_readb<MemOpMode::SkipBreakpoints>`, which is instantiated unconditionally. So it can compile outside `C_DEBUGGER`.

**Build mechanics, corrected:** dropping the `#if C_DEBUGGER` is not sufficient. `src/CMakeLists.txt:6-8` only adds the debugger subdirectory when `OPT_DEBUGGER`, and `src/debugger/CMakeLists.txt` unconditionally links imgui. Move the file to its own directory added unconditionally, or split the CMakeLists. Put the declaration in a new public header; `debugger_inc.h` drags in SDL3.

**Validation, mandatory:** `DasmI386(char* buffer, ...)` takes no buffer length and writes via `uprintf` with no cap. Feeding it attacker-chosen bytes at an HTTP-supplied address is exactly the untrusted-input case. Add an explicit output-size parameter or a bounded internal buffer with truncation. `getbyte()` walks forward with no limit, so range-check start and count up front with 64-bit math.

Must run as a Bridge Command: decode state is file-static (`ubufs`, `ubufp`, `getbyte_mac`, `opmap1`) and `mem_readb` goes through the TLB. Clamp count 1..256 via `num_param` and raise that route's timeout above 250 ms. Read the raw-bytes column with `SkipBreakpoints`.

**`AnalyzeInstruction` is not portable.** It depends on `CDebugVar` (private to `debugger.cpp`), a static result buffer, `curSelectorName` and `CALLBACK_GetDescription`. Keep the annotation behind `#if C_DEBUGGER` or reimplement a minimal EA resolver. Do not promise annotation on stock builds.

**Return structured operand and target addresses**, not only a rendered string, so 2.17 can substitute symbols into them.

**Shipped**, with one significant correction to the plan's own scoping and two real bugs found and fixed (one via live testing before the review, one by the adversarial review pass) - plus two more the review found but confirmed were not reachable and needed no fix:

- **Both citations confirmed exact** on inspection: `debugger_disasm.cpp:1084` and `src/CMakeLists.txt:6-8` were both still accurate at implementation time.
- **A third build-mechanics change the plan's text didn't call out, found while implementing:** dropping the `if(OPT_DEBUGGER)` guard around `add_subdirectory(debugger)` is necessary but not sufficient on its own - `debugger_disasm.cpp` had its *own*, separate `#if C_DEBUGGER` / `#endif` wrapped around its **entire body** (not just individual declarations), so even once the file was reachable by the build graph it would still have compiled to an empty translation unit under `C_DEBUGGER=0`. Removed that internal guard too (confirmed via a full-repo grep that no other `C_DEBUGGER`/`C_HEAVY_DEBUGGER` reference remained anywhere in the file, and that neither of its two remaining includes, `dosbox.h`/`hardware/memory.h`, has any `C_DEBUGGER`-conditional declaration the file was silently relying on).
- **CMake split** exactly as the plan suggested: `src/debugger/CMakeLists.txt` now adds `debugger_disasm.cpp` to `dosboxcommon` unconditionally, keeping `debugger.cpp`/`debugger_gui.cpp` and the `imgui` link inside `if(OPT_DEBUGGER)` (mirroring an existing, repeated pattern already used by `src/dos`, `src/gui`, `src/libs` for their own `OPT_DEBUGGER`-conditional pieces). Dropped an unused `DEBUGGER_SRC` CMake variable in the same file while touching it (dead code - `target_sources` never referenced it, using a duplicated literal list instead). New `src/debugger/debugger_disasm.h` carries just the four exported declarations (`DasmI386`, `DasmLastOperandSize`, and the two new getters below), so a caller doesn't need `debugger_inc.h` (which drags in `<SDL3/SDL.h>` and the interactive debugger's own GUI state) just to disassemble; `debugger_inc.h` now includes this new header instead of declaring `DasmI386`/`DasmLastOperandSize` inline itself, so `debugger.cpp`'s existing include kept working unchanged.
- **`DasmI386` gained an explicit `buffer_size` parameter**, used to compute a new `ubufend` bound; `uprintf` (switched from `vsprintf` to `vsnprintf`) and `uputchar` both now check against it and truncate rather than overflow, always leaving the buffer null-terminated. All 5 existing interactive-debugger call sites (`debugger.cpp`) updated to pass `sizeof()` of their own local buffer. `getbyte()` also gained a defense-in-depth cap (`MaxInstructionBytes = 32`, roughly 2x real x86's 15-byte architectural max) after which it stops reading rather than walk forward indefinitely - this function is now reachable from untrusted HTTP input for the first time, not just the interactive debugger's own trusted use.
- **Structured target-address extraction, scoped to relative branches:** two new file-statics (`has_relative_target`, `last_relative_target`, reset each `DasmI386` call) are set inside `percent()`'s `'J'` case (Jcc, JMP rel, CALL rel, LOOP*, JCXZ - the same computation the existing code already did to render the address as text, just also retained as a value), exposed via `DasmHasRelativeTarget()`/`DasmLastRelativeTarget()`. Indirect/far branches (ModRM/SIB-addressed targets) are **not** extracted as structured data - the rendered text still shows them correctly, there's just no separate field for 2.17 to substitute a symbol into. This was a deliberate scope decision: research found that unlike the `'J'` case's single clean choke point, extracting indirect targets would mean touching several more, deeply-nested decode paths (`do_modrm`/`outhex`) with no equivalent already-computed value to retain - real but meaningfully larger scope than what the plan's one-line "return structured...target addresses" asked for.
- **`AnalyzeInstruction` deliberately not ported.** Confirmed it's a fully separate, only loosely-related piece of `debugger.cpp` (never called from `debugger_disasm.cpp` itself) - a live-CPU-state annotation pass (memory-operand value peek, callback-number-to-name lookup, branch-taken prediction) layered on top of the disassembler's static output, depending on `CDebugVar`, `curSelectorName`, and other `debugger.cpp`-private state. Out of scope here, matching the plan's own "do not promise annotation on stock builds."
- **New `GET /api/v1/debug/disassemble/:segment/:offset/:count`**, a `DisassembleCommand` Bridge Command registered **unconditionally** (no `#if C_DEBUGGER` split at all, unlike every other `debug/*` route) - the one route in this whole area that doesn't need the debugger capability. Real-mode-style numeric `segment`/`offset` (matching every other `debug/*` route this session shipped: execute/interrupt/memory breakpoints, `debug/run_to` - none of them do protected-mode GDT resolution either), `count` clamped 1..256 via `num_param`, `WaitForCompletion(2000)` matching `SearchMemoryCommand`/`DebugStepCommand`'s own raised-timeout precedent for a bounded-but-nontrivial worst case. `bit32` comes from the live `cpu.code.big`, not a request parameter - real-mode DOS code (the overwhelming common case) has no per-segment operand-size bit to get wrong regardless of which segment is being disassembled, since real mode has no D-bit concept at all. New `disassemble` capability group (`capabilities.cpp`, `AlwaysOn`), deliberately separate from and independent of the existing `debugger` capability (which stays gated by `C_DEBUGGER`/`C_HEAVY_DEBUGGER` as before, unrelated to this item).
- **Real bug found via live testing, before the formal review:** the physical address computation (`(segment << 4) + offset`) was originally `uint32_t` arithmetic. `segment=0xFFFF, offset=0xFFFFFFFF` silently wrapped to a small, in-range-looking address instead of being rejected as out of range - live-reproduced (response showed a decoded instruction at a wrapped-around offset instead of an error). Fixed by doing the computation, the bounds check, and the whole per-instruction loop's address tracking in `uint64_t` throughout, matching the "untrusted input, 64-bit math" caution the plan's own text already called out for `getbyte()`'s internal walk - this was the same caution applying one level up, to the request's own starting address.
- **Real bug found by an adversarial review pass (two independent reviewer/verifier pairs, one live-reproducing it), fixed before shipping:** `DasmI386`'s own byte fetcher (`getbyte()`) has no awareness of emulated memory size - only the unrelated 32-byte instruction-length cap above - so an instruction starting just inside bounds but longer than the remaining room decoded some of itself from `IllegalPageHandler`'s fabricated `0xFF` fill bytes rather than real guest memory, while the route's separate raw-bytes-reading loop correctly zero-filled those same out-of-range positions - so `text`/`length` and `bytes` silently described two different instructions, and `truncated` stayed `false` since that flag only fired on the *next* instruction's start being out of range, never on the current one's overrun. Fixed by checking `physical + length > mem_total` immediately after each `DasmI386` call and, if so, discarding that instruction and setting `truncated = true` instead of including it - matching this exact route's own documented contract ("stops early... rather than walk past the end of emulated memory") and the same up-front-rejection principle `ReadMemoryCommand` already established elsewhere in this codebase. This also made the raw-bytes loop's own per-byte bound check provably redundant (every byte of an instruction that survives the new check is now guaranteed `< mem_total`), so it was simplified away rather than left as confusing dead logic.
- **A second bug the same review pass found, fixed in the same pass:** the output-buffer bounds fix above broke an unrelated, pre-existing padding loop in `ua_str()` (mnemonic-column padding, unrelated to and untouched by anything else in this item) that implicitly assumed `uputchar` always advances the output pointer - true before the bounds fix, no longer true once `uputchar` correctly stops advancing at a full buffer. For any `buffer_size <= 5`, the loop could no longer terminate. **Not reachable today** - all 6 real call sites (5 interactive-debugger, 1 this route) pass fixed 200- or 256-byte buffers - but fixed anyway (`while (ubufp < ubufend && ...)`, mirroring the guard already used everywhere else in the file) since the whole point of this item's bounds work was to make the function's safety hold for *any* input, not just the specific buffers currently in use, and the file is now reachable from strictly more, less-trusted callers than before.
- **Two further things the same review checked and confirmed did not need fixing:** `OPT_HEAVY_DEBUGGER=ON` (never built during this item's own testing, which only exercised `OPT_DEBUGGER=ON`/heavy-off and `OPT_DEBUGGER=OFF`) was independently configured and built end-to-end from scratch by two separate reviewers - `debugger.cpp`, `debugger_gui.cpp`, `debugger_disasm.cpp` and `imgui` all present and linking correctly, confirming the `OPT_HEAVY_DEBUGGER` → `OPT_DEBUGGER` force-on `CACHE INTERNAL` logic in the top-level `CMakeLists.txt` still resolves correctly across the `add_subdirectory` boundary. And `uputchar`'s own unconditional trailing null-terminator write has a latent (not reachable today - no call site passes `buffer_size == 0`) gap for that specific edge case, noted but left alone since fixing an input no real caller produces would be speculative hardening beyond this item's actual scope.

**Verification:** built and linked successfully in **two** configurations from a clean reconfigure - the existing `OPT_DEBUGGER=ON` build (this repo's `build-debugger/`, `C_HEAVY_DEBUGGER` off) and a fresh scratch `OPT_DEBUGGER=OFF` build (confirmed via its own `CMakeCache.txt`/`build.ninja` that `debugger.cpp`/`debugger_gui.cpp` are genuinely absent from that build, only `debugger_disasm.cpp` present). Full engine suite on the `OPT_DEBUGGER=ON` build: 1109 tests, same 20 pre-existing `MountPolicyTest` failures, zero new failures (no test suite exists for the `OPT_DEBUGGER=OFF` configuration - `dosbox_tests` itself needs `debugger.cpp` for some of its own test files). Live-verified against both real running binaries: basic multi-instruction decode; the relative-target field on an actual JMP instruction in live BIOS POST code, hand-verified against the rendered `$-13` displacement text; `count=0`/`count=257` correctly 400, `count=256` succeeds; the `truncated` flag correctly sets near the top of configured memory; the boundary-straddling-instruction bug's exact repro (write a multi-byte opcode at the last valid byte via `PUT /api/v1/memory`, then disassemble it) now correctly returns an empty result with `truncated: true` instead of the previous `text`/`bytes` mismatch; and - the defining structural claim of this whole item - `GET /api/v1/debug/disassemble` returns `200` with correctly decoded instructions on the `OPT_DEBUGGER=OFF` binary, where `GET /api/v1/debug/status` (every other `debug/*` route) correctly `501`s with "debugger capability not built in this binary" and there is no pause/breakpoint machinery reachable at all.

Bridge: new `debug_disassemble` tool, registered with `feature="disassemble"` - deliberately **not** the module's default `feature="debugger"` every other tool in this file uses, since gating this one the same way would incorrectly block it on a non-debugger build even though the engine itself now supports it unconditionally; confirmed this resolves correctly against `guard()`'s existing capability-lookup logic (falls back gracefully to a clean refusal against an even older engine that predates this item's own `disassemble` capability entirely, rather than attempting a request against a route that wouldn't exist). One new bridge-side test (path construction from segment/offset/count); full bridge suite 186 passed, 2 skipped, zero failures. Also verified through the real MCP dispatch path (`server.request_handlers[types.CallToolRequest]`) against the live `OPT_DEBUGGER=ON` binary, `isError: false`.

#### 2.6 Conditional breakpoints, hit counts, ignore counts
**Engine. Effort: M. Status: done.**

The only condition today is AH/AL matching on interrupt breakpoints. An execute breakpoint inside a hot helper stops on the first hit with no way to say "only when AX==0x4C00" or "skip the first 200".

**Do not evaluate the condition inside `CheckBreakpoint`.** On the shipped build, execute breakpoints are `0xCC` patched into guest memory. When `DEBUG_Breakpoint` returns false at a patched byte, `core_normal/prefix_none.h:736-745` falls through to `CPU_SW_Interrupt_NoIOPLCheck(3, GETIP)`, delivering a **real INT3 to the guest** with EIP past the byte that replaced a real instruction. Today `CheckBreakpoint` effectively never returns false at a patched address; conditions and ignore counts make false the common case, so every skipped hit would corrupt the program.

Correct design: let `CheckBreakpoint` keep returning true so the trap is consumed and `oldData` is restored, then evaluate the condition at the stop point (top of `DEBUG_Loop`, or in the 2.2 publish path) and auto-resume when false. That costs a full stop/resume cycle per non-matching hit. Say so in the tool description, so an agent knows a condition on a hot function is slow rather than free.

`hit_count` can still be incremented inside `CheckBreakpoint` (it does not change the return value). Condition shape is fixed, never an expression parser: `{reg, op, value}` or `{segment, offset, width, op, value}`, validated entirely on the web thread. Memory operands use `mem_readb_checked` and friends (declared in `cpu/paging.h:396`, not `hardware/memory.h`). Reusing `Webserver::RegisterKind` means extending it with 8/16-bit sub-register names, which shares a table with `PUT /cpu/register`: check that the extension does not accidentally widen what that route accepts.

**Depends on:** 2.2 (needs the stop-point hook), 2.4 (stable ids).

**Shipped**, with one deliberate deviation from the plan's own suggestion and one real bug found and fixed by an adversarial review pass:

- **`mem_readb_checked`'s `cpu/paging.h:396` citation confirmed exact** on inspection - no drift. `CheckBreakpoint`'s "keep returning true, evaluate later" design is exactly what shipped: a new `ShouldSkipBreakpointHit()` runs at the very top of `DEBUG_Enable`'s pressed-body, before any of its UI/state side effects, consulting `pending_breakpoint_hit` (2.2) to find which breakpoint just matched (via a new `CBreakpoint::FindById`, mirroring 2.4's `DeleteById`) and asking it to evaluate its own condition/ignore_count. `hit_count` is incremented at all four match sites inside `CheckBreakpoint`/`CheckIntBreakpoint` (the same sites that already snapshot `pending_breakpoint_hit`), unconditionally - it counts every genuine match, whether or not that hit later gets silently skipped.
- **Deliberately did NOT reuse/extend `Webserver::RegisterKind`**, the table backing `PUT /api/v1/cpu/register`, despite the plan's own suggestion to reuse it. Confirmed before implementing (not assumed) that extending it with 8/16-bit sub-registers for this read-only purpose would also silently widen what that write route accepts: `WriteRegisterCommand::Execute` has no width-aware write logic (`*regs[ref.index] = value;` unconditionally overwrites the full 32-bit register), so a client writing `al` would corrupt the rest of `eax` rather than doing the width-scoped write its name implies. Instead, `ConditionRegister` (30 values: 8/16/32-bit GPRs plus the 6 segment registers) is a self-contained enum in `debugger.h`, with its own name table and parser in `webserver/debug.cpp` (`ConditionRegisterNames`/`ParseConditionRegister`) that has zero connection to `RegisterKind` - confirmed live that `PUT /cpu/register` with `{"register":"al",...}` still correctly 400s exactly as before this item, unaffected.
- **The auto-resume mechanism reuses, rather than reimplements, 2.4's `DispatchDebugRunCallback(DEBUG_Run(1, false))`** - the same primitive `DEBUG_Resume`/`DEBUG_StepOver`/`DEBUG_RunToAddress` already use for an external continue. Verified safe to call from deep inside `DEBUG_Enable` (itself reached via `Callback_Handlers[debugCallback]()` from inside `dosbox.cpp`'s `normal_loop()`) before writing any code: `DEBUG_Run`'s `quickexit=false` tail always ends with `DOSBOX_SetNormalLoop()` regardless of call depth, and `DEBUG_EnableDebugger()` (the actual `Callback_Handlers[debugCallback]` entry) unconditionally sets `exitLoop=true` before calling `DEBUG_Enable` and zeroes `CPU_Cycles` after - so even the silent-skip path still unwinds `normal_loop()` entirely via its post-dispatch `DEBUG_ExitLoop()` check, and `DOSBOX_RunMachine` re-enters `normal_loop()` fresh on its next iteration. This exactly mirrors what a real external continue already does, just triggered automatically instead of by a client request - confirmed by an independent adversarial reviewer tracing the same call chain, no reentrancy or state-corruption path found.
- **A memory condition's read failure evaluates as true (stop), not skip** - deliberately fails toward stopping rather than silently spinning forever on an operand that can never be observed (an unmapped page, say).
- **`once=true` is rejected in combination with `ignore_count`/`condition`**, not silently ignored: a once-only breakpoint self-deletes inside `CheckBreakpoint`/`CheckIntBreakpoint`'s own match-site bookkeeping, before `ShouldSkipBreakpointHit` (which runs later, from `DEBUG_Enable`) could ever look it up by id - `FindById` would return `nullptr` for an already-deleted once breakpoint, so any condition/ignore_count on one could never actually apply. Caught before implementing, not found by review.

**Real bug found by an adversarial review pass (two independent reviewer/verifier pairs, both landing on the same finding), fixed before shipping:** co-located breakpoints let the most-recently-added one permanently shadow an older one's `ignore_count`/condition. `CheckBreakpoint`/`CheckIntBreakpoint` only ever act on the **first** match in list order (`BPoints.push_front` on add, scan from `begin()`, return on first match) and never consider any other breakpoint at the same location in that same pass - `condition`/`ignore_count` only ever get consulted for whichever one `CheckBreakpoint` happened to match. Before this item, which co-located breakpoint matched didn't matter (every match behaved identically: stop), so nothing prevented two breakpoints from coexisting at one address. Now it silently determines whose condition/ignore_count logic ever runs: add a breakpoint with `ignore_count: 5` at an address, then add a second, plain breakpoint at the *same* address, and the second one (pushed to the front) wins every future hit there - the first's `ignore_count` is never consumed, its condition never evaluated, permanently, for as long as both coexist. Also reachable via `CheckIntBreakpoint` for two interrupt breakpoints whose `int`/`ah`/`al` overlap (including a wildcard `ah`/`al` overlapping a specific one).

Fixed at the point of `POST /api/v1/debug/breakpoints`, not by touching `CheckBreakpoint`/`CheckIntBreakpoint` themselves (deliberately - that matching logic is delicate and directly on the ROM-write-protection/INT3-corruption path this item's own design already reasoned carefully about; re-touching it to make it condition-aware would reopen exactly the risk section above avoided). `DebugAddBreakpointCommand::Execute()` now checks, before adding, whether the new breakpoint's location (same segment:offset, or same `int` with overlapping `ah`/`al` accounting for `DEBUG_BPINT_ANY` wildcards) already has another breakpoint there, and refuses with `409 Conflict` whenever *either* side carries a non-default `ignore_count` or `condition`. Two plain breakpoints at the same location (the pre-existing, harmless case - neither carries logic that could be starved) are still allowed, matching prior behavior exactly. Not fixed: `DEBUG_Loop`'s pre-existing IRQ-while-paused bookkeeping (item 2.2 already documented this plants an internal one-shot breakpoint at the current pause address) goes through a direct engine-internal `CBreakpoint::AddBreakpoint` call, not this REST route, so it bypasses the new collision check - if a user's own conditional breakpoint happens to sit at the exact address the debugger was paused at when an IRQ moves execution away, the internal one-shot breakpoint can steal one hit before self-deleting. Judged acceptable and left as a disclosed, narrow, self-correcting residual limitation (worst case: one skipped hit, not a permanent starvation) rather than complicating unrelated IRQ bookkeeping to close a rare edge case.

**Verification:** engine build clean, zero new warnings. Full suite 1109 tests (excluding disabled), same 20 pre-existing `MountPolicyTest` failures, zero new failures. Live-verified against the real `build-debugger` binary: all 12 distinct condition/ignore_count validation cases (register condition, memory condition, unknown register, invalid op, invalid width, negative ignore_count, both-register-and-memory, neither, `once`+condition, `once`+ignore_count) rejected or accepted exactly as designed; breakpoint list round-trips `hit_count`/`ignore_count`/`condition` correctly; `PUT /cpu/register` with a sub-register name confirmed still 400s, unaffected by the new, deliberately separate `ConditionRegister` table; the collision fix verified live across all three shapes (plain-vs-conditional collision rejected, plain-vs-plain collision still allowed, wildcard interrupt overlap rejected) plus the recovery path (delete the conflicting breakpoint, retry succeeds). The runtime auto-skip mechanism itself (does a real breakpoint hit actually get silently skipped N times, then genuinely stop) could not be triggered end-to-end live in this test harness: the only reachable code at this point in boot is BIOS ROM (segment `F000`), where `CBreakpoint::Activate()`'s `0xCC` write silently no-ops (the same ROM write-protection behavior documented in 2.2 and re-confirmed here by reading a target address back after arming), and repeated live polling confirmed the emulator's "normal loop" does not advance over wall-clock time at all in this headless `SDL_VIDEODRIVER=dummy` harness - only the one instruction `DEBUG_Run(1, false)` forces per API call ever executes. This is the same pre-existing environmental limitation item 2.4's own adversarial review hit while trying to live-trigger its `DEBUG_Run` return-value dispatch fix; verified instead by code-level reasoning (see above) plus two independent adversarial reviewers reaching the same conclusion from the same call chain.

Bridge: `debug_breakpoint_add` gained `ignore_count` and `condition` (register or memory form) in its schema, with the 409-conflict and cost tradeoffs documented directly in the tool description; `debug_breakpoint_list`'s description now explains `hit_count`'s "counts skipped hits too" semantics. No handler logic changes needed (`_breakpoint_add` already posts `args` straight through). One new bridge-side test (`_breakpoint_add` posts `ignore_count`/`condition` straight through, closing a pre-existing gap - this tool had no test coverage at all before this item); full bridge suite 185 passed, 2 skipped, zero failures.

#### 2.7 Call stack / backtrace
**Engine. Effort: L. Partly speculative. Status: done.**

Nothing produces a call stack. Walking SS:BP and disambiguating near from far by decoding backwards to confirm a `call` is the same trick `StepOver` relies on. Report per-frame `confidence` and terminate when saved BP does not strictly increase, leaves SS, is below SP, is zero, or a read faults.

**Be honest:** DOS real-mode code is full of hand-written asm, interrupt handlers and leaf routines with no frame pointer. The common answer will be a 1 to 2 frame walk with low confidence. Render low-confidence frames visibly distinct or an agent will chase a function that never called anything. Hard-cap total memory reads and backwards-decode attempts, and raise the command timeout: 64 frames times 4 length probes is 256 `DasmI386` calls inside the Bridge mutex.

**Depends on:** 2.5. `DEBUG_StepOut` belongs here, not in 2.4, and should only be offered when frame 1 resolves with high confidence.

**Shipped**, with one deliberate scope decision beyond the plan's own text, and two real bugs found and fixed by an adversarial review pass (plus one finding the review made but confirmed did not need fixing):

- **New `src/debugger/debugger_backtrace.h`/`.cpp`, unconditionally available** - same reasoning as 2.5's disassembler: walking SS:BP and decoding bytes to confirm a call site has nothing to do with breakpoints or single-stepping, so it doesn't need `C_DEBUGGER` either. Added to `src/debugger/CMakeLists.txt`'s existing unconditional `target_sources` list (the same place `debugger_disasm.cpp` already sits) - no CMake restructuring needed this time, 2.5 already did it. Live-verified on both an `OPT_DEBUGGER=ON` and a fresh `OPT_DEBUGGER=OFF` scratch build: `GET /api/v1/debug/backtrace` returns `200` on the `OFF` binary, where `GET /api/v1/debug/status` (every other `debug/*` route) correctly `501`s.
- **Real-mode-style segment:offset throughout**, deliberately matching every other `debug/*` route this session added (execute/interrupt/memory breakpoints, `disassemble`) rather than the plan's more general "walking SS:BP" framing - no protected-mode GDT resolution for either the code segment (backward-decode candidates) or the stack segment (the BP chain itself). This was a scope decision made before implementing, not found by review: attempting GDT-aware addressing for just the stack segment while every other part of this session's `debug/*` work stays real-mode-only would have been an inconsistent hybrid, and genuine protected mode is out of scope for this whole area by established precedent.
- **`DEBUG_StepOut()`** (`debugger.cpp`, `C_DEBUGGER`-gated, unlike the backtrace walk itself) calls `DEBUG_Backtrace(2)` and, only when frame 1 resolves `high_confidence`, delegates straight to the already-shipped, already-proven `DEBUG_RunToAddress` (2.4) - no new breakpoint-planting logic, reusing a primitive that's already been tested and reviewed. `POST /api/v1/debug/step_out` mirrors `DebugRunToCommand`'s "actual stop happens later" shape, with a third `status` value (`no_confident_caller_frame`, distinct from `not_paused`) so a caller can tell "nothing to step out to" apart from "wasn't paused" - declining is not an error, since the same shaky-frame-pointer heuristics that make the walk uncertain would make a planted breakpoint just as likely to land somewhere nonsensical.
- **Per-frame `confidence`** is `"high"` only for frame 0 (the live CS:EIP/EBP - not a guess at all) or a frame whose return address was confirmed by backward-decoding a matching *near* CALL instruction ending exactly there; `"low"` otherwise, still reporting a best-effort segment:offset (propagated from the last confirmed-or-assumed segment) rather than nothing. `stopped_reason` (`bp_zero`, `bp_not_increasing`, `bp_below_stack_pointer`, `bp_out_of_range`, `stack_read_fault`, or `max_frames`) says *why* the walk ended, distinguishing "hit the requested cap, might continue further" from "the chain itself looked invalid here."
- **Far calls deliberately not chased**, matching the plan's own "disambiguating near from far" framing but resolving it toward safety rather than completeness: confirming a *far* call would mean reading an arbitrary stack slot as a plausible CS selector and backward-decoding whatever happens to be there - trading a small false-negative rate (a far-called frame just stays `low_confidence`) for avoiding a real false-positive risk (a *wrong* high-confidence guess, worse than an honest low-confidence one). `ConfirmNearCallSite` only ever assumes the caller's segment equals the current frame's own segment (the near-call hypothesis), and rejects anything that renders as a far call.
- **Backward-decode probe set is exactly 4 lengths, `{2, 3, 5, 7}` bytes** - chosen to cover the shortest common near-CALL encodings (`FF /2` register-indirect with no displacement, `E8 rel16`, `E8 rel32`/direct far ambiguous-by-length, `FF /2` with SIB+disp32), not an attempt to enumerate every legal encoding. This is also exactly where the plan's own "64 frames times 4 length probes is 256 `DasmI386` calls" arithmetic comes from - confirmed to match by construction, not coincidence: the route's `WaitForCompletion(2000)` reuses the same raised-timeout precedent 2.5's disassemble route already established for an equivalent worst case.
- **`GET /api/v1/debug/backtrace?max_frames=N`** (1..64, default 16 via `num_param`'s optional-param pattern, matching `debug/wait`'s `since_stop_id`/`timeout_ms`), new `backtrace` capability group (`capabilities.cpp`, `AlwaysOn`, independent of `debugger` - same shape as `disassemble`'s).

**Real bug found by an adversarial review pass (two independent reviewer/verifier pairs), fixed before shipping:** the saved-BP/return-IP read width (2 vs 4 bytes) was computed from `cpu.stack.big` (SS's *address-size* attribute), not `cpu.code.big` (CS's *operand-size* attribute - what actually governs how many bytes `push bp`/a near call's return address transfer). These are architecturally orthogonal and can diverge: the reviewer traced `src/cpu/cpu.cpp`'s `CPU_SetSegGeneral`/`CPU_SET_CRX` to confirm a real, known "big real mode stack" trick (load SS from a 32-bit GDT descriptor while briefly in protected mode, then clear `CR0.PE` without reloading SS) leaves `cpu.pmode == false` with `cpu.stack.big == true` but `cpu.code.big` still false - exactly the scenario `debugger_backtrace.h`'s own "unreliable in genuine protected mode" disclaimer does *not* cover, since `cpu.pmode` is false throughout. With the bug, `DEBUG_Backtrace` would read 4-byte dwords at `bp`/`bp+4` instead of the real 2-byte pushes at `bp`/`bp+2`, splicing two unrelated adjacent stack words into one bogus value and desyncing the whole walk - potentially producing exactly the "wrong high-confidence result" this feature's design otherwise goes out of its way to avoid. Fixed by switching the width computation to `cpu.code.big` (the file already computed this correctly, one line above, for the decoder's own `bit32` parameter - it just wasn't reusing it for the read width too). Not independently live-reproduced (bootstrapping genuine protected mode purely over the REST API to toggle `cpu.stack.big`/`cpu.code.big` independently was judged impractical for this item's scope), but the mechanism was traced through exact source citations in `src/cpu/core_normal.cpp`, `prefix_none.h`, and `cpu.cpp` by two independent agents reaching the same conclusion from the same call chain - and the fix was regression-tested against every existing live scenario (below) to confirm the common case, where both flags agree, is unaffected.

**A second real bug found by the same review pass, live-reproduced both before and after the fix:** `ConfirmNearCallSite`'s far-call rejection checked the single raw byte *at the start of* each candidate window for `0x9A` (direct far call) - but a far call preceded by prefix bytes (a redundant segment-override, say) has its opcode somewhere *inside* that window, not at the start of it, so the fixed-position check missed it while the decode still landed exactly on the return offset and rendered a plausible `"call ..."` mnemonic. Live-reproduced exactly as the review found it: bytes `2E 36 9A 34 12 78 56` (two redundant segment-override prefixes then a direct far call to `5678:1234`, 7 bytes total - landing squarely on the probe set's `len=7` slot) placed as a candidate call site returned `confidence: "high"` for what was actually a far call. Fixed by dropping the raw-byte peek entirely and checking the *rendered text* instead: the existing `strstr(text, "far")` check already correctly catches `FF /3` far-indirect regardless of prefixes (its `"far "` keyword is emitted by the decoder's own table entry, unaffected by any preceding prefix), but the direct `0x9A` form never renders that keyword - it renders its segment:offset operand as two colon-joined hex groups instead, which *does* stay correct no matter how many prefix bytes precede the opcode. Added a `strchr(text, ':')` check alongside the existing `"far"` one. This also rejects the rarer near form of a segment-overridden memory operand (e.g. `"call near ds:[1234]"`, which also contains a colon) - an accepted, conservative false negative, not a new gap. Live-reverified with the exact same repro: now correctly returns `confidence: "low"`; regression-tested against the original genuine-near-call and genuine-unprefixed-far-call live scenarios (still `"high"`/`"low"` respectively) and the unrelated `disassemble` route (unaffected, since neither fix touched it).

**A third finding the same review made but confirmed did not need an engine-side fix:** a DOS program can fabricate its own saved-BP/return-IP pair plus matching call-shaped bytes on its own stack to force a `high_confidence` frame that was never really called from - true, but not a defect: this is the same inherent, universally-accepted limitation every frame-pointer-chain backtracer has (gdb and WinDbg's heuristic unwinding included), it doesn't cross any trust boundary this project actually defines (webserver validation, the Bridge, memory safety, resource caps), and the limitation is already disclosed at every layer - `debugger_backtrace.h`'s doc comment, `capabilities.cpp`'s reason text, and the bridge tool description all say plainly that confidence rests on static byte-pattern confirmation, not an execution trace.

**Verification:** engine build clean in both `OPT_DEBUGGER=ON` (this repo's `build-debugger/`) and a fresh `OPT_DEBUGGER=OFF` scratch build, zero new warnings either way. Full suite (`OPT_DEBUGGER=ON`) 1109 tests, same 20 pre-existing `MountPolicyTest` failures, zero new failures, both before and after the review fixes. Live-verified against the real running binary across many hand-crafted stack scenarios (via `PUT /api/v1/memory` + `PUT /api/v1/cpu/register` while paused, then `GET /api/v1/debug/backtrace`): a genuine `E8 rel16` near call 3 bytes before the return address confirms `high`; the same bytes overwritten to NOPs correctly drops to `low`; an unprefixed direct far call (`9A ...`) correctly stays `low`; `saved_bp <= bp` stops with `bp_not_increasing`; `bp == 0` at boot stops with `bp_zero`; `max_frames=0`/`65` correctly `400`, `max_frames=64` succeeds and reports `stopped_reason: "max_frames"` even for a 1-frame walk that hit the cap before any real anomaly; `DEBUG_StepOut` on a `high_confidence` frame 1 arms a one-shot breakpoint at exactly that frame's segment:offset (confirmed via `GET /api/v1/debug/breakpoints` afterward), and on a `low_confidence` frame 1 correctly declines with `no_confident_caller_frame`. Both adversarial-review fixes (read width, far-call prefix bypass) were live-reverified against their exact repro scenarios after the fix, plus a full regression pass over every scenario above.

Bridge: new `debug_backtrace` tool, registered with `feature="backtrace"` - the same deliberate override 2.5's `debug_disassemble` established, since gating it under the default `feature="debugger"` would incorrectly block it on a non-debugger build even though the route works unconditionally there; and new `debug_step_out` tool, using the module's default `feature="debugger"` like every other execution-control tool, since it genuinely does need the debugger capability. Two new bridge-side tests (`_backtrace` builds the right query params, `_step_out` posts with no body); full bridge suite 189 passed, 2 skipped, zero failures. The same adversarial review pass found `dosbox-mcp/tests/test_server.py`'s tool-registration and capability-mode assertions never mentioned either new tool (a real, if low-severity, gap - it also doesn't mention several pre-existing `debug_*` tools, so this wasn't a regression introduced here, but the two new tools are now covered): added `debug_backtrace`/`debug_step_out` to the registration-presence list, `debug_backtrace` to the `observe`-mode presence check, and `debug_step_out` to both the `observe`- and `interact`-mode absence checks (it needs `full`). Also verified through the real MCP dispatch path (`server.request_handlers[types.CallToolRequest]`) against the live `OPT_DEBUGGER=ON` binary for both tools, `isError: false`.

### 2B. Memory

#### 2.8 Memory tool rework: segmented addressing, readable views, compare-and-swap
**Bridge, plus a small engine echo. Effort: M. Status: done.**

The two most-used memory tools are strictly weaker than the routes behind them. The engine serves `/memory/:segment/:offset/:len` with live register-name resolution (CS/SS/DS/ES/FS/GS on the emulation thread); the bridge only builds the linear form, so every real-mode read forces `cpu_read_registers` plus `seg*16+off` arithmetic by hand. The engine implements `If-Match` compare-and-swap returning 412 with the conflicting bytes; the bridge cannot reach it because `DosboxClient.put` takes no `headers` parameter. Both directions speak only base64, which a model cannot reliably decode in context. And `mem_read`'s description says "max 65536" while the engine's cap is 128 MiB and the bridge enforces nothing.

Add `segment` (enum of register names, case-insensitive, or an integer 0..0xFFFF, validated client-side before it is interpolated into a URL path). Document the semantic difference: the register form resolves on the emulation thread at `Execute` time, the numeric form is folded on the web thread, so `"ds"` means DS at the instant of the read and `0x1234` means a fixed address.

Add `view`: `hex` (offset-prefixed dump with a CP437 column via Python's built-in `cp437` codec), `bytes`/`words`/`dwords` (little-endian integer arrays), `text`, `base64` (default for bulk). **Cap the rendered views independently**: hex on 64 KiB is ~4000 lines, a bigger bomb than the base64 it replaces. Refuse the rendered views above a few KiB. Default length 256. Pick one client-side length cap and make description, schema `maximum` and enforcement agree, or you have only moved the lie.

Add `include_registers` defaulting false, and state plainly that this is client-side filtering: `ReadMemoryCommand::Execute` loads registers unconditionally.

Add `expected` (base64) as `If-Match`. **The 412 does not reach the tool by adding headers alone:** `client._handle` raises on any status >= 400 and the 412 body has no top-level `error` key, so the conflicting bytes arrive stringified inside a RuntimeError. Special-case 412 into a typed conflict carrying the parsed actual bytes, or CAS is worse than useless.

**Why the agent is better off:** "read 64 bytes at ES:DI" is one call instead of two plus arithmetic, the result is readable, and CAS makes patching a running game safe instead of a blind clobber of whatever the guest wrote a millisecond earlier.

**Protocol:** `If-Match`/412 is shipped engine behavior that `PROTOCOL.md` never described. Add it, with a test for both the 200 and 412 paths (there is none today on either side).

**Shipped**, almost entirely on the bridge side as the plan predicted - the engine's `Segment` enum and `If-Match`/412 were already fully implemented and shipped, just unreachable through the bridge - plus one deliberate engine-side echo (bounds-checking a pre-existing gap the rework's own live testing surfaced) and, via an adversarial review pass, one real engine bug and eight real bridge bugs found and fixed before shipping:

- **`segment`** (`dosbox_mcp/tools/memory.py`, new `_resolve_segment`): register name (case-insensitive `cs`/`ds`/`es`/`fs`/`gs`/`ss`) or an integer/numeric-string 0x0000..0xFFFF, validated client-side - confirmed exact match against the engine's own `str_to_base_segment`/`parse_mem_addr` (`src/webserver/memory.cpp`) before implementing. `offset` is *not* auto-resolved the same way - see the adversarial-review fix below for why it needed its own, separate validation.
- **`view`**: `base64` (default), `hex` (offset/hex/ASCII dump), `bytes`/`words`/`dwords` (little-endian integer arrays), `text` (CP437-decoded via Python's built-in `cp437` codec - confirmed it never raises across the full 0-255 byte range, unlike a plain UTF-8 decode). Two independent caps, exactly as the plan asked: `MAX_LENGTH_BYTES = 65536` (matching what the tool description already, falsely, claimed - now actually enforced, both via the JSON schema's `maximum` and a handler-side check) for any read, and a much tighter `MAX_RENDERED_VIEW_BYTES = 4096` for anything but `base64`. `words`/`dwords` additionally require `length` to be a multiple of 2/4, rejected up front rather than silently dropping trailing bytes.
- **`include_registers`** (default false): pure bridge-side filtering, documented as such in the tool description - the engine's `ReadMemoryCommand::Execute` calls `regs.load()` unconditionally either way, confirmed by reading it before writing the claim.
- **`expected`/`If-Match`/412**: `DosboxClient.put` gained a `headers` parameter (previously had none at all); `DosboxError` gained a `body` field (the full parsed JSON response body) so a 412's `{"memory": {"addr", "data"}}` - which has no top-level `error` key for `_handle`'s usual fallback to find - doesn't arrive stringified inside the exception message. `mem_write` catches the 412 and returns a typed, non-error result (`{conflict: true, addr, actual_data}`) instead of an error - a CAS conflict is an expected, actionable outcome for a caller doing a compare-and-swap loop, the same reasoning `debug_wait`'s timeout already established for "not satisfied but not broken" outcomes.

**Real bug found by my own testing before the formal review, fixed as part of the initial implementation (not a later patch):** none this time - unlike 2.5's disassemble route, live testing of the base implementation found no bugs before the adversarial review pass did. The review below is what actually found the real ones.

**Real bug found by an adversarial review pass, fixed before shipping - engine, `src/webserver/memory.cpp`:** `parse_mem_addr`'s numeric-segment resolution (`offset += seg_addr`) and both `ReadMemoryCommand::Execute`/`WriteMemoryCommand::Execute`'s `effective_addr = base_segment_to_offset(base) + offset` did plain `uint32_t` addition, promoted to `uint64_t` only *after* for the `mem_total` bounds check - so the promotion could never see a wraparound that already happened. `GET /api/v1/memory/4096/4294967295/16` (segment 0x1000, offset 0xFFFFFFFF) wrapped to physical address 0xFFFF and silently succeeded instead of correctly rejecting an offset that obviously exceeds any real memory config - not a memory-safety bug (`MEM_BlockRead`/`Write` stay within the emulator's own TLB-routed physical space either way) but a real violation of this project's own "bounds-check numeric parameters from API requests" rule, and newly exposed by this exact item making segmented addressing reachable through the bridge for the first time. Fixed by widening both additions to `uint64_t` before either checking or truncating, matching the same "widen first, validate, only then narrow" pattern already established this session for `disassemble`/`backtrace`'s own address arithmetic - `parse_mem_addr` now throws `std::invalid_argument` (400) on overflow, `Execute()` sets `error` the same way its own `mem_total` check already does. Live-reverified with the exact repro: now `400` with `"segment:offset exceeds the addressable range"`; a normal segmented read at a sane offset still returns `200` unaffected.
- **A closely related engine-side gap the same review found and I fixed as an echo of the same fix, not a separate bug:** `WriteMemoryCommand::Execute`'s CAS comparison read used `expected_data.size()` (the `If-Match` length) for its own `MEM_BlockRead`, but only the *write* payload's length (`data.size()`) was ever bounds-checked - a short `data` paired with a longer `If-Match` value could read past `mem_total` into `IllegalPageHandler`'s synthetic fill bytes and compare against those instead of failing with the same clear error the sibling checks give. Fixed by adding the missing, symmetric bounds check on `expected_data.size()` right before the `MEM_BlockRead`, reusing the already-computed `mem_total`/`addr64`. Live-reverified: writing 2 bytes at `mem_total - 2` (fits exactly) with a 4-byte `If-Match` now correctly `400`s with `"If-Match range ... exceeds emulated memory size"` instead of silently comparing against fabricated bytes.

**Real bugs found by the same adversarial review pass, all bridge-side (`dosbox_mcp/tools/memory.py`), fixed before shipping:**
- **`offset` had zero client-side validation**, unlike `segment` and `length` in the very same rework - a float, a negative value, or a value past `0xFFFFFFFF` reached `_mem_path`'s f-string untouched and was only ever rejected several layers downstream by the engine's `num_param<uint32_t>`, producing a raw, engine-internal-looking 400 instead of the bridge's own clear `invalid_argument` result the equivalent mistake on `segment` already got. Not a path-injection risk (a JSON `integer` can't smuggle path separators), but a real internal inconsistency and a genuine "fail early, fail loudly" gap. Fixed with a new `_resolve_offset` (mirroring `_resolve_segment`'s rigor: rejects non-int, rejects `bool`, bounds to `0x00000000..0xFFFFFFFF`), called from `_mem_path` for both tools, plus matching `minimum`/`maximum` added to both schemas as an early SDK-level backstop.
- **`mem_write`'s `data` had no client-side size bound at all**, unlike `mem_read`'s `length` - a caller could make the bridge build, JSON-encode and transmit an arbitrarily large base64 payload before the engine's own (128 MiB) cap ever got a chance to reject it, exactly the class of gap this item's own description asked to close for reads but the rework's first pass missed on the write side. Fixed with `_validate_write_data` (decodes and checks the real byte count against the same `MAX_LENGTH_BYTES`, and rejects malformed base64 with a clear message instead of letting it reach the engine as one), plus a generous `maxLength` on the schema as an early backstop, confirmed live to be enforced by the MCP SDK itself before the handler even runs.
- **`mem_write`'s 412 handler trusted `e.body["memory"]`'s shape with no validation** - a malformed or future-shape body (missing `memory`, or `memory` present but not a dict, or missing `addr`/`data`) would either silently produce `{conflict: true, addr: null, actual_data: null}` as if it were a legitimate conflict, or crash with an unhandled `AttributeError` that the MCP SDK's own generic exception catch would turn into a bare Python exception string instead of this codebase's structured error shape. Not reachable against the real engine today (it always sends `addr`/`data` together), but exactly the untrusted-response-boundary validation this project's rules call for, and the review's own explicit ask. Fixed with an explicit shape check before trusting the body, returning a clear `to_error_result` instead of a silent or crashing failure.
- **`mem_read` never validated the response's actual size before rendering it** - `MAX_LENGTH_BYTES`/`MAX_RENDERED_VIEW_BYTES` only ever bounded the *request*; a response whose decoded byte count didn't match what was asked for (only possible against a non-conforming or compromised connection target, not the real engine) would crash the `words` view outright with an unhandled `IndexError` from indexing past the end of a shorter-than-expected buffer. Fixed with an explicit length check right after decoding, before `_render_view` ever runs.
- **`mem_write`'s success and conflict responses had inconsistent shapes** - success passed through the engine's raw, nested `{"memory": {"addr": ...}}` body unchanged, while the newly-added conflict path returned a hand-flattened `{"conflict": true, "addr": ..., "actual_data": ...}` - so `addr` lived in a different place depending on which of the tool's two possible outcomes happened, undocumented either way. Fixed by flattening the success shape too (`{"status": "ok", "addr": ...}`), matching both the conflict shape's own convention and `mem_read`'s pre-existing top-level `addr`. The tool description was also updated to actually state both shapes - it had previously described the conflict response without mentioning that `addr` is always present there too.

**Verification:** engine build clean, zero new warnings, both before and after the review fixes. Full suite 1109 tests, same 20 pre-existing `MountPolicyTest` failures, zero new failures. Live-verified against the real running binary: segmented reads/writes by register name and by numeric paragraph both resolve to the correct physical address (cross-checked against live `cpu_state`); `If-Match` succeeds when bytes match and the write lands, and correctly `412`s with the real current bytes when they don't, with the write provably blocked either way; both adversarial-review engine fixes (address-overflow rejection, CAS-length bounds) live-reverified against their exact repro scenarios. The new `dosbox-automation` e2e tests (`tests/integration/test_api_contract.py`) could not be run through the full `pytest`+`conftest.py` harness in this environment (`conftest.py` hardcodes `SDL_VIDEODRIVER=offscreen`, which fails to initialize a render backend in this specific sandbox - unrelated to this item); their logic was instead verified by manually driving the exact same `DosboxClient` methods they call against a live instance, which passed. Bridge test suite 235 passed, 2 skipped, zero failures (up from 189 at the start of this item - 46 new tests across validation, CAS handling, and every adversarial-review fix). Also verified through the real MCP dispatch path (`server.request_handlers[types.CallToolRequest]`) against the live binary for `mem_read`/`mem_write` across every view, both segment forms, both cap layers (schema and handler), CAS success/conflict, and every fixed validation gap, `isError` correct in every case.

Bridge: `mem_read`/`mem_write` fully reworked as above; `mem_search`/`dos_memory_map` untouched (2.9's job). `PROTOCOL.md` (lives in the `dosbox-mcp` repo) gained a `1.0.2 (draft)` changelog entry documenting the segmented route forms and `If-Match`/412 semantics - both pre-existing engine behavior that predates this session but was never in the spec.

#### 2.9 mem_search: bound it, then make it a discovery loop
**Engine + bridge. Three commits.**

`SearchMemoryCommand::Post` permits a 16 MiB span and returns every match with no cap and no limit, and the bridge re-dumps it at `indent=2`, one integer per line. A width=1 search for a common byte over conventional memory is megabytes of transcript from one call. There is also no differential next-scan, so the one memory-discovery workflow that matters is structurally unavailable.

**(a) Cap, ship alone. Effort: S. Status: done.** Add `limit` (default 256, max 4096) and return `{matches, total, truncated}`. Do **not** add `offset` to the plain search: without a stored result set, page N re-scans the whole span on the emulation thread. Adding fields to an existing response changes a shape `PROTOCOL.md` makes normative via openapi.json, so add additively and default the limit high enough that existing callers are unaffected, or bump the minor.

**Shipped (a).** No deviations from the plan's own text, no bugs found by testing:

- **`ScanBufferForValue`** (`src/webserver/memory.cpp`/`private/memory.h`) gained `limit` and an optional `size_t* total_out`, both defaulted so every pre-existing call site - the 7 existing unit tests included, whose exact return-value assertions are untouched - compiles and behaves identically to before. `total_out`, when given, receives the *true* match count even past `limit`, without allocating a vector entry for anything beyond it - the scan itself is unchanged (still bounded by the pre-existing `MaxSearchSpanBytes`, 16 MiB), only the amount of match data actually stored and serialized shrinks.
- **`SearchMemoryCommand`** gained a `limit` constructor argument (validated in `Post`: 1..`MaxSearchLimit`, defaulting to `DefaultSearchLimit` when omitted) and a `Total()` accessor. `Post` now returns `{matches, total, truncated}` - `matches` capped at `limit`, `total` uncapped, `truncated = total > matches.size()`. New `DefaultSearchLimit = 256`/`MaxSearchLimit = 4096` constants (`private/memory.h`), matching the plan's own numbers exactly, also surfaced via `capabilities.cpp`'s `memory` group (`default_search_limit`/`max_search_limit`) alongside the pre-existing `max_search_span_bytes`.
- **Treated as a minor protocol bump, not just documentation**, per the plan's own explicit fork ("default the limit high enough that existing callers are unaffected, **or** bump the minor"): a default of 256 is deliberately *not* "high enough to be unaffected" for the actual problem case (a common byte value over a large span can realistically produce far more than 256 matches) - picking a technically-safe-but-arbitrary high default would have undermined the whole point of adding a cap. `PROTOCOL.md` (lives in the `dosbox-mcp` repo) gained a `1.2.0 (draft)` changelog entry - the next minor after the existing `1.1.0` - explicitly calling out that this is a real behavior change for a caller that doesn't pass `limit`, not a no-op clarification like the `1.0.x` patch entries. `resources/webserver/openapi.json`'s `MemorySearch` schema and the route's response schema were updated to match (confirmed the build's copied `Resources/webserver/openapi.json` picks it up automatically, no separate sync step).

**Verification:** engine build clean, zero new warnings. Full suite 1109 tests (4 new `MemSearch` cases: `limit` capping, `limit` at/above the true count, `limit=0`, and the pre-existing unlimited call shape unchanged), same 20 pre-existing `MountPolicyTest` failures, zero new failures. Live-verified against the real running binary: a 300-byte span of a repeated value returns exactly 256 matches by default with `total: 300`/`truncated: true`; an explicit `limit=10` returns 10 with the same `total`/`truncated`; `limit=0` and `limit=4097` both `400` with a clear message; `limit=4096` (the max) succeeds and correctly reports `truncated: false` once every match fits. Bridge: `mem_search` gained `limit` in its schema (passed through only when given, matching `_wait`'s established optional-param pattern) and its description now explains `total`/`truncated`. Six new bridge-side tests (limit omitted vs. passed through, `total`/`truncated` surfaced in the tool's own response); full bridge suite 238 passed, 2 skipped, zero failures. Also verified through the real MCP dispatch path (`server.request_handlers[types.CallToolRequest]`) against the live binary, `isError: false`, matching the direct-HTTP results exactly.

**(c) done** - snapshot/diff, see below. 2.9 as a whole (a, b, c) is complete.

**(b) Masked signature scan. Effort: M. Status: done.** `POST /memory/scan {pattern: "8B 46 ?? 50 E8", start, end, limit}`. Parse and validate on the web thread: hex pairs or `??` only, 1..256 bytes, at least one fixed byte, reject all-wildcard and reject patterns whose fixed-byte count is so low the scan blows the 2000 ms budget. This is what finds a Ghidra function in live memory and is the mechanism behind 2.16's auto-anchor.

**Shipped (b).** One deliberate deviation from the plan's own validation direction, reasoned through and documented rather than followed blindly - plus a real worst-case-cost bug in that reasoning that a subsequent adversarial review caught before shipping, corrected below.

- **`ParseScanPattern`/`ScanBufferForPattern`/`ScanMemoryCommand`** (`src/webserver/memory.cpp`/`private/memory.h`) mirror `ScanBufferForValue`/`SearchMemoryCommand`'s shape exactly - `ScanBufferForPattern` takes the same defaulted `limit`/`total_out` pair, `ScanMemoryCommand::Post` returns the same `{matches, total, truncated}`. New `MinScanPatternBytes`/`MaxScanPatternBytes` (1/256) and `MaxScanWorstCaseOps` constants in `private/memory.h`, `max_scan_pattern_bytes`/`max_scan_worst_case_ops` surfaced via `capabilities.cpp` alongside the search constants.
- **The plan's literal validation direction ("reject patterns whose fixed-byte count is so low") does not hold for the algorithm actually implemented.** The scan does an early-exit masked compare per candidate position (skip wildcards for free, bail on the first fixed-byte mismatch); for that algorithm, worst-case cost per position is bounded by the *fixed*-byte count, not the wildcard count. Two genuinely-motivated checks replaced the plan's literal one: a **selectivity floor** (`256^fixed_count >= span_len`, computed with a 4-iteration cap that can't overflow since `end`/`start` fit in `uint32_t`, exposed as `PatternSelectiveEnoughForSpan`) rejecting patterns too permissive to be a useful signature for the requested span, and a **worst-case CPU bound** (`span_len * fixed_count <= 1e9`, exposed as `PatternWithinScanCpuBudget`) rejecting a densely-specified pattern over a large span.
- **Adversarial review found the worst-case CPU bound's premise was false, and confirmed it by tracing the real code, not just asserting it.** `ScanBufferForPattern`'s inner loop, as first written, walked the *raw pattern including wildcards* per candidate position and only `break`s on a mismatching **fixed** byte - a wildcard falls through to the next iteration instead of being skipped. Cost per position was therefore bounded by pattern length (up to 256), not fixed-byte count, exactly contradicting the comment justifying the budget formula. Concrete repro the review constructed and I reproduced live: 253 `??` wildcards followed by 3 fixed zero bytes, scanned across the full 16 MiB span against near-zero (untouched) memory - passes both guards as written (selectivity needs only 3 fixed bytes at max span; `16777216*3` is nowhere near `1e9`) but the real inner-loop cost approached `16777216*256` (~4.3 billion iterations), synchronously inside `ScanMemoryCommand::Execute` on the emulation thread with the Bridge mutex held - a multi-second stall of the entire emulator, not just the one request, since `WaitForCompletion(2000)`'s timeout is client-side only and cannot cancel or unblock an in-flight `Execute()`. **Fixed at the algorithm, not the budget number:** `ScanBufferForPattern` now precomputes the fixed `(offset, value)` pairs once and loops only over those per candidate position, making the cost genuinely `O(fixed_count)` as the budget check always assumed. Re-verified live: the exact repro pattern now completes in ~1 second including JSON serialization of a quarter-million matches, and a regression test (`MemScanBuffer.MostlyWildcardPatternOverNearZeroBufferMatchesCorrectly`) locks in correctness of the precomputation.
- **The same review found a second, independent unbounded-cost path in my own breakpoint-caveat fix.** The naive version called a per-address `DEBUG_GetOriginalByteAtBreakpoint` once per scanned byte, and that function linearly scanned the *entire* breakpoint list on every call - cost `O(span_len * breakpoint_count)`, invisible to both validation guards since neither has any notion of how many breakpoints are currently registered. **Replaced with a single range query:** `DEBUG_GetOriginalBytesInRange(start, end) -> std::unordered_map<PhysPt, uint8_t>` does one `O(breakpoint_count)` pass building a map of just the breakpoints inside the scanned span, called once before the buffer-fill loop; each byte then does an O(1) hash lookup. `mem_readb<MemOpMode::SkipBreakpoints>` (the mechanism `disassemble.cpp`/`wait.cpp`/`debug_events.cpp` already use) only suppresses `DEBUG_UpdateMemoryReadBreakpoints` - a heavy-debugger-only memory-*read*-watchpoint side effect - and was never sufficient by itself; confirmed empirically both before and after this fix (an armed execute breakpoint's `0xCC` was missed by `SkipBreakpoints` alone, then correctly read through once the range-query substitution was added). Gated `#if C_DEBUGGER && !C_HEAVY_DEBUGGER` with an inline no-op fallback for the other two build configurations, mirroring `DEBUG_UpdateMemoryReadBreakpoints`'s own pattern for the opposite condition. No cap exists anywhere on total breakpoint count (checked); left as an acknowledged, pre-existing characteristic of the debugger module rather than expanding this item's scope - `CheckBreakpoint`'s existing per-instruction linear scan over the same list, on a far hotter path, already has the same property.
- **Three smaller findings from the same review, all fixed:** the malformed-pattern-token error echoed the untrusted token back with no length cap (now truncated at 32 chars); a pattern longer than the requested span silently returned `200` with an empty result instead of a clear `400` (now rejected explicitly); the two new validation guards had zero automated test coverage at any level, which is exactly how the cost-model bug above went unnoticed until an adversarial pass looked for it (now 17 new unit tests directly against `PatternSelectiveEnoughForSpan`/`PatternWithinScanCpuBudget`/`ScanBufferForPattern`, including the review's own repro scenario as a named regression test).

**Verification:** all three build configurations compile clean, both before and after the review fixes - no debugger (`OPT_DEBUGGER=OFF`, the plain `build/` most users run), non-heavy debugger (`build-debugger/`), and heavy debugger (temporary scratch config each time, confirmed the `#else` no-op paths compile and the rest of `debugger.cpp` is unaffected). Full suite 1132 tests (23 new `MemScanParse`/`MemScanBuffer`/`MemScanSelectivity`/`MemScanCpuBudget` cases), same 20 pre-existing `MountPolicyTest` failures, zero new. Live-verified against the real running binary, both before and after the review: the plan's own example pattern finds both hand-written matches with the wildcard byte varying; all-wildcard, malformed tokens, >256 tokens, span >16 MiB, pattern-longer-than-span, and both `limit` bounds each `400` with a clear message; the selectivity and worst-case-CPU guards exercised directly at their exact boundaries; `capabilities.memory.limits` now reports `max_scan_worst_case_ops`. The breakpoint fix was verified with the actual mechanism it targets: paused the non-heavy-debugger binary, armed an execute breakpoint over a hand-written signature, confirmed a raw memory read shows the patched `0xCC`, then confirmed the scan still reports the match through the range-query substitution - re-run after the naive per-byte version had already been shown to miss it. The review's own worst-case-cost repro (253 wildcards + 3 trailing fixed bytes over the full 16 MiB span, near-zero memory) was reproduced live before the algorithm fix was confirmed necessary, and re-timed after: ~1 second including response serialization, down from an unmeasured multi-second single-threaded stall of the whole emulator. Bridge: `mem_scan` added (`dosbox_mcp/tools/memory.py`), same optional-`limit`-passthrough shape as `mem_search`; 4 new tests; full bridge suite 242 passed, 2 skipped. Verified through the real MCP dispatch path against the live binary for both a successful scan and a validation rejection, matching direct HTTP exactly.

**Adversarial review, for the record:** a 4-dimension review (parsing/validation correctness, the new debugger cross-module boundary, security/DoS resistance, consistency and test coverage) found 10 issues, all independently confirmed by 2/2 skeptical re-verification votes each - no false positives, and the two high-severity ones (the cost-model bug and the per-byte breakpoint lookup) were real, live-reproducible bugs in code I had already built, tested, and live-verified as working before the review ran. Worth internalizing: "I verified this live and it looked fast" is not the same claim as "I proved the worst case is bounded," and this item is the concrete case where the gap between those two mattered.

**(c) Snapshot and refine. Effort: L. Status: done.** `POST /memory/snapshot` returning a handle, and `POST /memory/diff {handle, op: changed|unchanged|increased|decreased|equals}`. Cap by **total bytes** (e.g. 32 MiB across all snapshots) with LRU eviction, not by count. Cap a stored address set at 65536, because refine reads each recorded address individually on the emulation thread inside one `Execute` with the Bridge mutex held. Do only the memcpy on the emulation thread; keep the registry and the comparison on the web thread, following `FreezeRegistry`. Re-validate every address against `MEM_TotalPages()*MemPageSize` with 64-bit math before reading. Clear the registry in `WEBSERVER_Destroy`.

**Caveat for (c):** on a non-heavy debugger build, active execute breakpoints are `0xCC` bytes in guest memory, so a diff taken while running can match traps. (b) shipped `DEBUG_GetOriginalBytesInRange` for exactly this - reuse the same single-range-query shape (not a per-address call in a per-recorded-address loop) rather than re-introducing the O(n * breakpoint_count) mistake (b) itself made and fixed.

**Shipped (c).** New module (`src/webserver/memory_snapshot.cpp`/`private/memory_snapshot.h`), not folded into `memory.cpp` - a genuinely different concern from search/scan, matching how `freeze.cpp` is its own file. `SnapshotRegistry` follows `FreezeRegistry`'s shape exactly: a mutex-protected singleton holding pure data with zero knowledge of the Bridge or emulation thread. A snapshot starts **Dense** (a plain byte copy of `[start, end)`, capped at `MaxSearchSpanBytes` per request like search/scan); the first `diff` call on a handle does a dense scan (mirroring `ScanBufferForValue`'s overlapping-window, little-endian-`width` convention exactly) and moves the handle to **Sparse** mode, keeping only survivors (capped at `MaxCandidates = 65536`, the number `Execute` can read individually inside one Bridge call without stalling the emulation thread too long); every `diff` call after that re-reads and re-compares only that shrinking set, in one `SnapshotSparseReadCommand::Execute`. Each round **re-baselines**: `increased` means increased since the *previous* diff call on that handle, not the original snapshot - the multi-round "refine" workflow the item's own name describes. `equals` is a literal synonym for `unchanged` (`ParseDiffOp` maps both to the same enum value): there is only ever one recorded value per candidate to compare against, so the two names can't mean anything different - documented as a deliberate reading of the plan's five-name list, not an oversight. Response shape `{matches, total, truncated, candidates}` - `matches`/`total`/`truncated` match `memory/search`'s contract; `candidates` is new, disambiguating "how many survived this round" from "how many are trackable for the next round" (they can differ when `MaxCandidates` truncates the stored set).

**A 4-dimension adversarial review found 13 confirmed issues** (registry correctness/LRU, diff semantics, security/cost-bound verification, consistency/tests), the most important of which was a real gap against this item's own stated caveat, not a subtle edge case:

- **The breakpoint-read-through caveat above was written but never implemented.** Both `SnapshotRangeReadCommand::Execute` and `SnapshotSparseReadCommand::Execute` called raw `MEM_BlockRead` - no `DEBUG_GetOriginalBytesInRange` call anywhere, meaning a snapshot or diff over an active execute breakpoint silently recorded/compared the `0xCC` trap byte, exactly the failure mode this item's own text warns about. Worse: `MEM_BlockRead` unconditionally uses `mem_readb`'s default `MemOpMode::WithBreakpoints`, which calls `DEBUG_UpdateMemoryReadBreakpoints` - an *unbounded per-byte* linear scan of the whole breakpoint list on a heavy-debugger build, the same cost-model mistake (b)'s review had already found and fixed, reintroduced here through a different call path. Fixed by switching both commands to `mem_readb<MemOpMode::SkipBreakpoints>` plus one `DEBUG_GetOriginalBytesInRange` query up front (the dense command queries `[start, end)`; the sparse command queries the whole address space once, since candidates aren't contiguous and the function's cost is independent of range width) - exactly the pattern (b) established. Live-reverified with the same breakpoint-armed repro (b) used: raw read shows `0xCC`, both a dense diff and a subsequent sparse refine round correctly report the real byte underneath.
- **`byte_size` accounting for Sparse mode undercounted the real cost of `std::unordered_map<uint32_t,uint32_t>` by roughly 5-6x** (measured, not just estimated: a 65536-entry map's real heap growth vs. the 8-bytes-per-candidate the code charged) - meaning the 32 MiB budget didn't actually bound real memory use anywhere close to 32 MiB once several Sparse-mode entries existed. Fixed by charging a documented, deliberately conservative estimate (`ApproxSparseEntryOverheadBytes = 48`) instead of a `sizeof()`-derived one that would repeat the same mistake.
- **Two independent bugs in `SnapshotRegistry::Create`'s ordering**, both around `EvictLocked`: it ran before checking whether the new entry could ever fit (so a doomed request would evict every other live snapshot first, unreachable today only because the per-request span cap is comfortably under the total budget, not because the code enforced that relationship); and its own linear-scan-per-eviction is O(k·n) for evicting k of n entries, bounded (`MaxEntries²` ≈ 16.8M cheap comparisons at the extreme) but worth documenting rather than leaving unstated. Fixed the ordering; left the O(n) scan as-is with a comment explaining why a full LRU data structure isn't worth the added complexity for a bounded, sub-100ms worst case.
- **`GetReadPlan` never touched LRU state**, making a handle *actively being diffed* (mid-Bridge-round-trip) the single most eviction-prone entry in the registry for that whole window. Fixed by having `GetReadPlan` count as a touch - correct because it's never exposed as a standalone lookup, only ever the first step of an actual diff attempt.
- **`DiffSparse` had no protection against two requests racing on the same handle**, unlike `DiffDense` (whose mode-flip incidentally makes a second concurrent dense diff cleanly fail). A second caller's comparison could run against whatever the first caller's diff had already mutated the candidate set to, silently mixing two different rounds' data. Fixed with an explicit `generation` counter per entry, threaded through `SnapshotReadPlan`/`DiffDense`/`DiffSparse`: a stale generation is refused, not silently applied.
- **`MaxCandidates` truncation always keeps the numerically-lowest matching addresses**, an arbitrary, undocumented bias that could silently and permanently drop the address a caller is actually looking for on the very first over-broad round. Not changed algorithmically (no deterministic selection is meaningfully better, and the existing `truncated`/`candidates` fields already surface that truncation happened) - instead documented explicitly, in the struct comment, the bridge tool description, and `PROTOCOL.md`, that a heavily-truncated round should prompt narrowing the search and taking a new snapshot, not trusting the kept subset.
- **`MemoryDiffHandlers::Post` used 400 for "handle not found,"** inconsistent with this codebase's established convention for the same class of case (`FreezeHandlers::Delete`, debug.cpp's breakpoint-delete both use 404). Fixed to 404, keeping the structured `error`/`error_code`/`retryable` body the rest of this module's exception-driven errors get (httplib's exception handler has no 404 mapping, so this one path builds the response manually rather than throwing).
- **The HTTP-level validation in `MemorySnapshotHandlers::Post`/`MemoryDiffHandlers::Post` had zero automated coverage** - the C++ suite only exercised the pure `SnapshotRegistry` class. Added Python integration tests (`tests/integration/test_api_contract.py`) covering the span cap, width lock-in, unknown-handle 404, bad-op 400, and the full snapshot/changed/refine/exhaustion sequence - logic verified live against the real binary (same `SDL_VIDEODRIVER=offscreen` sandbox limitation as every other item this session; run manually instead of through `pytest`+`conftest.py`).

One finding was investigated and correctly found **not real**: `DiffDense`/`DiffSparse` trust their documented input-length preconditions without asserting them, which is true but not reachable - the single call site always satisfies the contract.

**Verification:** all three build configurations compile clean, both before and after the review fixes. Full suite 1154 tests (22 new `SnapshotRegistryTest`/`ParseDiffOpTest` cases), same 20 pre-existing `MountPolicyTest` failures, zero new. Live-verified against the real running binary, both before and after the fixes: basic snapshot creation; span-too-large 400; the full changed/equals/refine sequence with exact byte-level assertions matching the new integration tests; width lock-in rejects a mismatched width at round 2; a handle narrowed to zero candidates is removed and a further diff on it 404s; the breakpoint-read-through fix re-verified with the same armed-breakpoint repro (b) used, for both the dense and sparse read paths. Bridge: `mem_snapshot`/`mem_diff` added (`dosbox_mcp/tools/memory.py`), thin passthroughs matching `mem_search`/`mem_scan`'s shape; 6 new tests; full bridge suite 248 passed, 2 skipped. Verified through the real MCP dispatch path against the live binary, including the 404-handle-not-found case surfacing correctly as a tool error.

**Pattern across (b) and (c), worth stating plainly:** both items shipped, were live-tested, and looked correct - and both had a real, adversarial-review-only-findable bug in a cost-bound or correctness claim that felt obviously true while writing it. Live testing with friendly inputs confirms a feature works; it does not confirm a stated worst-case bound actually holds. Treat those as separate claims requiring separate verification.

#### 2.10 Allocation tools
**Bridge. Effort: S. Depends on 1.4. Status: done.**

Once the truncation, the free-by-registry gate and the JSON error bodies land, expose `mem_alloc` / `mem_free` / `mem_allocations`. Free-memory totals (largest conventional block, conventional free, UMB, XMS) belong on the allocate response or the allocations route, **not** in `dos/internals`: `private/dos.h:36-39` states that command's purpose verbatim as pointers only, "not a place to pull random info that can also be read by the client from these addresses directly". Everything else the enrichment proposals wanted (PSP, parent chain, environment, current drive, DOS version) is reachable from the list-of-lists and the SDA the route already hands out.

Two small defects in the same struct while you are there: `list_of_lists`, `dos_swappable_area` and `first_shell` are declared `uint16_t` but assigned `PhysPt` (`uint32_t`), and `WalkMcbChain` truncates silently on a corrupt chain with no flag in the response. Add `truncated`.

**Shipped.** The `mem_alloc`/`mem_free` engine routes and the `AllocationRegistry` free-by-registry gate already existed (an earlier item's prerequisite work), but the JSON error bodies did not - both were addressed here alongside the new listing route and the two struct fixes.

- **JSON error bodies**: `AllocMemoryCommand::Post`'s 503 and `FreeMemoryCommand::Post`'s 400 previously set only a status code with an empty body. Both now send `{"error": "..."}`, and the 503 case distinguishes *why* allocation failed - `AllocMemoryCommand` gained a `registry_full` flag so the message says "allocation registry is full" versus "insufficient free memory" instead of one generic string for two different causes.
- **The two struct defects**: `DosInternalsCommand`'s `list_of_lists`/`dos_swappable_area`/`first_shell` are now `PhysPt` (`uint32_t`), matching what `RealToPhysical`/`PhysicalMake` actually return - the narrowing was live and silent (`openapi.json`'s own pre-existing example values, 655360/720896/786432, already exceeded `uint16_t`'s range, meaning the documented contract and the actual wire type had already diverged before this fix). A new `McbChainTruncated(chain)` helper (`chain.empty() || !chain.back().is_last`) backs a `memoryMapTruncated` field on `dos/internals` and a `conventionalTruncated`/`umbTruncated` pair on the new allocations route - one function, since both read the identical MCB-chain-walk primitive and both need the identical truncation signal.
- **`GET /api/v1/memory/allocations`** (`MemoryAllocationsCommand`, new): `{allocations: [{addr, size, area}], conventionalFreeBytes, conventionalLargestBlockBytes, conventionalTruncated, umbFreeBytes, umbTruncated, xmsFreeBytes}`. Free-memory totals are computed from a fresh `WalkMcbChain` walk (conventional and, if linked, UMB), not from the classic "request 0xFFFF paragraphs, read the largest-block size back out of the failure" DOS trick (`DOS_AllocateMemory`'s own mechanism) - that trick also calls `DOS_CompressMemory()` as a side effect, which a pure read-only stats query has no business triggering. `xmsFreeBytes` is `MEM_FreeTotal() * MEM_PAGE_SIZE`, the same page-allocator pool `mem_alloc`'s own XMS area draws from.
- **`AllocationRegistry` extended from a bare address set to `{addr: {size, area, owner_psp}}`**: `Add()` now takes the actually-allocated byte count (paragraph/page-rounded, not the raw request size - both `AllocDos`/`AllocXms` already computed this for their `LOG_DEBUG` lines) and the `MemoryArea`, and a new `List()` returns every live entry address-ascending (`unordered_map` iteration order is otherwise unspecified, and a route an agent might poll deserves a stable order). A new `AreaName(MemoryArea)` free function is the single source of truth for the `"CONV"/"UMA"/"XMS"` strings, shared between `mem_alloc`'s request parsing (already existed) and the new listing route's response (added here). `owner_psp` is a review fix, see below.
- **Bridge**: `mem_alloc` (`POST /memory/allocate`), `mem_free` (`POST /memory/free`), `mem_allocations` (`GET /memory/allocations`) in the `memory` group. `mem_alloc`/`mem_free` are `read_only=False` (full mode only - they mutate real guest/DOS allocator state, unlike e.g. `mem_snapshot`'s bridge-side-only mutation); `mem_allocations` is `read_only=True` (observe mode too). Schema-level `minimum`/`maximum` on `size` (1-65535) mirrors the engine's own bound as a fast backstop, confirmed enforced pre-dispatch by the MCP SDK (same pattern 2.16 established and verified).
- **Found while touching `dos/internals`, not fixed - out of scope for this item**: `McbBlock.type` is a raw `uint8_t` (e.g. `77` for `'M'`), serialized by `entry["type"] = b.type` as a JSON *number*; `openapi.json`'s existing (pre-dating this item) schema and example both document it as a *string* (`"M"`/`"Z"`). Confirmed live: `GET /dos/internals` really does return `"type": 77`, not `"type": "M"`. This is a real, pre-existing documentation/wire mismatch, unrelated to anything 2.10 touches - noted here rather than silently left for the next person to rediscover, not fixed, since fixing it belongs with whoever next touches `dos/internals` for its own sake.
- **`PROTOCOL.md` (dosbox-mcp)** gained a `1.6.0 (draft)` entry: documents `memory/allocate`/`memory/free` for the first time (they predate this session but were never added to the spec), the new `memory/allocations` route, and the `dos/internals` `memoryMapTruncated` addition. `resources/webserver/openapi.json` (this repo) gained the new route's schema and fixed `MemoryAllocate`'s `required` list, which incorrectly listed `area`/`strategy` as required when the engine has always defaulted them.

Adversarial review (2 dimensions - engine correctness/security, bridge consistency/completeness - each finding independently re-verified by 2 skeptical agents): 4 raw findings, all 4 confirmed, 0 rejected. All fixed:

- **(high) The registry can go silently stale across a DOS process exit, and `mem_free` would then corrupt a different, currently-running program's memory while reporting plain success.** `AllocDos()` allocates through `DOS_AllocateMemory`, which stamps the new block's MCB with *whatever PSP happens to be active at that moment* (`dos.psp()`), not a bridge-reserved one - there is no such thing in this engine. If that program later exits normally, `DOS_FreeProcessMemory` reclaims every block it owns, including ones `mem_alloc` handed out, with zero notification to `AllocationRegistry`. DOS can then hand that exact segment to a *different* program. A later `mem_free` on the stale address would pass the registry check (the stale entry is still there) and then pass `DOS_FreeMemory` too (its only check is the MCB type byte, not ownership - it has a standing `// TODO Check if allowed to free this segment`) - silently flipping the new program's in-use memory to free. **Fixed, not by reserving a bridge-owned PSP (a materially larger change touching core DOS process-management assumptions, out of proportion for an "Effort: S" item) but by detecting the mismatch**: `AllocationInfo` gained `owner_psp` (the PSP `dos.psp()` was at allocation time, meaningless for Xms which isn't PSP/MCB-tracked at all), and `FreeMemoryCommand::Execute` now re-reads the block's *current* MCB owner before freeing a Conv/Uma entry, refusing (400, new `owner_changed` error code) if it no longer matches. `AllocationRegistry::Remove` changed from `bool` to `std::optional<AllocationInfo>` so `Execute` has the removed entry's `area`/`owner_psp` to check against without a second lookup. Live-verified by directly overwriting a live allocation's MCB `psp_segment` field via `mem_write` (the same effect a process-exit-then-reallocate sequence would have) and confirming `mem_free` now refuses it instead of silently succeeding, then confirmed a normal (unmodified-owner) free still works. **Residual gap, disclosed rather than chased further**: this detects the common case (a different program, almost certainly a different PSP value) but not the theoretical case of DOS recycling the exact same PSP segment number for an unrelated later process - judged not worth the complexity this deep into a hardening fix for an "S" item; the tool descriptions and `PROTOCOL.md` say plainly that a block should be freed before the program active at allocation time exits, not relied on as a bridge-managed heap.
- **(medium) `MemoryAllocationsCommand`'s UMB gate was weaker than every existing engine call site's for the identical field.** It walked the UMB chain whenever `GetStartOfUMBChain() != 0xffff`; `DOS_AllocateMemory`/`DOS_FreeProcessMemory`/`DOS_LinkUMBsToMemChain` all instead require exact equality with the fixed link segment, treating anything else as corruption. A guest-corrupted or `mem_write`-set value in between would make `WalkMcbChain` parse whatever guest bytes happen to sit at that segment as if they were real MCBs (it only validates the type byte, not that the chain is genuine), silently folding fabricated numbers into `umbFreeBytes` and reporting `umbTruncated: false` as if trustworthy. **Fixed**: the shared constant this needed was previously private to `dos_memory.cpp` (`umb_start_seg`) - promoted to `UmbStartSegment` in the already-existing shared `dos/dos_memory.h`, with `dos_memory.cpp`'s own five call sites switched to reference it too (one definition, not a second copy), and `MemoryAllocationsCommand` now requires exact equality like its siblings.
- **(high) The new 503/400 error bodies had `error` but not `error_code`/`retryable`**, unlike every other error response in this API (the centralized exception handler always emits both), which `dosbox-mcp`'s `DosboxClient._handle()` explicitly depends on for retry hints and programmatic cause-branching - both silently degraded to `"unknown"`/`false` for these two routes specifically. **Fixed**: both now carry distinct `error_code` values (`registry_full`/`insufficient_memory` for allocate, `not_allocated`/`owner_changed` for free), `retryable: false` for all four (none of these clear on their own - the caller has to act, not just wait, matching this API's existing `retryable` semantics for e.g. `bridge_timeout`).
- **(medium) `_mem_alloc`'s failure path had zero bridge-side test coverage**, unlike the identical case already covered for its sibling `_mem_free`. **Fixed**: added `test_mem_alloc_propagates_a_dosbox_error_on_failure`, mirroring the existing `_mem_free` test exactly.

**Verification:** engine build clean (both `build` and `build-debugger`), zero new warnings, both before and after the review-fix pass. Full suite grew from 1109 to 1168 tests (new: `McbChainTruncatedTest` in `webserver_dos_map_tests.cpp`; `webserver_alloc_tests.cpp`'s pre-existing `AllocRegistryTest` suite updated for the new `Add`/`Remove` signatures plus new `List()`/`AreaName()`/owner-psp coverage), same 20 pre-existing `MountPolicyTest` failures, zero new failures. Live-verified against the real `build-debugger` binary, both before and after the review fixes: allocate/list/free round-trips for all three areas (CONV/UMA/XMS), double-free correctly 400s with the new error body, exhausting conventional memory via repeated large allocations correctly 503s with "insufficient free memory," `dos/internals` and the allocations route both report the truncation flags as `false` in the normal case, free-memory totals move correctly and return to baseline after freeing everything, and - the review-fix-specific check - directly corrupting a live allocation's MCB owner via `mem_write` and confirming `mem_free` now refuses it with `owner_changed` instead of silently succeeding, with the guest memory itself confirmed untouched afterward. Bridge: full suite 292 passed, 2 skipped (up from 283 before this item, 291 before the review-fix pass). Also live-verified through the real MCP dispatch path (`server.request_handlers[types.CallToolRequest]`): `mem_alloc`/`mem_free`/`mem_allocations` all round-trip correctly, `mem_alloc size=0` is rejected by the JSON-schema backstop before the handler runs, a double-free correctly surfaces as `isError` via `guard()`, and mode gating is correct (`mem_allocations` present in observe mode, `mem_alloc`/`mem_free` present only in full).

### 2C. Machine control

#### 2.11 Drive and mount visibility, then swapping
**Engine + bridge. Effort: M. Status: done.**

"Automated multi-disk installs" is a headline feature an agent cannot perform. `POST /drive/swap` is fully implemented with mount-policy validation and correct build-before-release ordering, `PROTOCOL.md:126` lists it in the 1.0 contract, and no MCP tool calls it. Neither does anything call `mount/lock`. There is no read side at all. The decisive detail: `MountPolicy::ValidateImagePath` denies every API-origin mount when `allowed_image_roots` is empty, and the default config has no roots. So the out-of-the-box agent experience is a flat 400 "Blocked by mount policy" with no way to discover that an operator must configure roots.

- `GET /api/v1/drive` as a Bridge Command walking `Drives[]`. **Do not use `GetTypeString()`/`GetInfoString()`**: they go through `MSG_Get` and return localized UI strings. Map `DosDriveType` to stable lowercase identifiers yourself and keep `GetInfo()` as a separate free-text field. `GetInfo()` on a local drive is the mounted host path: emitting it is an information disclosure, acceptable behind the token on loopback, but decide it deliberately in a comment rather than leaking it by accident.
- **Surface `DenyReason` as a stable code.** `MountPolicy` already computes DoesNotResolve / NotRegularFile / SymlinkComponent / SystemPath / OutsideWhitelist / NotADiskImage and `drive.cpp:46` throws all of it away for one string. This single change is worth more to an agent than the listing route.
- `GET /api/v1/mount/policy` returning locked state and the configured roots.
- `GET /api/v1/mount/images`, if wanted: non-recursive, regular files only, hard cap per root, canonicalize each entry and re-run `IsUnderAnyRoot` before emitting, skip symlink components, run on the **web thread** (filesystem I/O must never sit inside a Bridge Command).
- Bridge: `drive_list`, `mount_status`, `mount_images` (read-only), `drive_swap` and `mount_lock` in group `media`, with `mount_lock` marked destructive since the latch is one-way by design.

**Note in the tool description:** `drive/swap` has no check that the target drive is mounted, and it constructs a `fatDrive` (real disk I/O) inside the Bridge with the emulation thread blocked under a 5000 ms deadline.

**Shipped.** Every piece of the plan text landed on both sides.

- **Engine (`src/webserver/drive.h`/`.cpp`)**: `DriveDenyReasonCode(DenyReason)` maps the six real deny reasons plus `None` to stable lowercase wire codes. `DriveSwapCommand` reworked: `Execute()` now records *which* of six distinct failure modes occurred (`locked`, `deny_reason`, `file_not_found`, `invalid_drive`, `mount_failed`) instead of only the free-text `Command::error`, and `Post()` maps each to its own HTTP status and `error_code` - `mount_locked` moved from a same-shape-as-everything-else 400 to its own 403 (previously 403 was documented as covering both "locked" and "policy violation"; policy violations are 400 now, split by `DenyReason`), and `file_not_found`/`mount_failed` are new 404/500 cases carved out of what used to be one generic 400. New `DriveListCommand` (`GET /api/v1/drive`, a Bridge Command since `Drives[]` is emulation-thread state): all 26 letters always, hand-mapped `type` strings (never `GetTypeString()`), `info`/`read_only`/`removable` present only when mounted. New `MountHandlers` struct - `GetLock`/`PostLock` (moved from inline lambdas that already existed in `webserver.cpp`, identical logic, now alongside the new `GetPolicy`/`GetImages`), `GetPolicy` (`GET /api/v1/mount/policy`), `GetImages` (`GET /api/v1/mount/images`) - none of these cross the Bridge: `MountPolicy`'s config is read-only after startup and `Lock()`/`IsLocked()` is a plain atomic, and `GetImages` does real filesystem I/O via the new `ScanImageRoot(root, cap)`, which must never sit inside a Bridge Command regardless. `capabilities.cpp`'s existing always-on `drive` capability gained a `max_images_per_root` limit. `resources/webserver/openapi.json` gained the three new paths, `drive/swap`'s updated response documentation, and (a small adjacent fix, needed because the new routes would otherwise have inherited it) the shared `Error` schema component's missing `error_code`/`retryable` properties - present in real responses since 2.10 but never added to the schema that documents them.
- **Bridge (`dosbox_mcp/tools/media.py`)**: new `register_drive()` (separate from the existing `register()`, which stays video-capture-only), adding `drive_list`, `mount_status` (calls `GET /mount/policy`, the superset of the bare `GET /mount/lock` - no separate bridge tool for the subset), `mount_images`, `drive_swap`, `mount_lock`, all `feature="drive"`, group `"media"` (already in `_INTERACT_GROUPS`, so the two mutating tools are reachable from interact mode, not just full - consistent with `video_capture_start` already being there). `mount_lock`'s "destructive" status needed no special plumbing: `read_only=False` alone already gets it `destructiveHint: true` by the MCP spec's own default.
- **Adversarial review (3 dimensions - engine security, engine correctness, bridge completeness/plan-conformance - each finding independently re-verified by 2 skeptical agents): 4 raw findings, all 4 confirmed, 0 rejected.** All fixed:
  - **(medium) `ScanImageRoot`'s cap only bounded accepted images, not raw directory entries examined, so a root full of disqualifying entries (subdirectories, broken symlinks, device nodes) was walked without limit before the cap could ever engage - directly contradicting the code's own comment describing the cap as a backstop against "tens of thousands of files."** Fixed with a second, separate bound (`max_entries_scanned`, defaulting to `cap * DefaultMaxScannedEntriesMultiplier`) checked on every entry regardless of whether it qualifies, refactored around a small `consider` lambda so the per-entry qualification logic (unchanged) stays readable against the new outer walk.
  - **(medium) The same function's directory walk used a range-based `for` loop's implicit throwing `directory_iterator::operator++()`, unlike every other filesystem call in it, which all take an `error_code` out-param - a root that became unreadable mid-scan (unmounted removable/network media, the directory deleted, a permission change) threw instead of degrading gracefully, surfacing as an opaque 500 rather than the partial-result-so-far the same function already returns for a root that doesn't exist at scan start.** Fixed by switching to a manual walk using `directory_iterator::increment(error_code&)`, treating an increment failure the same as hitting the entry-scan cap: stop and report `truncated`, keep what was already found.
  - **(low) `feelies/REFCARD.md`'s "Media and recording" section - the file's own stated purpose is "every tool, one block" - wasn't updated for the five new tools.** Fixed by adding them in the file's existing style. Noted, not chased further: this file already predated several other already-shipped items (2.10's alloc tools, 2.16, 2.17 among them) without being updated for those either - a pre-existing, broader staleness this item didn't introduce and doesn't fully close.
  - **(low) `feelies/MANUAL.md`'s "Driving a game" walkthrough - written to motivate exactly the "automated multi-disk installs" capability this item's own plan text names as the headline blocked feature - still didn't mention `drive_swap` anywhere.** Fixed: a step added to the walkthrough's numbered list, plus a new "Multi-disk installs" section in the same weight this manual already gives other specialized capabilities (Ghidra, memory-watch logging).

**Verification:** both engine build configurations (`build`, `build-debugger`) compile clean, before and after the review-fix pass. Full C++ suite grew from 1164 (before this item) to 1172 (initial implementation) to 1175 (after the review-fix pass: new `tests/webserver_drive_tests.cpp` - `DriveDenyReasonCodeTest`'s exhaustive enum mapping, `ScanImageRootTest`'s temp-dir fixture covering empty/nonexistent roots, size reporting, non-recursion, symlink skipping, the accepted-images cap's exact-boundary correctness, and (review-fix-driven) the new entries-scanned cap's own exact-boundary correctness), same 20 pre-existing `MountPolicyTest` failures (an unrelated, already-documented macOS temp-dir symlink environmental issue), zero new failures. Live-verified against a real `build-debugger` binary, twice (once before the review-fix pass, once after, to confirm the `ScanImageRoot` rewrite didn't change observable behavior for a well-formed root): a synthetic FAT12-formatted 1.44 MB floppy image was built and placed under a configured `mount_allowed_image_roots`; `GET /drive`, `GET /mount/policy`, and `GET /mount/images` all correctly reflected it and the engine's own bundled/virtual drives (Y, Z); `POST /drive/swap` successfully mounted it onto a free letter, confirmed via a follow-up `GET /drive`; every `DenyReason`-derived `error_code` was individually triggered and confirmed (`outside_whitelist`, `does_not_resolve`, `symlink_component` - incidentally, via `/etc/hosts` on macOS where `/etc` is itself a symlink - `missing_field`, `invalid_drive_letter`), along with `mount_failed` (an earlier, structurally-invalid draft of the test image) and, tested last since it's a one-way latch, `mount_lock` correctly refusing every further mount attempt. Bridge: full suite 347 passed, 2 skipped (up from 342 before this item). Also live-verified through the real MCP dispatch path (`server.request_handlers[types.CallToolRequest]`): all five tools round-trip correctly, including `drive_swap` succeeding and `mount_lock` then correctly blocking a subsequent `drive_swap` with `guard()` surfacing the refusal as `isError`.

#### 2.12 Replay lifecycle: status and cancel
**Engine + bridge. Effort: M. Status: done.**

`POST /input/sequence` returns `{status:"ok", events_scheduled:N}` the moment the chain is armed. The engine tracks exactly what an agent needs (`pending_dispatched`/`pending_total`, `frame_pending_*`, plus drift) and only ever logs it. There is no cancel, so a 90-second replay armed by mistake runs to completion and every further POST gets 409.

`GET /api/v1/input/replay/status` returning `{active, engine, total, dispatched, remaining, elapsed_ms, drift_ms, current_frame}`. The PIC path has **no active flag**: completion is implied by the queue being empty while the counters keep their last values, so derive `active` from the queue under `pending_mutex` and report a finished run as `{active:false, dispatched:N, total:N}` rather than as a live replay. `ReplayDispatchFrame` reads `frame_replay_active` outside its mutex; a status route must take the lock.

`DELETE /api/v1/input/replay` drains the queues, calls `PIC_RemoveEvents` (must run on the emulation thread, so a Bridge Command; no deadlock risk, `pic_input_handler` already takes `pending_mutex` on that thread), and clears the titlebar and OSD flags.

Add a stall watchdog so a wedged chain self-heals: with unbounded timing validated away by 1.4, the remaining wedge cases are slow-but-legal ones.

Rewrite the `input_sequence` description to say the call returns immediately and point at the status tool.

**Depends on:** 1.4 (timing validation), 1.6 (recording handlers onto the Bridge, same file).

**Shipped.** Every piece of the plan text landed on both sides.

- **Engine (`src/webserver/input.h`/`.cpp`, plus `webserver.cpp` route registration and `capabilities.cpp`)**: new `ReplayStatus` struct and `InputReplay::GetStatus()` back `GET /api/v1/input/replay/status`, implemented as a plain (non-Bridge) web-thread handler - the same established precedent as `ControlHandlers::GetStatus` and this file's own `RecordingHandlers::GetStatus`, since a Bridge Command would block/time out in exactly the stalled case the route exists to report. Each engine gained frozen `elapsed_ms`/`drift_ms` fields (updated at dispatch, natural completion, stall-abort, and cancellation) so a finished/aborted/cancelled chain keeps reporting real numbers instead of zeroing out, and a small `last_engine` tracker (its own mutex, `mark_last_engine()` called from every transition-to-inactive point on both engines) attributes a finished run's numbers to the correct engine even when the other engine started first but is no longer relevant. New `ReplayCancelCommand` (`DELETE /api/v1/input/replay`, a Bridge Command since it calls `PIC_RemoveEvents`) drains both chains unconditionally, freezing `elapsed_ms` at the actual cancellation moment rather than the last dispatch's stale timestamp. New stall watchdog: both engines track how long their *current* front event specifically has been stuck on keyboard backpressure (the only way either engine can wedge - mouse events never check buffer space) via `std::optional<time_point>` timers, self-aborting past `ReplayStallThresholdMs` (5000ms, surfaced as `capabilities.input.limits.replay_stall_threshold_ms`) of no progress on that one event - deliberately not a generic "time since last dispatch" timer, since a legitimate sequence can have long intentional gaps between events. `input_sequence`'s route description and `resources/webserver/openapi.json` (new 409 doc, two new paths, `ReplayStatus` schema) were updated per the plan's explicit instruction.
- **Bridge (`dosbox_mcp/tools/input.py`)**: new `replay_status` (read-only) and `replay_cancel` tools, group `input` (already in `_INTERACT_GROUPS`, so `replay_cancel` is reachable in interact mode alongside `input_key`/`input_sequence`, its natural trust level). `input_sequence`'s description rewritten to say the call returns immediately and point at both new tools, per the plan.
- **Adversarial review (3 dimensions - concurrency/lock-ordering, stall-watchdog functional correctness, bridge/docs conformance - each finding independently re-verified by 2 skeptical agents): 5 raw findings, all 5 confirmed, 0 rejected.** All fixed:
  - **(high) The frame engine's stall timer never reset on partial progress within a call, only on a fully-clean frame.** A chain throttled to roughly one dispatch per frame by keyboard backpressure - genuine, continuous progress - ended every single frame call "stuck" on the next due event, so the timer accumulated across ~300 frames of real progress and self-aborted a chain that was never actually wedged. Fixed by resetting the timer whenever `dispatched_this_frame > 0`, independent of whether the call also ends stuck - mirrors the PIC engine's already-correct per-dispatch reset, which doesn't have this bug because it only ever dispatches one event per invocation by construction.
  - **(medium) `InputReplay::GetStatus()` snapshotted the two engines under two separate, sequentially-released locks, then combined the results without holding either** - a chain could arm or finish in the gap between the two critical sections, producing a combined report (e.g. `active:false` with a stale engine attribution) that was never true at any single instant. Fixed by holding both `pending_mutex` and `frame_replay_mutex` together for the entire snapshot; safe against deadlock because every mutator in the file only ever locks one of the two, never both, so nothing can be waiting on the other while `GetStatus()` holds them.
  - **(medium) PROTOCOL.md's semantics bullet claimed `elapsed_ms` is measured from when the chain actually starts dispatching, not from the POST call - backwards from what the engine does** (`replay_start_wall` is captured synchronously at arm time in both `ExecutePicBased`/`ExecuteFrameBased`, before the first event's own delay elapses, so `elapsed_ms` includes any lead-in wait). Fixed the wording; `resources/webserver/openapi.json`'s vaguer description was already accurate and didn't need a change.
  - **(low) `ExecutePicBased`/`ExecuteFrameBased` reset the dispatch counters when arming a new chain but never reset `drift_ms`,** so a status read landing in the arm-to-first-dispatch window could report a stale drift figure left over from a completely different, already-finished chain. Fixed by zeroing `drift_ms` alongside the dispatch counters.
  - **(low) The "mixed" engine branch of `GetStatus()` always reported the PIC chain's `drift_ms`, silently discarding the frame chain's,** unlike `elapsed_ms` in the same branch which correctly combines both via `std::max()`. Fixed to report whichever chain's drift has the larger absolute value.
- One structural note surfaced during live testing, not an adversarial-review finding: `ExecuteFrameBased`'s pre-existing (not touched by this item) event-normalization loop rebases against the first event whose `frame`/`t` is nonzero, so a hand-constructed sequence whose first event explicitly sets `frame:0` is misread as "no real input yet" and gets rebased against a later event instead - a genuine frame-based sequence has to either omit the first event's frame/t or use a nonzero value. Not fixed here: pre-existing behavior this item's diff never touches, and out of this item's scope.

**Verification:** both engine build configurations (`build`, `build-debugger`) compile clean, before and after the review-fix pass. Full C++ suite unchanged at 1175 passed / 20 pre-existing `MountPolicyTest` failures (no new gtest coverage added: every new code path is concurrency/emulation-lifecycle-driven state with no pure function to extract, matching this file's existing all-integration-tested pattern). `tests/integration/test_api_contract.py` gained six tests (idle status shape, PIC-engine mid-flight/post-completion/frozen-not-climbing, frame-engine mid-flight, cancel draining a running chain and freeing the engine for an immediate new POST, cancel-when-idle); `tests/integration/dosbox_client.py` gained `replay_status`/`replay_cancel`/a `_delete` helper. The local dev environment's SDL `offscreen` video driver doesn't initialize here (same pre-existing, unrelated environmental gap noted in 2.15), so these weren't run through the actual pytest e2e harness; every behavior was instead live-verified directly against a real `build-debugger` binary over HTTP, twice (once before the review-fix pass, once after): idle status, a PIC-timed chain's live mid-flight numbers and frozen post-completion numbers, a frame-timed chain's mid-flight status, cancelling a running PIC chain and a running frame chain (confirming the queue was genuinely drained, not just marked inactive, by successfully posting a fresh sequence immediately after), the 409-conflict-then-cancel round trip, the `mixed` engine state (two independent chains armed at once, confirmed correctly combined `total`/`dispatched` and the post-review-fix `drift_ms` tie-break) - and, after the review-fix pass specifically, that a 60-event burst dispatches cleanly without falsely tripping the (now progress-aware) stall watchdog. The stall-abort path itself (waiting out a genuine 5-second keyboard-buffer wedge) was not live-reproduced - live-triggering it black-box would need flooding the actively-input-draining DOS shell with enough held keys to risk corrupting other tests' state, judged impractical and not attempted, same reasoning as 2.11's mid-scan-race decision - so that path relied on the adversarial review's direct-reading verification instead. Bridge: full suite grew from 350 to 352 passed, 2 skipped (`tests/test_input_tools.py` new, plus `replay_status`/`replay_cancel` registration-presence assertions added to `tests/test_server.py`). Also live-verified through the real MCP dispatch path (`server.request_handlers[types.CallToolRequest]`): `input_sequence`/`replay_status`/`replay_cancel` round-trip correctly including the 409-conflict case surfacing as `isError` with the engine's message intact.

#### 2.13 Named recording store
**Engine + bridge. Effort: L. Status: done.**

All four `input/record/*` routes have zero MCP tools, and the shape is wrong for an agent anyway: `PostStop` returns the entire event vector as JSON, `rec_buffer` has no cap, and every host mouse motion appends an event. A two-minute mouse-driven install is tens of thousands of JSON objects the agent must pull into context and post straight back.

**Do this first, before any cap:** coalesce consecutive `mouse_move` events landing in the same rendered frame at record time (`OnMouseMove` currently appends every host motion). Host mice run at 125 to 1000 Hz against a ~70 Hz frame clock; summing `x_rel`/`y_rel` within a frame and keeping the last `x_abs`/`y_abs` cuts volume roughly an order of magnitude and loses nothing the replay engine can act on. That is what turns 30k events into ~3k.

Then an **in-memory** named store: `POST /input/record/stop?name=<slug>` moves the vector into a process-lifetime map, `?include_events=false` suppresses the body, `GET /input/recordings` returns metadata only, `DELETE /input/recordings/:name`. Extend `InputSequenceCommand::Post` to accept `{"recording":"<name>"}`, resolved on the web thread into an already-validated `std::vector<InputEvent>` before the Command is constructed. Zero filesystem surface. Validate `name` with the rule from `Lua::ScriptValidator::ValidateParams` (<=64 chars, `[A-Za-z0-9_-]`).

Cap `rec_buffer` at 32000 (matching the replay cap) **and set a `truncated` flag** surfaced by both `record/status` and the stop response, or an agent replays half an install and cannot tell.

**Optional, separate commit:** JSON persistence under `<config>/recordings/`. If you do it, a file on disk is untrusted input on the way back in: re-parse through the same per-type field whitelist `/input/sequence` uses, never memcpy into `InputEvent`. Canonicalize and reject symlinked directories, same discipline as `MountPolicy`.

**Shipped.** The in-memory store, coalescing, and cap all landed; filesystem persistence was correctly left undone - the plan itself marks it optional and separately scoped.

- **Engine (`src/webserver/input.h`/`.cpp`, plus `webserver.cpp` route registration, `capabilities.cpp` for new limits, `resources/webserver/openapi.json`, and new gtest coverage in `tests/webserver_input_validation_tests.cpp`)**: `InputRecording::OnMouseMove` now coalesces consecutive samples landing in the same rendered frame into the buffer's last entry (summed `x_rel`/`y_rel`, latest `x_abs`/`y_abs`/`t_ms`) - checked against literally `rec_buffer.back()`, so an intervening key/button/wheel event correctly breaks the coalesce run rather than letting a later mouse sample merge backward past it and reorder the recording. A new `RecordingHasRoom()` (file-local, `rec_mutex` already held) caps `rec_buffer` at the existing `MaxInputEvents` (32000, reused rather than duplicated - a recording that couldn't fit one `/input/sequence` body couldn't be replayed by it either) and latches a new `rec_truncated` flag, surfaced via `InputRecording::IsTruncated()` in both `GET /input/record/status` and the `POST /input/record/stop` response; the coalesce path is deliberately exempt from the cap since merging into an existing entry never grows the buffer. New `RecordingStore` namespace (`IsValidName` - `<=64` chars, `[A-Za-z0-9_-]`, the same rule `Lua::ScriptValidator::ValidateParams` uses for script names - `HasRoom`, `Save`, `Get` (returns a copy, never a move, so a name stays replayable more than once), `List`, `Delete`): a process-lifetime, in-memory, web-thread-only map, never touched by the emulation thread. `RecordingHandlers::PostStop` validates an optional `?name=` query param and checks store capacity *before* ever stopping the live recording, so an invalid name or a full store (400 / 503 `registry_full`) never costs the caller their in-progress capture - it's left running, retryable. `?include_events=false` omits the `events` array from the response. `InputSequenceCommand::Post` gained a `recording` body field as an alternative to `events` (404 if unknown, 400 if both given), resolved into an already-validated event vector on the web thread before any Command is constructed; a stored recording always replays via the frame-timed engine, since every recorded event already carries a `frame`. New `GET /api/v1/input/recordings` (metadata only) and `DELETE /api/v1/input/recordings/:name` routes.
- **Bridge (`dosbox_mcp/tools/input.py`)**: six new tools - `record_start`/`record_pause`/`record_stop`/`record_status` (the plan's headline observation was that these four pre-existing routes had zero MCP tools; they do now) plus the two new store routes `recordings_list`/`recording_delete`. `record_stop` builds its `name`/`include_events` query string by hand via `urllib.parse.urlencode`, since the bridge's `Connection.post()` wrapper has no `params` kwarg for POST (a first for this bridge - no other tool needed query params on a mutating call before). `input_sequence`'s schema and handler extended with a `recording` property, forwarded verbatim to the engine rather than validated bridge-side, consistent with this bridge's existing "the engine is the trust boundary" posture.
- **Adversarial review (3 dimensions - concurrency/capacity, correctness/security, bridge/docs conformance - each finding independently re-verified by 2 skeptical agents): 4 raw findings, all 4 confirmed, 0 rejected.** All fixed:
  - **(medium) `InputSequenceCommand::Post`'s `recording` body field had no `IsValidName` check before being used as a `RecordingStore` lookup key and echoed into the 404 body - unlike the equivalent `name` query param in `PostStop`, which validates first.** A request with a multi-megabyte garbage `recording` string would clear the 10 MB body cap, get hashed as a full map-key lookup, and on the inevitable miss get echoed whole back into the response: an avoidable reflection/resource-waste path with no cheap rejection, directly contradicting the sibling code path added in the same diff. Fixed by adding the identical `IsValidName` check before ever touching `RecordingStore`, matching `PostStop`'s pattern exactly.
  - **(low) `openapi.json`'s `include_events` query parameter was documented with `enum: ["false"]`, but the engine actually treats every non-`"false"` value (including `"true"`) the same as omitting it entirely** - a strict client generated from the spec would wrongly reject a legitimate `include_events=true` call. Fixed by dropping the enum and describing the actual only-`"false"`-is-special behavior.
  - **(low) The `/input/record/stop` and `/input/record/status` 200 response examples were missing the new `truncated`/`name` fields that their own referenced schemas (updated in this same diff) already declared.** Fixed by adding them to both examples.
  - **(low) `record_stop`'s tool description pointed to `dosbox_status/capabilities.input.limits.max_stored_recordings`, omitting that `dosbox_status`'s combined JSON nests the engine's `/dosbox/info` response under an `info` key** (`session.py`'s `_status()` composes `{status, program, info}`), so the real path is `dosbox_status.info.capabilities...`. Fixed the pointer; noted but did not touch `debug.py`'s pre-existing identical shorthand (`"capabilities.debugger in dosbox_status"`), predating this diff and out of its scope.
- One TOCTOU question raised and resolved without a code change: `RecordingStore::HasRoom()` and `Save()` are two separate, non-atomic calls, so two concurrent named `stop` requests could in principle both pass a capacity check before either saves. Traced against the actual `Bridge`/`StopRecordingCommand` machinery: only one live recording ever exists process-wide, Bridge Commands execute strictly serially on the emulation thread, and `PostStop` only reaches `RecordingStore::Save` after its own `StopRecordingCommand` reports `was_recording:true` - so of any two racing stop requests, only the one whose Command actually executes first can ever succeed past that point; the second finds `rec_active` already false and returns 409 before reaching `Save`. The adversarial review's concurrency dimension explicitly targeted this and did not surface a way to defeat it, so no fix was needed here.

**Verification:** both engine build configurations (`build`, `build-debugger`) compile clean, before and after the review-fix pass. Full C++ suite grew from 1175 (before this item) to 1191 (16 new tests in `tests/webserver_input_validation_tests.cpp`: `RecordingStoreNameValidation`'s exhaustive validation-rule coverage, and a `RecordingStoreTest` fixture exercising `Save`/`Get`/`List`/`Delete`/`HasRoom` directly against synthetic `InputEvent` structs - no engine dependency, so genuinely unit-testable unlike the mouse-move coalescing itself), same 20 pre-existing `MountPolicyTest` failures, zero new failures, unchanged by the review-fix pass. `tests/integration/test_api_contract.py` gained eight tests (named save/list/replay-twice/delete round trip, replay of an unknown name, delete of an unknown name, events+recording conflict, an invalid name leaving the live recording running, `include_events=false`, and filling the store to capacity including the overwrite-at-capacity case) - collection-checked (85 tests collect cleanly) but not run through the actual pytest e2e harness, same pre-existing local SDL-offscreen-driver gap noted in 2.12/2.15. Two code paths could not be live-exercised at all, by design: genuine mouse-move coalescing and the 32000-event truncation cap both require real "host" input, which the REST API cannot synthesize (input injected via `POST /input/sequence` is deliberately excluded from recording by the `in_replay_dispatch` guard, so a replay can't re-record itself) - both were verified via direct reading, the new gtest coverage of `RecordingStore`'s pure logic, and the adversarial review's dedicated scrutiny of exactly these two paths. Everything else was live-verified directly against a real `build-debugger` binary over HTTP, twice (once before the review-fix pass, once after): the full named-recording round trip, invalid-name-leaves-recording-running, the capacity limit and its overwrite-at-capacity exception, `include_events` true/false, and (after the fix) that an oversized or invalid-charset `recording` body value now gets a clean 400 instead of a 404 with the raw value echoed back, with a valid-but-unknown name still correctly reaching 404. Bridge: full suite grew from 352 to 364 passed, 2 skipped (13 new tests in `tests/test_input_tools.py`, plus registration-presence assertions across all three capability-mode tests in `tests/test_server.py`). Also live-verified through the real MCP dispatch path (`server.request_handlers[types.CallToolRequest]`): the full `record_start`/`record_status`/`record_stop`/`recordings_list`/`input_sequence`/`recording_delete` round trip, including the 404/400/409 error cases surfacing correctly as `isError` with the engine's exact message, and the invalid-name-doesn't-stop-the-recording behavior confirmed via a follow-up `record_status` call.

#### 2.14 DOS mouse position
**Engine + bridge. Effort: M.**

Mouse-driven installers and GUI-era DOS software are not drivable. `x_abs`/`y_abs` are whitelisted, parsed, recorded from genuine host coordinates and round-tripped through `record/stop`, and `dispatch_input_event` calls only `MOUSE_InjectMoved(x_rel, y_rel)`. The bridge has capitulated: `input.py:66-76` tells the agent there is no absolute positioning and teaches a corner-sweep hack, and `x_abs`/`y_abs` are not even in its schema.

**The obvious fix does not work.** Adding `MOUSE_InjectAbsolute` that calls `interface.NotifyMoved(0,0,x_abs,y_abs)` reaches `MOUSEDOS_NotifyMoved`, which branches on the static `use_relative`. With the mouse captured (the normal case, and the only case for a windowless run) that is true, so `move_cursor_captured` consumes `pending.x_rel/y_rel` and the absolute coordinates are ignored. Absolute coordinates only reach `move_cursor_seamless`, where they are host-resolution-normalized, not guest pixels.

Also note the drift story is not what it looks like: `MOUSE_EventMoved` and `MOUSE_InjectMoved` both scale for the interfaces and hook the *unscaled* values, so record and replay are symmetric. The drift comes from the DOS driver's own mickey accumulation and acceleration.

Build the closed loop instead. `MOUSEDOS_GetPosition(x,y,started)` and `MOUSEDOS_SetPosition(x,y)` over the driver's plain state (`get_pos_x`/`get_pos_y`, `state.SetPosX`/`SetPosY`). `GET /api/v1/input/mouse` returns `{driver_started, x, y, buttons}`. `POST /api/v1/input/mouse {x,y}` is a Bridge Command that clamps to `state.GetMinPos*/GetMaxPos*` (do not skip this: `limit_coordinates()` exists for a reason), sets the position, and triggers the move event so the guest callback fires and the cursor redraws. Do not chase "inject the exact relative delta": `move_cursor_captured` runs deltas through pixels-per-mickey plus a move threshold, so a naive delta will not land.

**Document the limit honestly:** this is the INT 33h driver's position. Guests talking to the PS/2 or serial mouse directly (Windows 3.x, some protected-mode games) have no readable position; report `driver_started:false` rather than lying. For those, relative injection remains the only path.

Cheap follow-on: honour `x_abs` in `dispatch_input_event` when present, tracked by a `has_abs` bool set during parsing (a zero test is wrong, (0,0) is a legal position). Add `x_abs`/`y_abs` to the bridge schema and delete the corner-sweep paragraph in the same change.

#### 2.15 Capture status: path, frames, mode, compression
**Engine + bridge. Effort: S. Status: done.**

`capture/video/status` returns `{capturing, mode, last_stop_reason}`. An automation run that records a seven-disk install produces an AVI the agent cannot name, size-check, or confirm got frames. The data exists: `video.frames` and `video.written` are live in the capture struct and the resolved path is computed inside `CAPTURE_CreateFile` and only logged.

Retain the path (add an out-param to `CAPTURE_CreateFile` or resolve it in `capture_video.cpp`; do **not** change its signature, it has eight callers across audio, video, midi, image, serial and OPL). Add accessors and extend the status command to `{capturing, mode, path, frames, elapsed_ms, bytes_written, compression_level, last_stop_reason}`. Retain path, frames and reason for one capture past the stop, since stop-then-ask is the normal sequence. `elapsed_ms` needs a start timestamp measured from **file creation**, not the API call: `CAPTURE_StartVideoCapture` only sets state to Pending and the file is not created until the first frame arrives.

Bridge: **one** tool change, not two. Fold `compression` (0..9) and `mode` (raw|rendered) into `video_capture_start` as optional arguments, set then start, atomically from the agent's point of view. Separate compression tools would be near-useless: the level is latched at `deflateInit2` and PUT is refused while recording.

**Why the agent is better off:** `frames` is the highest-value field. It is the only way to detect that a capture was nominally running while the emulator was paused or minimized and nothing was written.

**Disclosure note:** the path is a host filesystem path, same decision as 2.11.

**Shipped.** Every piece of the plan text landed on both sides.

- **Engine (`src/capture/capture.h`/`.cpp`, `src/capture/capture_video.cpp`/`private/capture_video.h`)**: `CAPTURE_CreateFile`'s signature is untouched, as required (its eight callers are unaffected) - instead, `capture.cpp` now remembers the path it just resolved (autogenerated or caller-supplied) in a new `last_created_path` field, queryable via `CAPTURE_GetLastCreatedFilePath()`, and `create_avi_file()` in `capture_video.cpp` reads it back immediately after its own `CAPTURE_CreateFile` call succeeds. New `video` struct fields `path`, `start_ticks_ms`, `end_ticks_ms`: `start_ticks_ms` is stamped with `GetTicks()` (`hardware/timer.h`) at the same point `frames`/`written` already reset to 0 for a new recording - i.e. at file-creation time, not when `CAPTURE_StartVideoCapture` merely arms the `Pending` state - and `end_ticks_ms` is stamped once in `capture_video_finalise()`, freezing the reported duration rather than letting it climb after the capture stops. New accessors `capture_video_get_path/frame_count/bytes_written/elapsed_ms()` (`private/capture_video.h`) back four new public `CAPTURE_GetVideo*()` functions in `capture.h`, forwarded to from `webserver/capture.cpp`'s `CaptureStatusCommand`. `frames`/`bytes_written` reuse the pre-existing `video.frames`/`video.written` counters directly, exactly as the plan text pointed out they already existed. `compression_level` in the status response is the level currently configured for the reported `mode` (`CAPTURE_GetVideoCompressionLevel`) rather than a separately-latched per-capture value - accurate for a running capture (the level can't change mid-recording, enforced below) and a reasonable best-effort figure for a finished one. `CaptureStartCommand` gained `compression_level` (-1 = leave unchanged) and folds `PUT`-equivalent compression-setting into the same Bridge Command as the start, atomically: `Execute()` applies `CAPTURE_SetVideoCompressionLevel(mode, compression_level)` before `CAPTURE_StartVideoCapture(mode)` when a level was given, and refuses (`rejected_recording_active`, mapped to 409 in `Post()`) rather than silently dropping the request if a capture is already running - mirroring `CaptureCompressionSetCommand`'s existing rejection rule, since the level is latched at `deflateInit2` either way. No second bridge-facing compression tool was added, per the plan's explicit instruction. `resources/webserver/openapi.json`'s `/capture/video/start` request/response schemas and `/capture/video/status`'s response schema and example were extended to match, plus the new 409.
- **Bridge (`dosbox_mcp/tools/media.py`)**: `video_capture_start`'s schema grew optional `mode` and `compression` properties (previously it had no properties at all - `mode` existed engine-side but was never reachable from the bridge either), both forwarded into the POST body only when actually given, preserving the exact no-body request for the common case. `video_capture_status`'s description documents all five new response fields and the retained-past-stop/frozen-elapsed-time semantics up front, since those are exactly the details an agent would otherwise have to discover by trial and error.
- No adversarial-review Workflow run for this item: contained blast radius relative to 2.11/2.17 (no new trust-boundary crossing, no new filesystem traversal surface, no new bridge routes - only new response fields plus one already-validated optional request field folded into an existing route), effort S. Self-reviewed instead for the usual risk points - thread-safety of the new `video`-struct fields (all reads and writes happen on the emulation thread, either from `Execute()` or from the existing frame-render call path, same as `frames`/`written` already were), the compression bounds check short-circuiting correctly before comparing a possibly-non-numeric JSON value, and that a plain double-start with no `compression` field keeps its exact pre-existing lenient behavior (silently a no-op on the already-running capture) - only an explicit `compression` request on top of an already-running capture is refused.

**Verification:** both engine build configurations (`build`, `build-debugger`) compile clean. Full C++ suite unchanged at 1175 (no new gtest coverage: every new accessor depends on live capture-lifecycle state - real file creation, ZMBV codec setup - that this module has never had isolated unit tests for; it's exercised by the Python integration suite instead, matching its existing all-integration-tested pattern). `tests/integration/test_capture_api.py` gained four tests covering the idle-state field defaults, path/frames/elapsed_ms/bytes_written after a run and their being frozen past a stop, the atomic mode+compression start, and the 409 conflict; `tests/integration/dosbox_client.py`'s `capture_start` gained optional `mode`/`compression` kwargs. The local dev environment's SDL `offscreen` video driver does not initialize here (pre-existing, unrelated to this item - the `api` category also fails to collect locally over a missing `PIL` import), so these new tests were not run through the actual pytest e2e harness; instead, every behavior was live-verified directly against a real `build-debugger` binary over HTTP: idle status defaults, `frames`/`bytes_written`/`elapsed_ms`/`path` all populating correctly during a real headless recording, all four fields staying byte-for-byte frozen across a stop and a further 1-second wait, atomic `mode`+`compression` start reflected in both the status response and the separate `GET /capture/video/compression` route, the 409 refusal when `compression` is requested against an already-running capture (with the running capture's own state left completely undisturbed), and 400 rejection of both an out-of-range and a non-integer `compression` value with no accidental state change. Bridge: full suite grew from 347 to 350 passed, 2 skipped (three new tests for `_capture_start`'s body-forwarding behavior in `tests/test_media_tools.py`).

### 2D. Reverse engineering support

#### 2.16 Rebuild the Ghidra mapping
**Bridge. Effort: M. Status: done.**

`_to_live` computes `offset = ghidra_address - delta` and `linear = base_segment*16 + offset` with no check that the offset lands in 0..0xFFFF or is non-negative, and hands the result straight to `debug_breakpoint_add`, where the engine validates the segment but takes `offset` as a raw `uint32_t` with no range check. `_set_base` validates nothing. One live segment by construction. Lost on every restart. And all four tools register with `feature="debugger"` though the module header states the arithmetic never touches the engine, so on a stock build the agent carries four permanently-refusing tools, and in observe mode the three read-only ones survive to say "No mapping set" forever.

**Ship first, standalone:** validation in both directions (validate the **sum**, not just the parts) and dropping the feature gate. Removing the gate also needs a group that survives observe mode, or `set_base` is dropped again. Add the missing engine-side offset bound too; the bridge is not the only client.

Then: a list of ranges `[{ghidra_start, ghidra_end, live_segment, delta, label}]` resolved by containment, so multi-segment EXEs work and an uncovered address yields "no range covers this" rather than a wrong number.

Then `debug_map_auto`: take a byte signature from Ghidra, find it with 2.9(b), derive segment and delta with no manual pausing. A `.COM` variant can derive the load segment from the MCB chain (`pspSegment` + 0x10), handling "not found in the chain" explicitly since `WalkMcbChain` caps at 1000 blocks and truncates silently.

**Persistence, corrected:** persist the **delta and label only** and re-derive the live segment on attach. `delta` is stable across runs for a `.COM`; `base_segment` depends on what DOS has resident, so persisting it silently produces wrong answers on the next boot, which is the failure mode this item exists to eliminate.

**Depends on:** 2.9(b) for auto-anchoring.

**Shipped.** Every piece of the plan text landed - multi-range containment model, `debug_map_auto`, corrected persistence, dropped feature gate, a mode-gate mechanism for `set_base`, and the missing engine-side bound - but the first adversarial pass found the engine-side bound only half-closed the bug it was meant to close, and the mode-gate mechanism itself quietly broke the exact invariant it existed to protect. Both fixed below.

- **Engine (`src/webserver/debug.cpp`): `SegOffsetWithinMemory(seg, off)`**, 64-bit-widened (seg<<4)+off checked against `MEM_TotalPages()*MemPageSize`, mirroring `DisassembleCommand::Execute`'s existing pattern. Called from `DebugAddBreakpointCommand::Execute` (execute/memory types) and `DebugRunToCommand::Execute`; both `Post()` handlers now check `cmd.error` and throw. **Review found the fix originally only checked half of what an untrusted request can carry**: a breakpoint's memory `condition.segment`/`condition.offset` - parsed by the same `RequireU16`/`RequireU32` as the breakpoint's own location, evaluated through the identical `GetAddress()` call at fire time (`CBreakpoint::EvaluateCondition`) - was left completely unchecked, reachable via `{"type":"execute","segment":0,"offset":0,"condition":{"segment":65535,"offset":4294967295,...}}`: the outer address passes, the condition's wrapped address silently doesn't. Fixed by checking `condition.segment`/`condition.offset` too whenever `condition.kind == Memory`, in the same `Execute()`.
- **Review also found the check was wrong, not just incomplete, for protected-mode debugging**: `GetAddress()` only takes the flat `(seg<<4)+off` path the new check validates when the CPU isn't in protected mode; a live GDT selector resolves through `PhysMakeProt` (`desc.GetBase()+offset`) instead, a different formula the check knew nothing about - meaning it could both wrongly reject a legitimate protected-mode breakpoint and miss overflow in the GDT path entirely. Properly bounding the GDT/CS-relative paths too would mean replicating `GetAddress`'s full runtime resolution (including live descriptor-cache state) in the validator, well beyond a bound-check hardening item's scope. Fixed by gating the check on `!cpu.pmode` at both call sites: applies exactly where it's correct (flat real-mode, the case `debug_map_auto`/`debug_map_set_base` ever produce), leaves the pre-existing unchecked protected-mode behavior as-is rather than half-fixing it. Documented, including the residual unreal-mode edge this doesn't chase (a CS-relative breakpoint under an "unreal mode" trick can have a live base that isn't `SegValue(cs)<<4`) - same class of disclosed, real-mode-only scope as `disassemble.cpp`'s own precedent.
- **Bridge (`dosbox_mcp/tools/ghidra.py`): rewritten from a single anchor to a list of named ranges** - `{label, ghidra_start, ghidra_end, delta, live_segment}`, resolved by containment (`_find_covering_range`/`_to_ghidra`). `debug_map_set_base` and the new `debug_map_auto` both upsert by label (re-anchoring replaces, a new label adds a range); validation checks the anchor lies inside its own claimed span and both live_segment/live_offset are 0x0000..0xFFFF (schema-level `minimum`/`maximum` added too, confirmed by review to actually be enforced pre-dispatch by the MCP SDK - a genuine fast-backstop layer, not just documentation).
- **`debug_map_auto`**: `mem_scan` (2.9b) for a unique signature match, `dos_memory_map`'s MCB chain to derive a PSP segment, `pspSegment + 0x10` as the .COM code-segment convention (chosen so a `.COM`'s own live offset equals its Ghidra byte offset, per the module's existing anchor-offset note). Refuses outright - never guesses - on zero matches, ambiguous (>1) matches, a match outside every walked MCB block, or a derived segment/offset that doesn't fit 0x0000..0xFFFF. **Review (high severity) found the block-selection step trusted `pspSegment` alone**: `DOS_Execute` tags a program's *environment* block with the same `pspSegment` as its own code block, and a freed block's stale owner tag is never cleared, so a signature match landing in either produced a confident, silently wrong mapping instead of a refusal - exactly the failure mode this whole item exists to eliminate. Fixed by additionally requiring the matched block be *self-owned* (`block.segment + 1 == block.pspSegment`, the actual invariant of a .COM's own PSP+code block), which rejects both the sibling-block and the freed-block case for free (a freed block's `pspSegment` is `MCB_FREE = 0`, which `segment+1` can never equal).
- **Persistence corrected as specified**: only `{label, ghidra_start, ghidra_end, delta}` ever reach disk (`default_ghidra_map_path()`, new in `dosbox_mcp/config.py`); a range loaded from a previous session starts with `live_segment: None` ("no live segment yet this session") until re-anchored, rather than trusting a stale segment. Atomic write (`tempfile` + `os.replace`, mirroring `config.py`'s own `_atomic_write`). **Review found a malformed persisted entry discarded the whole list**, not just itself (`_load_ranges`'s single try/except wrapped the entire parse loop); fixed to catch per-entry and skip only the bad one, plus reject a duplicate label within the file.
- **No overlap detection was the plan's own stated purpose being silently undermined**: nothing stopped two ranges' `[ghidra_start, ghidra_end)` spans from overlapping, and `_find_covering_range` picked whichever came first in insertion order with no warning - "a wrong number instead of an explicit refusal," precisely what the multi-range model exists to prevent. Fixed with `_check_no_overlap`, checked at both `set_base` and `auto` before any engine call.
- **The feature gate and mode-gate fixes, and the flaw review found in the second one.** `feature="debugger"` dropped for the four pure-arithmetic tools (`feature=None` - they never call the engine) and `feature="memory"` kept only for `debug_map_auto` (it calls `mem_scan`/`dos_memory_map`, both memory-gated). `needs_connection` was also still defaulting to `True` for every tool - review caught that the three read-only translation tools and `set_base` would refuse with "not connected" even though they touch nothing but local state; now `False` for all four, `True` (unchanged, correct) for `debug_map_auto`. For the mode gate: a new `_LOCAL_ONLY_GROUPS` set in `server.py` bypasses mode gating entirely (even observe) for any group placed in it, since the existing `_mode_allows` had no way to let a genuinely engine-free mutation like `set_base` survive observe mode at all. **Review found `debug_map_auto` was placed in that same bypassed group** (`group="mapping"`), directly contradicting both the group's own comment ("their mutation is confined to the bridge process's own local bookkeeping... and never reaches the connected engine or guest") and the plan text above, which names only `set_base` as needing this exemption - `debug_map_auto` genuinely reads live engine memory (up to 640 KB via `mem_scan`) and the full MCB chain as part of what it derives. Fixed by splitting registration: `register()` (the four local-only tools, group `"mapping"`) and a new `register_auto()` (`debug_map_auto`, group `"mapping_auto"`, not in `_LOCAL_ONLY_GROUPS` - so it now correctly requires full mode, same as any other engine-reaching mutation), sharing one `state` dict returned by `register()` and passed into `register_auto()`.
- **One low-severity finding accepted without a structural fix**: `_LOCAL_ONLY_GROUPS`'s bypass is checked before a tool's own `read_only` flag, so the safety property still rests on a human correctly choosing not to add an engine-touching tool to a local-only group, not on a code-level guarantee. Documented explicitly in the set's own comment (now naming the invariant and pointing at `register_auto` as the counter-example of getting it right) rather than building enforcement machinery for a two-tool module.
- **`debug_map_auto`'s own tool description undercounted its refusal paths** (3 of 5 documented) - fixed to describe all five, and to note it needs full mode.
- **`PROTOCOL.md` gained a `1.5.0 (draft)` changelog entry and a semantics bullet** for `debug/breakpoints`' new bound-check behavior (including the protected-mode carve-out) - the existing "a client talking HTTP directly must be subject to exactly the same limits as one going through a bridge" principle applies here as much as it did to the 1.2.0/1.3.0/1.4.0 memory-route entries, per two independent reviewers who pushed back on an initial decision not to document it. `debug/run_to`'s identical fix is **not** documented there, since `debug/run_to` was never part of the 1.1 route table to begin with - a separate, pre-existing gap, not created or worsened by this item. `resources/webserver/openapi.json` does not document `debug/breakpoints` or `debug/run_to` at all (neither ever has); adding that from scratch is a materially larger, pre-existing documentation gap judged out of scope here.

**Verification:** engine build clean (both `build` and `build-debugger` configurations) with zero new warnings. No new ctest coverage for the engine-side bound check - no existing doctest harness in this codebase initializes enough emulator state to exercise `MEM_TotalPages()`-dependent code (confirmed: neither `webserver_mem_scan_tests.cpp` nor `webserver_memory_snapshot_tests.cpp` from earlier 2.9 work actually call into live memory state either), consistent with every other 2.9/2.16 fix that touches `Execute()`. Live-verified against a real `build-debugger` binary instead: valid breakpoint/run_to succeed; an overflowing top-level `segment:offset` 400s; an overflowing *condition* `segment:offset` 400s (previously silently accepted); a valid memory condition still succeeds; `run_to` while not paused still reports `not_paused` (200) unchanged. Bridge: full suite 283 passed, 2 skipped (up from 273 before the review-fix pass, 248 before this item), all new tests in a dedicated `tests/test_ghidra_tools.py` plus registration/mode/connection tests in `test_server.py` (including one exercising the real `guard()`/`add_tool` dispatch path per-tool, specifically to catch the class of bug (`needs_connection` defaulting wrong) that a handler-level-only test suite had already missed once). Also live-verified through the real MCP dispatch path (`server.request_handlers[types.CallToolRequest]`) against both a `build-debugger` and a stock (non-debugger) binary: all five tools work on the stock build (the original bug), `debug_map_auto` correctly derives a live segment from a real, self-owned MCB block and round-trips through `debug_map_to_live`/`debug_map_to_ghidra`, and an ambiguous-signature scan correctly refuses.

Adversarial review (3 dimensions - engine bound check, bridge Ghidra correctness, plan-consistency/completeness - each finding independently re-verified by 2 skeptical agents): 12 raw findings, 11 confirmed, 1 rejected as not real (a claimed missing type-check on `ghidra_start`/`ghidra_end`/`ghidra_address` - refuted by direct reproduction: the MCP SDK's `inputSchema` validation runs before dispatch and already rejects a non-integer there, confirming the schema-level bound additions above are a genuinely load-bearing backstop, not decoration). All 11 confirmed findings fixed as described above.

#### 2.17 Symbol annotation, in the bridge
**Bridge. Effort: M. Status: done.**

Every address in every response is a bare integer. A transcript full of `0x1E4F2` is unreadable and unmemorable, and the agent re-queries Ghidra per address.

**Build it in the bridge, not the engine.** The symbol data originates from Ghidra, which the agent talks to; the Ghidra-to-live mapping already lives in `tools/ghidra.py`; closure-held state is the established pattern there. An engine-side symbol store would mean a 20000-entry, 128-byte-name untrusted payload retained in emulator memory, a new webserver module, address validation and a protocol bump, all for metadata with zero emulator-side semantics. It also works on stock non-debugger builds for free, which was the original motivation for keeping it out of `src/debugger/`.

`debug_symbols_load` takes `list_functions`/`list_globals` output directly and translates through 2.16. Then annotate every address-bearing response the bridge already relays: disassembly lines, stop records, backtrace frames, and the MCB map.

The one thing that needs engine cooperation is 2.5 returning structured operand and target addresses rather than only a rendered string.

**Depends on:** 2.5, 2.16.

**Shipped.** No engine changes at all - this landed entirely in `dosbox-mcp`, on top of 2.5's structured disassembly and 2.16's already-shipped Ghidra range mapping.

- **New `dosbox_mcp/tools/symbols.py`**: closure-held `{ghidra_address: name}` table plus `debug_symbols_load` (JSON-first, line-regex-fallback parser for `list_functions`/`list_globals`/`list_functions_enhanced`-shaped text; unparseable lines/entries are skipped, not a load failure) and `debug_symbols_status`. Both registered in a new `"symbols"` group added to `server.py`'s `_LOCAL_ONLY_GROUPS` (same reasoning as `"mapping"`: pure bridge-local bookkeeping, never reaches the engine, so it survives observe mode). `make_annotator(ghidra_state, symbol_state)` returns a bound `annotate(live_segment, live_offset)` doing a nearest-symbol-at-or-below lookup (`bisect`) with a `MaxSymbolDistance` (0x10000) cap so a huge, meaningless `+0xN` offset past the last known symbol doesn't get reported as if it meant something. `MaxSymbols` (20000 entries) and a schema-level `MaxTextLength` (8 MiB, matching `mem_write`'s existing `maxLength` convention for bridge tool inputs) cap resource use on a pasted load.
- **`server.py`'s `build_server()`** now registers `ghidra`/`symbols` before `memory.register_search()`/`debug.register()`, builds one `annotate_fn`, and threads it into both - `debug.py` gained an `annotate=None` param wired into `_status`/`_pause`/`_step`/`_step_over`'s stop records (nested under `"stop"`), `_wait`'s response (the same fields, but flat at the top level rather than nested - `debug/wait`'s own shape), `_disassemble` (each instruction's own address and, when present, its relative-branch `target`), and `_backtrace` (each frame); `memory.py`'s `register_search()` gained the same param, wired into `_dos_memory_map` (each MCB block).
- **Adversarial review (3 dimensions - correctness, security-and-robustness, wiring-and-integration - each finding independently re-verified by 2 skeptical agents): 6 raw findings, 5 confirmed, 1 rejected.** The rejected one (`debug_breakpoint_add`/`debug_breakpoint_list` not annotated) held up on the facts but not the verdict: both verifiers found it matches the plan's own literal four-category enumeration ("disassembly lines, stop records, backtrace frames, and the MCB map") rather than an accidental gap - a deliberate scope boundary, not a bug. All 5 confirmed findings fixed:
  - **(high) `dos_memory_map` annotated the wrong segment, one paragraph off, silently making the feature's own headline promise for that route unreachable for any real DOS memory block.** `block["segment"]` is the MCB *header* paragraph (`dos.cpp`'s `WalkMcbChain`, and this bridge's own `ghidra.py` already treats it the same way); the block's owned/addressable memory - what any range is ever actually anchored at, via `cpu_read_registers` or `debug_map_auto`'s `pspSegment + 0x10` convention - starts one paragraph later. `annotate(block["segment"], 0)` could therefore never match a genuinely anchored range; masked in the original tests because their fake MCB fixtures set `segment` directly equal to the anchor, which no real engine response ever does. Fixed to `annotate(block["segment"] + 1, 0)`. Live-reverified against `build-debugger`: anchored a range at a real block's owned segment (header 0x016f, owned 0x0170) and confirmed the block now gets the correct `symbol`.
  - **(medium) The debugger's own `"never_stopped"` placeholder stop record (before the debugger has paused even once since engine start) could still get annotated as if CS:EIP were a real position.** `DebugStopInfo`'s default has `reason: "never_stopped"` but a real (all-zero, not empty/missing) `registers` object, so the existing `if not regs: return` guard didn't catch it - a range anchored at `live_segment=0` (schema-legal, and a real DOS segment: the IVT/low BIOS area) with a symbol near ghidra address 0 would have annotated a stop that never happened. Fixed by returning early whenever `stop["reason"] == "never_stopped"`.
  - **(high) A deeply/pathologically nested JSON paste to `debug_symbols_load` raised an uncaught `RecursionError` instead of degrading to line-based parsing, breaking the module's own documented contract.** `_parse_json_entries` only caught `(json.JSONDecodeError, TypeError)`; CPython's decoder recurses per nesting level and raises `RecursionError` (a `RuntimeError`, not a `ValueError`) well before the 8 MiB text cap is ever reached - reproduced directly with a 2 KB `"["*1000 + "]"*1000` payload on this project's own Python 3.11 venv. Fixed by widening the catch to any exception around `json.loads`, since the function's whole job is "try JSON, and on any failure let the caller fall back to text parsing."
  - **(medium) `debug_symbols_load`'s `MaxSymbols` cap dropped every entry after the cap was first hit in a batch, not just the ones that would have grown the table - including legitimate updates to already-loaded addresses, silently, with no signal in the returned counts.** The cap check (`len(symbols) >= MaxSymbols and addr not in symbols`) correctly exempted updates from counting against the cap, but the enforcement was `break`, aborting the whole batch rather than skipping just that one new entry - so an update later in the same call never ran, and neither `loaded` nor `skipped_lines` reflected it. Fixed: `break` to `continue`, plus a new `dropped_at_cap` field in the response (present only when nonzero) so a caller can actually tell.
  - **(medium) A stop record's own nested `breakpoint` sub-object - present whenever `reason == "breakpoint"` - carries its own `segment`/`offset`, the armed/watched location, distinct from CS:EIP and unannotated.** For a memory watchpoint in particular, the interesting address is the watched byte, not wherever CS:EIP happened to be when it changed (which can be anywhere in the code) - an agent still had to round-trip that address through `debug_map_to_ghidra` separately, the exact per-address lookup 2.17 exists to eliminate. Fixed: `_annotate_stop_dict` now also annotates `breakpoint.segment`/`breakpoint.offset` as `breakpoint.symbol`, but only when `breakpoint.type` is `"execute"` or `"memory"` - an `"interrupt"` breakpoint's `segment`/`offset` are meaningless placeholders (always 0/0), and annotating them could spuriously match whatever symbol sits at ghidra address 0.

**Verification:** bridge-only change, no engine build involved. Full `dosbox-mcp` suite grew from 292 (before this item) to 335 (initial implementation) to 342 (after the review-fix pass: `tests/test_symbols_tools.py` new, plus additions to `test_debug_tools.py`, `test_memory_tools.py`, `test_server.py`), 2 pre-existing skips unaffected. Live-verified twice against a real `build-debugger` binary through the real MCP dispatch path (`server.request_handlers[types.CallToolRequest]`, not just tool-module functions): first pass confirmed `debug_status`/`debug_disassemble`/`debug_backtrace` all correctly annotate CS:EIP/instructions/frames, including the nearest-symbol `+0xN` offset math across a run of sequential instructions, while an unrelated MCB block correctly got no `symbol` (no false positives); second pass, after the fixes, confirmed `dos_memory_map` now correctly resolves a real block's owner (the off-by-one-paragraph fix) and that the `"never_stopped"`/breakpoint-annotation guards' pure-Python logic is exercised by the new unit tests (the specific `"never_stopped"` and refiring-breakpoint live states weren't reproducible in this particular headless boot sequence - the instance had already stopped once by the time it was reachable, and a planted breakpoint at the paused position didn't refire within the wait window - so those two were confirmed via fabricated-but-structurally-verified fixtures instead, not live).

#### 2.18 Batch endpoint
**Engine + bridge. Effort: M. Value partly speculative.**

Every route is one request at one instruction boundary, so a struct read spanning two calls tears, a read-modify-write is racy, and a six-register VGA sequence costs six requests during which the CRTC index has moved. The only atomicity primitive is single-address CAS.

`POST /api/v1/batch {ops, on_error}` with a deliberately narrow op set (mem read/write/cas, cpu read/write, port read/write, freeze set/clear), fully parsed and range-checked on the web thread into a POD list, applied in order in one `Execute`. Caps: 64 ops, 1 MiB read, 256 KiB write, no nesting, no input/script/debug/capture ops. Scale the timeout as `250 + 4*ops` capped at 2000.

**This is the largest new untrusted-input parser in the codebase and it sits directly on the trust boundary.** Do not build it before 1.4 establishes the pure-validator pattern and the malformed-input corpus. The correctness argument is sound; whether agents actually hit the tearing case often enough to justify the parser is unproven.

---

## Tier 3: ergonomics and polish

### 3.1 One risk taxonomy driving annotations and the capability mode
**Bridge. Effort: M.**

`add_tool` sets `ToolAnnotations(readOnlyHint=read_only)` and nothing else, and the same boolean also drives `_mode_allows`, which approximates risk at group granularity via `_INTERACT_GROUPS`. That approximation is why `script_run` is available in the mode whose stated purpose is to withhold memory writes, while Lua exposes `mem_write`, key/mouse injection and `mount_lock`.

Replace the boolean with an explicit classification (`read`, `mutate_guest`, `mutate_host`, `destructive`, `lifecycle`) plus a `title`. Derive annotations mechanically and derive the mode gate from the same value, removing `_INTERACT_GROUPS` and forcing per-tool classification. Classify `script_run` as `mutate_guest`. Note the spec default: `destructiveHint` defaults to true when `readOnlyHint` is false, so declaring it explicitly on the *non*-destructive mutators (`mem_write`, `freeze_set`, `input_type`) matters as much as on `dosbox_shutdown`. `idempotentHint` stays a per-tool declaration: `mem_write` is idempotent, `input_type` emphatically is not.

Tighten every schema. mcp 1.29 validates arguments against `inputSchema` before dispatch, so each constraint is enforced for free: `enum:[1,2,4]` on width, `[1,2]` on io width, port and segment `maximum:65535`, `maxLength:4096` on text, `maxItems:32000` on events, `cps` 1..1000 (and clamp engine-side too), `additionalProperties:false` everywhere. On `input_sequence` events, a `oneOf` per type matches the engine's per-type allow-list exactly and catches the `x` vs `x_rel` typo the engine hard-400s on. Source the numbers from 2.1's limits so they cannot drift.

Fix the two false descriptions: `mem_read`'s "max 65536" against a 128 MiB engine cap, and `script_run`'s claim that any capability is reachable through Lua (the Lua surface has no port I/O, no CPU registers, no debugger, no freeze, no search, no drive swap, no framebuffer, and relative-only mouse with no wheel). Do **not** "fix" the claim that there is no absolute mouse positioning until 2.14 lands: it is currently true.

**`outputSchema` is a trap as usually sequenced.** Once a tool declares one, the SDK returns an error result if the handler returns unstructured content only. Every guarded handler returns a plain content list on its error paths, so adding `outputSchema` to `cpu_read_registers` would turn "Cannot reach dosbox at ..." into "Output validation error". Land 1.5 first, then migrate each tool's return to the structured form in the same commit that adds its schema.

**Dependency floor:** `pyproject.toml` pins `mcp>=1.2,<2`. `Tool.title` and `outputSchema` do not exist that far back, and argument validation landed much later than 1.2, so on a minimum-floor install none of this is enforced. Raise the floor.

Add a test that iterates the registry and fails on any tool without a title, a risk class, and consistent annotations.

**Depends on:** 1.5, 2.1.

### 3.2 Bridge output rendering
**Bridge. Effort: S.**

`dos_memory_map` fetches `/dos/internals`, discards list-of-lists, the SDA and first-shell, and dumps up to 1000 raw MCB objects at `indent=2`. `dosbox_status` fires three Bridge round trips for largely duplicate data and reports the program name three times. `mem_search` re-dumps the engine's array at `indent=2`, one integer per line. Summarize by default with a `detail:true` escape hatch, and stop pretty-printing arrays.

### 3.3 screen_capture parameters
**Engine + bridge. Effort: S. Depends on 1.3.**

`screen.py:46-50` hardcodes `format=png` and exposes nothing else, while the engine offers jpeg with quality, raw, and `mode=rendered`, so the agent can never see the post-shader image the human sees, never trade quality for size, and never ask for a thumbnail. `encode_png` pins compression level 1, roughly double the bytes of level 6.

Add `mode`, `format`, `quality`, `scale` (fixed enum divisor, box filter on the RGB888 buffer, on the web thread) and `crop` (validated against the actual frame dimensions after acquisition, rejected not clamped, applied before `convert_to_rgb888`). Raise the PNG default to level 6 but keep it parameterized.

**Do not default to jpeg.** Chroma subsampling smears text-mode glyphs and CGA/EGA dithering, which is exactly what the vision model has to read. PNG at level 6 (lossless, roughly half today's bytes) is the right default; let the agent opt into jpeg for large graphics frames.

### 3.4 Bridge cleanup
**Bridge. Effort: S.**

- Delete `bridge_help`: it re-sends the tool list the client already received, and derives one-liners via `description.split(". ")[0]`, which collapses `debug_breakpoint_add`'s three-kind explanation to "Add a breakpoint".
- Delete `bridge_version`: a strict subset of `bridge_status`.
- Add `debug` and `control` to `KNOWN_ROUTE_PREFIXES`. `/api/v1/control/shutdown` is already a false positive today, and all seven debug routes join it the moment openapi.json is corrected, at which point `bridge_swagger` starts telling the agent that tools the bridge itself exposes are outside the protocol.

**Do not fold 48 tools into action enums.** Measured, the whole registry is ~18.4k chars and the realistic saving is 1.5 to 2k, not a halving. Worse, `_mode_allows` gates registration per tool from `read_only`, and every family named for folding mixes read and write actions, so merging makes each merged tool non-read-only and observe mode loses `freeze_list`, `video_capture_status`, `script_status`, `debug_map_status`, `bridge_status`, `bridge_logs` and `bridge_swagger`. That is a straight regression for the mode whose purpose is read-only observation. If you want folding, fold only within one `read_only` class.

### 3.5 Script ergonomics
**Engine + bridge. Effort: S.**

Split `script_run` into `script_load(script, name, seed, debug, start=True)` and `script_start`, passing the three query params the engine already validates and that MCP can reach none of today. Map 429 to a message quoting `Retry-After`.

Engine: `GET /api/v1/script/log` serving the remembered `mgr.Log().FilePath()`, tail-capped, with **no caller-supplied path component at all**, refusing when no debug script is loaded. Open a separate read handle: the emulation thread holds the FILE* open and writes to it, so accept a possibly-torn last line and never route the read through the Bridge. Fold the path into `script/status`.

Separate commit, standalone bug fix: move the rate-limiter check in `LuaLoadCommand::Post` **below** the content-type, body and param validation. Today a request about to be rejected 415/413/400 still burns the 2-second slot, so a Content-Type mistake costs two seconds per retry.

### 3.6 Instance identity and restart detection
**Engine + bridge. Effort: S.**

`Connection.call` catches a RuntimeError containing "401", detaches, re-attaches, and **replays the request**. That is exactly what an engine restart looks like from outside. After the silent re-attach, the freeze registry, the loaded script, every breakpoint and the entire guest state are gone, and the agent keeps typing into a fresh COMMAND.COM and misreading every screen after that. Replaying a mutating request (an input sequence, a memory PUT) into a fresh DOS session is worse than an error.

Generate a 128-bit `instance_id` at `WEBSERVER_Init` reusing the CSPRNG in `generate_api_token` (same fail-closed posture) and add `{instance_id, pid, started_at_unix, uptime_ms}` to the **authenticated** `dosbox/info` only. Bridge: store it at attach, compare after re-attach, raise a distinct `EngineRestarted` carrying both ids instead of replaying. Report it in `bridge_status`, which currently reports `engine_name` from a field the engine structurally never sends.

**Do not add a `webserver_state_dir` setting.** Parallel instances are already isolatable and the engine's own harness proves it: `conftest.py:76-91` runs concurrent instances with HOME + XDG_CONFIG_HOME + `DOSBOX_API_TOKEN`, and an env-token instance never writes the token file at all. **Do not teach macOS `get_or_create_config_dir()` to honour `XDG_CONFIG_HOME`** either: plenty of macOS users set it globally, and making it authoritative silently relocates the config dir of every existing install. HOME is the isolation lever.

### 3.7 Move handlers off the event loop
**Bridge. Effort: S.**

`async def call_tool` calls the sync handler inline, and every handler makes a blocking httpx call with a flat 30 s timeout. One slow call (a `mode=rendered` frame waits up to 2 s in the engine) stalls the whole server: no concurrency, no cancellation, no progress notifications. Move handlers to `anyio.to_thread.run_sync`.

**Prerequisite for** any background work in the bridge, including resource subscriptions.

### 3.8 Bound the Lua output serializer and get it off the emulation thread
**Engine. Effort: M.**

`LuaTableToJson` caps recursion depth at 10 and nothing else: no node budget, no byte budget, no visited set. Ten tables each holding ten references to the previous one is about a hundred Lua slots and expands to 10^10 emitted nodes, inside a Bridge Command, on the emulation thread, with the Bridge mutex held. Because `ProcessRequests` has no try/catch (until 1.6), the resulting `bad_alloc` takes the process down.

Add a budget struct (max nodes, max bytes, a visited set of `lua_topointer` values) and report `output_truncated`. Split the work: `Execute` copies into a plain owned structure already reduced by the budget; the nlohmann conversion and dump happen on the web thread in `Get`. The intermediate representation must own its strings, since the Lua GC runs between phases.

`dosbox.output` is the only structured channel a script has back to the caller, so agents will use it heavily. Do this before encouraging that.

**Depends on:** 1.1 (nothing uses scripts today), 1.6.

---

## Tier 4: hygiene and infrastructure

### 4.1 Regenerate openapi.json and keep it honest
**Engine. Effort: S. Highest value in this tier.**

The spec is declared normative by `PROTOCOL.md`, is served unauthenticated, and is what `bridge_swagger` reads. It documents 36 paths against 48 registered, omits all seven debug routes, and still says `info.version 0.84-da1` against 0.84-da3. The only test asserts `openapi == "3.1.0"` and that `/api/v1/status` is a key.

Regenerate, and add a test that walks the registered route table and asserts every `(path, method)` appears in the spec. Note the naming divergence while regenerating: the spec table writes `memory/{offset}/{length}`, the engine registers `{len}`.

Also, `config_home` is mounted at `/` ahead of `resource_home`, so a file dropped in the user-writable config dir shadows the shipped resource and is served unauthenticated, including this normative document. Worth a decision.

### 4.2 Close the protocol negotiation loop
**Engine + bridge + protocol. Effort: S.**

The engine emits no `mcp_protocol` and no `name`, and `/api/v1/hello` does not exist, so `effective_version()` always takes the implicit-1.0 branch while both peers ship the entire 1.1 debug surface. `bridge_status` reports protocol 1.0, `engine_name` is permanently null, and `bridge_swagger` will flag the debug routes as outside the protocol.

Add `name` and `mcp_protocol` to `dosbox/info`, sourced from one `constexpr` in `webserver.h`. Register `GET /api/v1/hello` returning exactly `{name, version, mcp_protocol}` and nothing else: it is pre-auth, so no pid, no uptime, no state read, no Bridge crossing. Add a separate `IsPublicApiPath(method, path)` predicate consulted before the bearer check, exact-match on the one string, GET/HEAD only, Host allowlist still applied. **Do not widen `IsPublicDocPath`**: the comment there explains exactly why it is exact-match, and the config_home mount makes any widening a path toward the token file.

Make `KNOWN_ROUTE_PREFIXES` a dict keyed by minor. Raising `BRIDGE_PROTOCOL` to 1.1 is cosmetic until something consumes it.

**Do not gate tool registration on the negotiated version.** `Connection` starts disconnected by design and `bridge_start` must spawn before any attach exists, so at `build_server()` time there is no negotiated version; registration-time gating on connection state would break `bridge_connect` and `bridge_start`. Feature gating stays call-time, inside `guard()`, which is what the spec already says.

### 4.3 Tests
**Both. Effort: L.**

The gaps, in rough priority:
- 29 of ~33 REST-backed MCP tools have never issued a real or mocked HTTP request in any test; they are only asserted to be registered by name. A wrong path, verb or body ships green. The two route tests that exist were both written after production bugs.
- The debugger REST group (7 routes) has zero tests in either repo, and the 501-not-built shape is unasserted.
- `memory/freeze`, `io/port`, `memory/search` and `cpu/register PUT` have no HTTP-level test anywhere. They back 8 MCP tools.
- `If-Match`/412 has no test on either side.
- No C++ test for `Bridge` (the stated trust boundary) or for `input.cpp` (the largest untrusted-input parser).
- `test_api_contract.py:73` hardcodes `features["debugger"] is False`, so the existing debugger build cannot pass the suite. Parametrize over the build (read the expected value from the served block or an env marker) rather than deleting the assertion.
- `tests/run-e2e.py` reaches 5 of 13 suites; mount policy, path traversal, token provisioning, openapi docs, screen text and graphics modes are invisible to the documented runner.
- The bridge has no live-engine lane at all. The engine's harness (`conftest.py`) is good and is not packaged, importable or published, so the bridge cannot reuse it.

### 4.4 CI
**Both. Effort: M.**

The engine's four workflows only deploy the website. The bridge repo has no `.github` at all. Nothing runs gtest, `run-e2e.py`, pytest, ruff or bandit automatically, and nothing runs the two repos against each other.

### 4.5 Delete the stale trees
**Engine. Effort: S.**

`extras/mcp/` is a six-week-old fork of a third of the bridge (no `protocol.py`, `lifecycle.py`, `cli.py`, `tools/bridge.py`, `tools/ghidra.py`, no PROTOCOL.md), is not packaged, is referenced by no build rule, is license-attributed in the shipped `THIRD_PARTY_LICENSES.txt`, and its README tells developers to test against it. Anyone reading the engine repo for the MCP surface reads the wrong code. Delete it or reduce it to a pointer.

`extras/api/` ships a JS client that sends no Authorization header (so it cannot work against a token-enforcing engine), declares response fields the engine deliberately removed, has zero tests, and is the only place `compareAndSwap` and `alloc`/`free` are exercised at all. Decide: fix it or delete it.

### 4.6 Make the default token obtainable
**Engine. Effort: S.**

With `webserver_token_file=false` (the default) and no `DOSBOX_API_TOKEN`, the only output is `"API token: %.8s..."`. The full 64-char token is never printed anywhere. Meanwhile the setting help says "generated at startup and printed to the log output", the token_file help says "instead of printing it to the log", and the OpenAPI security scheme says "the API token printed to the log at startup". The out-of-the-box configuration cannot be authenticated to, and three user-facing texts claim otherwise. Either print it or fix all three texts and change the default.

### 4.7 Token scopes
**Engine + bridge + protocol. Effort: L. Value depends on deployment.**

The engine issues one all-powerful token. The only least-privilege control is the bridge's `mode`, enforced at registration time inside one Python process, invisible to the engine and bypassable by any local process holding the token, or by the agent itself through `script_run`.

A `webserver_token_scopes` setting (`read, write, input, script, media, debug, control`) checked in the pre-routing handler right after the bearer comparison, backed by a pure `RequiredScopeFor(method, path)` table, would let an operator hand out a read+input token honestly and would close the `script_run` escape hatch at the layer that can actually close it.

**Be honest about when this pays:** it matters for unattended runs and for multiple agents sharing a machine. For a single supervised agent it is policy overhead. The table is a security control that must be exhaustive and fail closed, with a test asserting every registered route appears in it, or it will drift exactly as openapi.json did.

---

## Dependency graph

```
1.1 script_run          (independent, do first)
1.2 bridge_start        (independent)
1.3 frame RAII    ------------------------------> 3.3 screen_capture params
1.4 validation sweep ---------------------------> 2.10 alloc tools
                     \-------------------------> 2.12 replay lifecycle
                      \------------------------> 2.18 batch
1.5 error envelope ----> 1.8 wait
                    \--> 2.2 stop record (long poll)
                     \-> 3.1 outputSchema
1.6 bridge hardening --> 1.8 wait (paused case)
                     \-> 2.12 replay (same file, recording handlers)
                      \> 3.8 lua serializer
1.7 change detection --> 1.8 wait (screen_change)
2.1 capabilities ------> 3.1 schema maximums
2.2 stop record -------> 2.4 execution control
                    \--> 2.6 conditional breakpoints
2.4 stable ids --------> 2.6
2.5 disassembly -------> 2.7 backtrace -> step-out
                    \--> 2.17 symbols (structured operands)
2.9b masked scan ------> 2.16 ghidra auto-anchor -> 2.17 symbols
2.13 mouse coalescing -> 2.13 recording store (do coalescing first)
3.7 async handlers ----> any bridge background work
```

Critical path for the biggest win: **1.5 -> 1.7 -> 1.8**. Everything else can proceed in parallel.

---

## Speculative, deferred, or rejected

**Machine checkpoints (RAM + CPU snapshot with restore).** XL, highest risk on the list. Converts brittle automation into search, which is genuinely transformative, but restoring RAM under unrestored device state (VGA CRTC, DMA mid-flight, sound buffers) wedges the guest in ways that look like emulator bugs, and missing the dyn-core cache flush produces silent wrong execution rather than a crash. If attempted: name it `checkpoint` not `savestate`, default it off behind a byte budget, return a `not_restored` list on every restore, refuse across a video-mode change, and refuse while a capture, replay or script is in flight. Do not start it before Tier 1 is done.

**Server-sent event stream.** Largely subsumed by 1.8 and 2.2. MCP has no server-push to the model, so the bridge would consume the stream in a background task and surface it as a drainable buffer, which is real client-side work on top of the engine change. Long-lived connections in a thread-per-connection server need a hard concurrency cap. Revisit only if a consumer appears that waits cannot serve.

**DOS/BIOS interrupt trace ring.** Plausible and cheap when disabled (one relaxed atomic on the hot DOS path), and it would work on every core and on non-debugger builds where breakpoints do not. But the value ("why did the installer fail") is unproven against just reading the screen, and it puts a hook on `DOS_21Handler`. Guest-controlled paths going into a JSON response need the same sanitization `TITLEBAR_NotifyProgramName` does.

**MCP resources and subscriptions.** The ETag half is in 1.7 and pays off unconditionally. The resources half depends on client support that is uneven, and on 3.7 landing first, or the notification channel is silently unreliable, which is worse than not having it. Land 1.7 and measure before building `resources.py`.

**Backtrace (2.7).** Kept in the plan but honestly heuristic in DOS real mode. Expect most stops to resolve one or two frames.

**Folding 48 tools into action enums.** Rejected. Measured saving is small and it regresses observe mode. Keep only the four cleanups in 3.4.

**Engine-side symbol table.** Rejected in favour of 2.17. Pure metadata with zero emulator-side semantics does not belong behind the trust boundary.

**`webserver_state_dir` and macOS XDG support.** Rejected in 3.6. Redundant with existing env-based isolation, and the macOS change silently relocates existing installs.

**Enriching `/dos/internals` with PSP, environment, current drive, DOS version.** Rejected. Contradicts the documented purpose of that command, and all of it is derivable from the pointers it already returns.